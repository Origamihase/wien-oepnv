name: Update VOR cache

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    uses: ./.github/workflows/reusable-update-vor-cache.yml
    secrets: inherit
    with:
      runner: ubuntu-latest
