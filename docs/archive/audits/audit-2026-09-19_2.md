# Audit 2026-09-19 (2) — Offene Punkte und Erkenntnisse

## Audit Update 15:45 (Europe/Vienna)

**Anlass:** Bestandsaufnahme nach einer Runde Feed-Korrekturen (#1835–#1843).
Diese Datei hält fest, was geschlossen wurde, was beim Nachmessen neu auffiel
und was offen bleibt.

**Modus:** gemischt. Die Befunde unten sind neu gemessen; die Runde davor hat
Code verändert (Liste in §1). Die Messungen in §2 sind rein diagnostisch —
zu G.1 und G.2 wurde **nichts** implementiert.

**Stand:** `e92612f` (Branch `claude/practical-clarke-jwp1na`), Basis
`d418e93` auf `main`.

**Abgrenzung:** `audit-2026-09-19.md` (gleicher Tag) trägt zwei fremde Befunde
zur WL-Titel-Zerlegung. Diese Datei ergänzt sie und überschreibt nichts; die
Nummerierung `_2` folgt `audit-2026-09-13_2.md` / `_3.md`.

---

### 1. Geschlossen in dieser Runde

| PR | Wirkung | Rang nach `AGENTS.md` |
| --- | --- | --- |
| **#1835** | WL-Verweis-Baustein („Nähere Informationen … sind der Auskunft der Wiener Linien zu entnehmen") fiel aus den Beschreibungen; `repair_glued_words` erkennt verklebte Funktionswörter (`ImBaustellenbereich`) | 1 — deutscher Feed |
| **#1836** | `_title_body` extrahiert; Grundlage für die Dedupe-Vergleiche | 1 — deutscher Feed |
| **#1837** | Wiederholung wird erkannt, wenn nur der Titel den Richtungspfeil verlor | 1 — deutscher Feed |
| **#1838** | zusammengeführte Meldungen lasen sich doppelt (Joiner-unempfindlicher Vergleich) | 1 — deutscher Feed |
| **#1839** | `docs/architecture.md` §8 beschreibt den zweisprachigen Feed; README nennt den EN-Feed | 4 — Doku |
| **#1842** | Artikel vor der Linienkennung gestrichen (`the line 17A` → `line 17A`), Cache-Epoche 12 → 13 | 2 — EN-Feed |
| **#1843** | Artikel vor Straßen- und Platznamen gestrichen (`to the Karlsplatz` → `to Karlsplatz`), Epoche 13 → 14 | 2 — EN-Feed — **offen, in Review** |

**Noch nicht live bestätigt:** #1842. Der erste `chore: update cycle` nach dem
Merge (13:31 UTC) lief mit der neuen Epoche, aber im aktuellen Zehnerfenster
steht kein Item mit Artikel vor einer Linienkennung — `line 46`, `line M`,
`line O` stehen dort bereits bloß, und sie standen es auch vor dem Merge. Die
Regel ist erst nachweisbar, wenn wieder ein betroffenes Item ins Fenster kommt.

---

### 2. Neue Befunde

Beide betreffen den **deutschen** Feed und damit Rang 1 der Ausgaben-Priorität.
Nach der Bewertungsreihenfolge in `AGENTS.md` stehen sie auf Stufe 2
(„verfälscht oder verstümmelt, was im deutschen Feed steht") und damit **vor**
den verbliebenen EN-, Statistik- und Log-Befunden.

Zusammen: **mindestens 19 von 250** veröffentlichten deutschen Items (7,6 %)
tragen eine wortwörtlich wiederholte Phrase in der Beschreibung. 19 ist eine
Untergrenze — die Messschwelle übersieht kürzere Dopplungen, siehe §7.

---

#### G.1 — Identischer Satz je Linie, einmal bloß und einmal mit `Linie X:` davor — **MITTEL**

**Betroffene Komponente**

Wiener-Linien-Provider, Beschreibungs-Zusammenbau bei mehrzeiligen Störungen
(`src/providers/wl_text.py` / `src/providers/wl_lines.py`, Beschreibungspfad).

**Problembeschreibung**

Die WL-Quelle liefert bei einer Störung, die mehrere Linien betrifft, je Linie
einen eigenen Text. Sind diese Texte identisch, landen trotzdem zwei Kopien in
der Beschreibung — die zweite nur um ein `Linie X:` erweitert:

```
TITEL: 51B/16A/51A/53A/60A/86A/87A: Verkehrsüberlastung
DESC : Unregelmäßige Intervalle in beiden Richtungen.
       Linie 16A: Unregelmäßige Intervalle in beiden Richtungen. [Am 14.09.2026]

TITEL: 1A/2A/3A: Veranstaltung
DESC : Derzeit ist ein Betrieb nicht möglich.
       Linie 2A: Derzeit ist ein Betrieb nicht möglich. [Am 17.09.2026]
```

Der zweite Satz trägt nichts bei. Schlimmer: er greift **eine** der betroffenen
Linien heraus — im ersten Beispiel eine von sieben — und suggeriert damit eine
Einschränkung, die die Meldung gar nicht macht. Der Titel zählt bereits alle
sieben Linien auf.

**Gemessen: 13 von 250** veröffentlichten deutschen Items (5,2 %), über den
gesamten Feed-Verlauf. Die Beschreibung ist auf dem Info-Display die einzige
Stelle, an der die Störung erklärt wird; hier ist die Hälfte davon eine Kopie.

**Konkreter Lösungsvorschlag**

Beim Zusammensetzen der Beschreibung jedes `Linie X: <Text>`-Segment verwerfen,
dessen `<Text>` wortgleich mit einem bereits aufgenommenen Satz ist. Das
`Linie X:`-Präfix trägt nur Information, solange sich die Texte *unterscheiden*
— sind sie gleich, ist die Linienzuordnung schon im Titel vollständig und
korrekt enthalten.

Abgrenzung beim Umsetzen: der Vergleich muss wortgleich sein (nicht „ähnlich"),
sonst verschluckt er echte, unterschiedliche Per-Linien-Texte. Ein Testfall mit
zwei *verschiedenen* Linientexten gehört zwingend dazu.

---

#### G.2 — Zusammengeführte Meldungen: der Titel faktorisiert, die Beschreibung nicht — **MITTEL**

**Betroffene Komponente**

Item-Zusammenführung (`src/feed/merge.py`) — Beschreibungspfad; der Titelpfad
ist teilweise bereits korrekt.

**Problembeschreibung**

Werden zwei Meldungen derselben Linie zusammengeführt, faktorisiert der Titel
den gemeinsamen **Anfang** heraus und hängt nur die abweichenden Enden an. Die
Beschreibung tut das nicht — sie verkettet beide Quelltexte wortwörtlich,
gemeinsamen Anfang inklusive:

```
TITEL: O: Schadhafter Bus Betrieb ab Praterstern, Quartier Belvedere      ← faktorisiert
DESC : Schadhafter Bus Betrieb ab Praterstern
       Schadhafter Bus Betrieb ab Quartier Belvedere                      ← nicht faktorisiert

TITEL: 6: Fremdunfall Züge halten bei den Linien 1, 18, 62 WLB, der Linie O
DESC : Fremdunfall Züge halten bei den Linien 1,18,62 WLB
       Fremdunfall Züge halten bei der Linie O
```

Ein zweiter Fall liegt anders: dort ist das **Ende** des ersten Textes zugleich
der **Anfang** des zweiten, und hier stottert auch der Titel:

```
TITEL: 11: Fahrtbehinderung PKW im Gleis & PKW im Gleis Betrieb ab Raxstraße
DESC : Fahrtbehinderung PKW im Gleis PKW im Gleis Betrieb ab Raxstraße

TITEL: 26E: Ersatzbus hält Karl-Waldbrunner-Platz vor Schloßhofer Straße,
            Kurt-Waldbrunner-Platz vor Schloßhofer Straße
```

Im letzten Beispiel steht überdies `Karl-Waldbrunner-Platz` neben
`Kurt-Waldbrunner-Platz`. Richtig ist `Karl-` (so schreibt es auch der aktuelle
WL-Cache an allen vier Fundstellen); `Kurt-` war ein Einzelfall am 14.09. Ob
die Verschreibung aus der Quelle stammt oder in der Pipeline entsteht, ist
**nicht nachgeprüft** — der Cache von damals liegt nicht mehr vor. Sie erklärt
aber, warum die Zusammenführung die beiden nicht als dieselbe Haltestelle
erkennt, und sollte beim Umsetzen von G.2 mitgeklärt werden.

**Gemessen: mindestens 6 von 250** veröffentlichten deutschen Items (2,4 %),
davon 2 mit stotterndem Titel. Untergrenze: mindestens fünf weitere Items —
darunter `Linien 25 und 26 Betrieb ab Josef-Baumann-Gasse Ersatzbus ab
Josef-Baumann-Gasse` — fallen unter die Drei-Wort-Messschwelle und gehören
ebenfalls hierher (§7).

**Konkreter Lösungsvorschlag**

Zwei Mechanismen, einer davon existiert schon:

1. **Gemeinsamen Anfang faktorisieren** — die Logik, die der Titel bereits
   anwendet, auf den Beschreibungspfad übertragen, statt sie ein zweites Mal
   zu schreiben.
2. **Überlappung trimmen** — beim Verbinden von `A` und `B` das längste
   Wort-Suffix von `A` entfernen, das zugleich Wort-Präfix von `B` ist
   (Mindestlänge zwei Wörter, nur an Wortgrenzen). Das repariert
   `Fahrtbehinderung PKW im Gleis` + `PKW im Gleis Betrieb ab Raxstraße`
   → `Fahrtbehinderung PKW im Gleis Betrieb ab Raxstraße` und greift für
   Titel **und** Beschreibung.

Reihenfolge: (2) vor (1), weil die Überlappungs-Trimmung auch den Titel heilt
und die kleinere, klarer abgrenzbare Änderung ist.

---

### 3. Richtigstellungen zu früheren Befunden

#### Audit 2026-09-18, Punkt 5 (ÖBB `<b>` / `<br>`-Reste) — **geschlossen für die Ausgabe**

Nachgemessen an `d418e93`: in `docs/feed.xml` und `docs/feed.en.xml` steht
**kein** `<b>`, `<strong>`, `</b>` oder die Zeichenkette `bold`. Beide Feeds
enthalten je genau **fünf** `<br/>` — und alle fünf trennen korrekt die
Beschreibung vom Zeitfenster:

```
Nach einer Fahrtbehinderung kommt es zu unterschiedlichen Intervallen.<br/>[Am 19.09.2026]
```

Das ist die vorgesehene Formatierung, kein Rest. Der Befund beschreibt den
**Cache**, nicht das veröffentlichte Ergebnis — derselbe Fehlertyp wie bei D.2
(Validator meldet zu einer Eigenschaft, die er nicht prüft) und wie bei Punkt 4
derselben Datei, der am 2026-09-18 aus demselben Grund geschlossen wurde.

#### Audit 2026-09-18, Punkt 1 (Artikel vor Eigennamen) — **geschlossen durch #1842 und #1843**

Der Befund nannte 4 Vorkommen und hielt den Fix für heikel, weil „the" vor
manchen Eigennamen korrekt ist. Beides war zu eng gefasst:

* **Die Zahl.** Der Befund erfasste die **kleinere** Hälfte. Neu gemessen über
  456 eindeutige EN-Texte: **5** Vorkommen vor Straßen- und Platznamen
  (`the Karlsplatz`, `the Kästenbaumgasse`, `the Althanstraße`) — aber
  **20** vor Linienkennungen (`the line 1`, `the lines 2`, …). Die größere
  Familie stand vier Audits lang unbemerkt daneben.
* **Der Aufwand.** Die Sorge, es brauche eine handgeschriebene Straßen-Suffix-
  Liste, hat sich nicht bestätigt: `_STREET_SUFFIX_RE` existiert bereits im
  Masker, der diese Namen ohnehin schützt, und ließ sich wiederverwenden.
  Genau diese Wiederverwendung hält die Regel von `the Ernst-Happel-Stadion`
  und `the Wiener Linien` fern, wo der Artikel vertretbares Englisch ist.

#### B.3 (DNS-Rebinding-Schutz bei gesetztem `*_PROXY`) — **live reproduziert**

Der Befund vom 2026-09-13/17 stand als „unverändert offen: nur `log.warning`,
kein Fail-Closed". In dieser Arbeitsumgebung ist `HTTPS_PROXY` gesetzt, und der
Effekt ist unmittelbar sichtbar: `tests/test_dns_rebinding_bypass.py` und
`tests/places/test_client_security.py::test_client_ssrf_protection` schlagen
fehl, weil `src/utils/http.py` die IP-Verifikation hinter einem Proxy nicht
durchführen kann. Ohne die Proxy-Variablen läuft die Suite vollständig grün
(**9598 passed, 2 skipped**).

Das ist kein neuer Befund, sondern der erste **praktische** Nachweis für B.3:
Der Schutz schaltet sich nicht nur theoretisch ab, sondern tut es in jeder
Umgebung mit Proxy — still, und nur die Tests machen es sichtbar. Die
Produktionsumgebung (GitHub Actions) hat keinen Proxy, daher ist der
veröffentlichte Feed nicht betroffen.

---

### 4. Erkenntnisse zur Arbeitsweise

**Ein Audit-Befund ist eine Stichprobe, keine Messung.** Punkt 1 vom
2026-09-18 nannte 4 Vorkommen. Die tatsächliche Familie war fünfmal so groß,
und die größere Hälfte betraf eine andere Wortklasse. Wer einen Befund
umsetzt, sollte ihn zuerst über den gesamten Feed-Verlauf nachzählen, statt
die Zahl aus dem Audit zu übernehmen — sonst repariert man die Hälfte und hält
den Punkt für erledigt.

**Unit-Tests beweisen nicht, dass eine Regel läuft.** Bei #1842 bestanden alle
Tests der neuen Regel weiter, nachdem der Aufruf aus `_translate_text_attempt`
entfernt worden war — sie riefen den Helfer direkt auf. Seither hat jede neue
Textregel einen Integrationstest durch `_translate_text_attempt` mit gemockter
Pipeline. Dabei zu beachten: das Mock **muss** den `XENT…`-Platzhalter
zurückgeben, sonst verwirft der Entity-Guard die Übersetzung und der Test
schlägt aus dem falschen Grund fehl.

**Ein als Erfolg gecachter Fehler verschwindet nicht von selbst.** Der
Sticky-German-Guard verdrängt einen Cache-Eintrag nur, wenn die Übersetzung
gleich dem deutschen Quelltext ist. `the Karlsplatz` ist falsch, ohne deutsch
zu sein — ohne Epochen-Bump bliebe der Fix für alle bereits übersetzten Items
unsichtbar. Regel dokumentiert in `docs/architecture.md` §8; Historie am
Konstanten-Block in `src/build_feed.py`.

**Der deutsche Feed fiel beim Messen des englischen auf.** G.1 und G.2 wurden
nicht gesucht — sie tauchten auf, als der EN-Feed über den Verlauf ausgezählt
wurde und dieselbe Auswertung zum Vergleich auf `docs/feed.xml` lief. Die
letzten vier Audit-Runden haben überwiegend den EN-Feed vermessen, obwohl der
deutsche Rang 1 hat.

---

### 5. Offene Punkte — Register

| ID | Säule | Schwere | Kurzfassung | Rang | Stand |
| --- | --- | --- | --- | --- | --- |
| **G.1** | Feed (DE) | MITTEL | identischer Satz je Linie doppelt | 2 | **neu**, 13/250 gemessen |
| **G.2** | Feed (DE) | MITTEL | Beschreibung faktorisiert den gemeinsamen Anfang nicht | 2 | **neu**, 6/250 gemessen |
| — | Feed (DE) | offen | WL-Titel: `Linien 25 und 26` bleibt im Titel stehen | 2 | fremder Befund, `audit-2026-09-19.md` |
| — | Feed (DE) | offen | WL-Titel: `4A/80A/N29: 4A. 80A, N29:` doppelt | 2 | fremder Befund, `audit-2026-09-19.md` |
| **Punkt 6** (09-18) | Statistik | NIEDRIG | `≥` in Beschriftungen, Logik prüft `>`; `min`-Abkürzung uneinheitlich | 3 | nicht nachgeprüft |
| **D.1** | Stationen | HOCH | Namens-Kollisionen, `station_info()` verwirft still | — | auf einen Fall reduziert |
| **B.1** | Provider | KRITISCH | Stammstrecke Nordrichtung stumm | — | **betrieblich, nicht Code** — upstream verursacht, Erkennung ist der Fix, roter Health-Check ist korrektes Verhalten |
| **B.3** | Provider | MITTEL | DNS-Rebinding-Schutz bei `*_PROXY` | — | unverändert offen, hier erstmals praktisch reproduziert (§3) |
| **B.4** | Provider | MITTEL | `prune_cache` nur nach erfolgreichem Write | — | unverändert |
| **C.2** | Feed | MITTEL | quadratische Regex-Laufzeit, 45 ms/Aufruf im Worst Case | — | unverändert |
| **C.5** | Feed/i18n | NIEDRIG | `<Grund> <Ticker-Fragment>`-Titel ergeben gebrochenes Englisch | 2 | unverändert |
| **D.2** | Stationen | MITTEL | Validator kennt keine Namens-Kollisionen | — | unverändert |
| **D.3** | Stationen | MITTEL | 86 % der Aliase redundant | — | unverändert |
| **F.2–F.5** | Doku | MITTEL–NIEDRIG | Docstrings verweisen ins Leere; Docstring-Abdeckung; C901-Baseline | 4 | unverändert |
| **A.2 / A.3** | Quota | ungeprüft | Cross-Prozess-Race, Poison-Sentinel im Lock-Fehlerpfad | — | brauchen einen Nebenläufigkeits-Aufbau |

**Geschlossen und nicht wieder aufzumachen:** Punkt 5 (09-18, §3 dieser Datei),
Punkt 1 (09-18, §3), Punkt 2 und 3 (09-18, PRs #1832/#1833), A.1, B.2, C.3,
E.1–E.3, F.1 (alle 2026-09-17 nachgeprüft).

**Empfohlene Reihenfolge:** G.1 → G.2 → die beiden fremden WL-Titel-Befunde →
Punkt 6. Alles vier davor betrifft den deutschen Feed; Punkt 6 betrifft nur die
Statistik-Darstellung.

---

### 6. Offene Frage an den Betreiber

`docs/architecture.md` §8 beschreibt seit #1839 den zweisprachigen Feed samt
Kaskaden-Reihenfolge und Epochen-Regel. `AGENTS.md` verweist bisher nicht
darauf. Ein Querverweis wäre nützlich — `AGENTS.md` steuert aber das Verhalten
von Agenten, weshalb hier bewusst nichts geändert wurde. **Entscheidung
ausstehend.**

---

### 7. Methodik

Alle Zahlen sind an `d418e93` neu gemessen, keine aus Vor-Audits übernommen.

* **Grundgesamtheit deutsch:** alle `<item>` aus jeder Revision von
  `docs/feed.xml` in der Historie von `origin/main` — **329 Revisionen**,
  daraus **250 eindeutige (Titel, Beschreibung)-Paare**.
* **Grundgesamtheit englisch:** dieselben Revisionen von `docs/feed.en.xml`,
  daraus **456 eindeutige Texte** (Titel *und* Beschreibungen einzeln gezählt).
* **Dopplungs-Erkennung:** eine Beschreibung gilt als auffällig, wenn nach
  Entfernen des `[…]`-Zeitfensters eine **Drei-Wort-Phrase** wortgleich ein
  zweites Mal vorkommt. Die Drei-Wort-Schwelle wurde gewählt, weil zwei Wörter
  zu viele echte Wiederholungen mitnehmen (`in Richtung` in einer Aufzählung,
  `Bahnhof (U)` bei zwei Stationsnamen, `Hütteldorfer Straße` in
  „zwischen … und … / ab …"). Bei zwei Wörtern liefert dieselbe Auswertung
  37 statt 19 Treffer.
* **19 ist eine Untergrenze, keine Gesamtzahl.** Die Drei-Wort-Schwelle
  übersieht kürzere echte Dopplungen — darunter
  `Linien 25 und 26 Betrieb ab Josef-Baumann-Gasse Ersatzbus ab
  Josef-Baumann-Gasse` (dieselbe Störung an vier Tagen) und
  `Umleitung ab Hintere Zollamtsstr. über O und 18 Veranstaltung Betrieb ab
  Hintere Zollamtsstraße`. Mindestens fünf der 18 zusätzlichen Zwei-Wort-Treffer
  sind ebenfalls echte Befunde; die tatsächliche Zahl liegt also bei **24 oder
  mehr von 250**. Wer G.2 umsetzt, sollte mit der Zwei-Wort-Schwelle und
  manueller Durchsicht nachzählen, statt die 19 zu übernehmen — genau der
  Fehler, den §4 beschreibt.
* **Artikel-Muster englisch:**
  `(?<!\w)[Tt]he\s+\w+(?:straße|strasse|gasse|platz|weg|ring|allee|brücke|kai|markt)\b`
  und `(?<!\w)[Tt]he\s+[Ll]ines?\s+[A-Z0-9]`.
* **Zählbasis-Hinweis:** die PR-Texte zu #1842 und #1843 zählen *Items*, diese
  Datei zählt *Texte* (Titel und Beschreibung getrennt). Die Zahlen sind daher
  nicht eins zu eins vergleichbar; maßgeblich sind die hier angegebenen Muster
  und Grundgesamtheiten.
* **Nicht gemessen:** ob G.1 und G.2 in der WL-Quelle oder erst im Zusammenbau
  entstehen. Die Vorschläge in §2 gehen vom Zusammenbau aus, weil der Titel
  dieselbe Faktorisierung bereits leistet — das ist eine begründete Annahme,
  keine Messung.

---

## Audit Update 16:35 (Europe/Vienna) — Korrektur zu G.1 beim Umsetzen

G.1 hielt der Prüfung im Mechanismus **nicht** stand. Der Befund bleibt oben
unverändert stehen — ein datiertes Audit ist ein Protokoll, keine Wahrheit im
Nachhinein —, aber wer danach handelt, soll die richtige Fassung lesen.

### Was G.1 behauptete

> Die WL-Quelle liefert bei einer Störung, die mehrere Linien betrifft, je
> Linie einen eigenen Text. Sind diese Texte identisch, landen trotzdem zwei
> Kopien in der Beschreibung — die zweite nur um ein `Linie X:` erweitert.

Das las die Symptome von hinten. Nachgemessen am Rohcache (`3884dfd`):

```
Linie 5B:  Unregelmäßige Intervalle in beiden Richtungen.
Linie 49A: Unregelmäßige Intervalle in beiden Richtungen.
Linie 50A: Unregelmäßige Intervalle in beiden Richtungen.
Grund: Verkehrsstörung.
```

**Alle** Segmente tragen das Präfix, nicht nur die zweiten. Die Asymmetrie
entsteht erst bei uns: `_strip_wl_description_line_prefix` entfernt bewusst nur
das **erste** — der Titel trägt die Linienzuordnung bereits — und lässt die
übrigen stehen.

### Was der Befund übersah

Der eigentliche Schaden ist nicht die Wiederholung, sondern was sie verdrängt.
`Grund: Verkehrsstörung.` steht am Ende und fällt der 180-Zeichen-Grenze zum
Opfer. **Bei 7 der 13 Fälle** war der Grund damit unsichtbar — die Kürzung traf
genau den einzigen Satz, der etwas Neues sagte. Das stand in G.1 nicht.

### Was beim Nachmessen zusätzlich auffiel

WL liefert die Ortsangabe des Grundes gelegentlich leer
(`Grund: Verkehrsüberlastung im Bereich .`) oder mit einem Leerzeichen vor dem
Punkt (`… Atzgersdorfer Straße .`) — **9 von 417** Cache-Beschreibungen. Beides
lag bisher hinter der Kürzung. Wer die Wiederholungen entfernt, holt es aufs
Display und muss es mitbehandeln; im selben PR erledigt.

### Methodik-Notiz

Die Messung in §2 lief über die veröffentlichten Feeds. Die Ursache war erst am
**Rohcache** zu sehen (`cache/wl_9d709a/events.json`, 267 Revisionen). Ein
Befund über eine Ausgabe benennt das Symptom; die Ursache steht eine Stufe
davor. Derselbe Fehlertyp, den §3 bei Punkt 5 und D.2 in die andere Richtung
beschreibt — dort beschrieb ein Befund den Cache und meinte die Ausgabe.

**G.2 ist von dieser Korrektur nicht berührt** und bleibt wie oben beschrieben
offen.

---

## Audit Update 17:05 (Europe/Vienna) — G.2 bestätigt, ein neuer Befund

### G.2 hielt der Prüfung stand

Anders als G.1 (siehe 16:35) war die Diagnose richtig: Der Titel läuft über
`_join_merged_names` → `_collapse_common_prefix` und faktorisiert den
gemeinsamen Anfang, die Beschreibung verkettete in `src/feed/merge.py`
wortwörtlich mit `f"{clean1}\n\n{clean2}"`. Behoben, indem die Beschreibung
durch dieselbe Funktion läuft.

**Der Vorschlag in §2 war in der Reihenfolge falsch.** Er empfahl, die
Überlappungs-Trimmung zuerst zu bauen, „weil sie auch den Titel heilt und die
kleinere Änderung ist". Nachgemessen deckt die **Präfix-Faktorisierung 4 der
6 Fälle** ab, die Überlappungs-Trimmung nur einen. Und sie ist nicht die
kleinere Änderung, sondern die einzige, die eine **neue** Funktion braucht —
die Faktorisierung war schon da und musste nur angeschlossen werden.

Offen bleiben damit zwei Fälle mit je eigenem Mechanismus:

| Form | Beispiel | Vorkommen |
| --- | --- | --- |
| Suffix von A = Präfix von B | `Fahrtbehinderung PKW im Gleis` + `PKW im Gleis Betrieb ab Raxstraße` | 1 (Titel **und** Beschreibung) |
| gemeinsames Suffix beider | `Linien 25 und 26 Betrieb ab Josef-Baumann-Gasse` + `Ersatzbus ab Josef-Baumann-Gasse` | 1 Störung, 4 Tagesvarianten |

### Neuer Befund G.3 — eine Straße wird zur Linie — **NIEDRIG**

**Betroffene Komponente**

Übersetzungspfad `_translate_text_attempt` in `src/build_feed.py` (EN-Feed).

**Problembeschreibung**

Enthält ein Titel sowohl eine Linienkennung als auch einen Straßennamen,
trägt das Modell `line` auf die zweite Ortsangabe weiter:

```
DE:  2: Züge halten bei Linie 46, in der Mühlfeldgasse
EN:  2: Trains stop at line 46, in line Mühlfeldgasse
```

`bei Linie 46` → `at line 46` ist korrekt; `in der Mühlfeldgasse` → `in line
Mühlfeldgasse` macht aus einer Straße eine Linie. Kein Glossar-Fehler, sondern
eine Analogiebildung des Modells innerhalb desselben Satzes.

**Gemessen: 2 von 463** eindeutigen EN-Texten (ein Item, Titel und
Beschreibung). Das Auslöser-Item ist inzwischen upstream verschwunden; der
Befund ist über die Feed-Historie belegt, nicht am aktuellen Stand
reproduzierbar.

**Konkreter Lösungsvorschlag**

Dieselbe Technik wie #1843: `line ` streichen, wenn unmittelbar ein Name
folgt, der `_STREET_SUFFIX_RE` erfüllt — eine Straße ist nie eine Linie. Die
Regel ist scharf, weil sie denselben wiederverwendeten Straßentest benutzt.

Rang 2 (EN-Feed), aber mit 2 von 463 **nachrangig** gegenüber allem, was den
deutschen Feed betrifft.

### #1843 ist live bestätigt

Der `chore: update cycle` um 14:30 lief mit Epoche 14. Im aktuellen Fenster
steht genau ein Item, das den Test zulässt — es ist zugleich die einzige
artikulierte Nennung im ganzen deutschen Feed:

```
DE:  2: Züge halten in der Mühlfeldgasse
EN:  2: Trains stop in Mühlfeldgasse
```

Deutsch mit Artikel, Englisch ohne. **#1842 bleibt unbestätigt**: es gibt
derzeit kein Item mit artikulierter Linienkennung. `at line 46` und
`at line O` stammen aus `bei Linie 46` / `bei Linie O` — dort ist auch das
Deutsche artikellos, das beweist nichts. Ebenso wenig die acht übrigen bloßen
Straßennamen im EN-Feed (`from Börsegasse`, `towards Schottenring` …): deren
deutsche Quellen sind bereits artikellos.

### Register-Fortschreibung

| ID | Stand |
| --- | --- |
| **G.1** | **geschlossen** (#1844), Korrektur siehe 16:35 |
| **G.2** | **zu 4 von 6 geschlossen**; zwei Rest-Mechanismen oben tabelliert |
| **G.3** | **neu**, 2 von 463, nachrangig |
| **Punkt 1** (09-18) | live bestätigt für #1843; #1842 mangels Testfall offen |

**Empfohlene Reihenfolge weiterhin:** die beiden G.2-Restmechanismen → die
zwei fremden WL-Titel-Befunde aus `audit-2026-09-19.md` → G.3 → Punkt 6.

---

## Audit Update 17:40 (Europe/Vienna) — Projektstand geprüft: ein Befund ersten Ranges

Auftrag: aktuellen Stand prüfen, die letzten Auditfiles auf Aktualität
lesen, klären, was behoben ist, und Lösungen vorschlagen. Geprüft wurden
`origin/main` auf `daacd72` (Stand nach #1844), der veröffentlichte Feed von
17:00, der WL-Rohcache (`cache/wl_9d709a/events.json`, 84 Ereignisse: 37
Langmeldungen, 47 Kurzmeldungen) und ein lokaler Feed-Bau mit auf 100
gehobenem Deckel, alle Ausgaben nach `data/tmp_audit/` umgeleitet und danach
entfernt. Nichts Verfolgtes wurde dabei angefasst.

### Stand der Behebungen

Seit dem Abend des 18.09. sind zehn PRs gemergt (#1835–#1844). Davon
betreffen sechs den deutschen Feed (#1835, #1836, #1837, #1838, #1844 sowie
der Dedupe-Fix in #1836) und drei den englischen (#1842, #1843 plus die
Glossar-Runde #1832/#1833 vom Vortag). G.2 liegt mit diesem PR vor —
vier der sechs gemessenen Fälle, plus eine Schranke, die erst die volle
Suite erzwungen hat (unten, „Lehre").

**Die Auditfiles sind aktuell** — mit drei Ausnahmen, die dieser Nachtrag
schließt: Das Register kannte den heute größten Befund nicht (G.4), zwei
kleinere fehlten (G.5, G.6), und Punkt 6 vom 18.09. stand als „nicht
nachgeprüft", obwohl seine erste Hälfte längst behoben ist.

### G.4 — Ein Ereignis, alle zehn Plätze — **HOCH** (Rang 1)

| | |
| --- | --- |
| **Betroffene Komponente** | Zusammenspiel dreier je für sich richtiger Regeln: `MAX_ITEMS = 10` (`src/config/defaults.py`), `_recency_sort_key` in `src/build_feed.py` (neueste zuerst nach `first_seen`) und die Faltung Kurz→Lang `_ticker_fold_target` in `src/providers/wl_fetch.py`. |
| **Problembeschreibung** | Der Feed von 17:00 besteht zu zehn von zehn Items aus Wiener-Linien-Kurzmeldungen zu **einer** Demonstration (Schwarzenbergplatz/Ring): acht tragen das Wort im Titel, zwei (`2: Züge halten in der Mühlfeldgasse`, `1: Züge halten bei Linie O`) sind zeitgleiche Folgen. Mit gehobenem Deckel zeigt sich das ganze Bild — 113 Rohmeldungen, und die Plätze 1–11 gehören alle diesem Ereignis. Auf Platz **12** liegt die Langmeldung `1/2/2A/3A/4A/71/D: Demonstration am 19.09.2026`, die **alles** enthält, was die Kurzmeldungen sagen, und mehr: „Linie D: Derzeit kein Betrieb zwischen Börse und Quartier Belvedere. Linie 1: Umleitung in beiden Richtungen zwischen Kliebergasse und Hintere Zollamtsstraße … Linie 2A: Kein Betrieb möglich. Linie 3A: Betrieb nur zwischen Oper, Karlsplatz und Hoher Markt. Linie 4A: Betrieb nur zwischen Lisztstraße und Wittelsbachstraße." Sie erscheint nicht. Verdrängt sind unter anderem `72A: Kraftwerk Simmering` (13), `12A/14A: Pilgramgasse U` (14), die beiden Items der fremden Befunde (19, 20), der seit 14.09. laufende Ersatzverkehr `25/26/27: Gleisbauarbeiten` (75) und jede ÖBB-Meldung. Kein Duplikat, kein Dedupe-Fehler — jede der elf Meldungen ist für sich legitim. Aber auf einem Display mit zehn Plätzen ist ein Tag mit einem Großereignis ein Tag ohne alle anderen Störungen. **Warum die Faltung nicht greift:** Sie verlangt (a) **gleiche** Linienmenge — die Langmeldung trägt sieben Linien, jede Kurzmeldung eine; (b) gleiche Kategorie — die Langmeldung ist `Hinweis`, die Kurzmeldungen sind `Störung`; (c) eine Kurzbeschreibung, die nichts über den eigenen Titel hinaus sagt — hier steht aber WLs Textbaustein „Nach einer Fahrtbehinderung kommt es zu unterschiedlichen Intervallen." (7 von 47 Kurzmeldungen im Cache). Drei Gatter, drei Nein. |
| **Konkreter Lösungsvorschlag** | Drei Wege, am Cache gemessen. **A — Obermengen-Faltung (empfohlen, kleinste Änderung):** In `_ticker_fold_target` (a) `!=` → „Kurz-Linien ⊆ Lang-Linien", (b) Kategorie-Gatter für das Paar `Störung`→`Hinweis` öffnen, (c) den WL-Textbaustein als „sagt nichts Neues" werten. Alles andere bleibt: Titel-Wörter müssen im Langtext stehen, Zeiträume überlappen, Ziel darf keine Schlagzeile sein. Streng gemessen falten heute **9 von 47** Kurzmeldungen: vier Demonstrations-Items (1, 2A, 4A, D) in die Langmeldung, fünf Ersatzbus-Items (25, 25, 26, 27, 27) in `25/26/27: Gleisbauarbeiten`. Wirkung heute: Plätze 1–11 → 7 Kurzmeldungen, die Langmeldung rückt auf Platz 8 **und wird sichtbar**, zwei Plätze werden frei. Die lockere Variante „Ursachenwort statt Titeldeckung" faltet 18 von 47 — zu locker, sie zählt `Züge` als Ursache; nicht empfohlen. A **braucht G.6**, sonst zeigt die sichtbar gewordene Langmeldung nur ihren Ankündigungssatz. **B — Ereignis-Bündel:** Kurzmeldungen mit gleichem Ursachenwort am gleichen Tag zu einem Item zusammenziehen (`1/2/2A/3A/4A/31/66A/74A/D: Demonstration`), die Fragmente als Beschreibung; heute 9 → 1, greift auch für 31/66A/74A, die in keiner Langmeldung stehen — aber ein neuer Mechanismus mit eigenem Titelbau. **C — Platzbudget je Thema:** In den Top-N höchstens *k* Items pro Ursachenwort und Tag, der Rest rutscht nach unten; informationsfrei, aber ein Sicherheitsnetz für Ereignisse ohne Langmeldung. **Empfehlung: A + G.6 zuerst, C als Netz; B nur, wenn der Betreiber Ereignis- statt Linien-Granularität auf dem Display will.** Das ist eine Produktentscheidung, die hier nicht vorweggenommen wird. |

### G.5 — Zusammengeführte Beschreibung liest einen Titelteil noch einmal vor — **MITTEL** (Rang 2)

| | |
| --- | --- |
| **Betroffene Komponente** | `deduplicate_fuzzy` in `src/feed/merge.py` (Beschreibungs-Zusammenführung); `_summary_duplicates_title` in `src/build_feed.py` läuft erst bei der Ausgabe und prüft nur die **ganze** Beschreibung gegen den Titel. |
| **Problembeschreibung** | Item 4 heute: Titel `2: Demonstration Züge halten Steig A & Züge halten bei Linie 46`, Beschreibung `Nach einer Fahrtbehinderung kommt es zu unterschiedlichen Intervallen. Züge halten bei Linie 46`. Der zweite Absatz ist die Schlagzeilen-Beschreibung der zweiten Meldung — allein hätte `_summary_duplicates_title` sie geleert, zusammengeführt überlebt sie und wiederholt, was der Titel drei Zentimeter darüber sagt. 1 von 10 heute. |
| **Konkreter Lösungsvorschlag** | Beim Zusammenführen, vor dem Stapeln: eine Beschreibung, deren normalisierter Text im zusammengeführten Titel enthalten ist, entfällt. Ein Ort, vor dem Schnitt, mit Test durch `deduplicate_fuzzy` an genau diesem Fall. Passt in den Rest-PR zu G.2. |

### G.6 — Langmeldung: der Zwei-Satz-Auszug behält die Ankündigung und verwirft die Maßnahmen — **MITTEL** (Rang 2, wird durch G.4-A sichtbar)

| | |
| --- | --- |
| **Betroffene Komponente** | Auszugsregel in `_emit_item`-Umfeld, `src/build_feed.py` (~Zeile 5360): erster Satz, zweiter nur, wenn beide zusammen ≤ 180 Zeichen; danach `_truncate_summary_180`. |
| **Problembeschreibung** | Für die Langmeldung der Demonstration liefert die Regel: „Wegen einer Demonstration im Bereich Schwarzenbergplatz und Ring kommt es zu folgenden Verkehrsmaßnahmen." — und dann nichts. Satz eins (107 Zeichen) plus „Zeitraum: Samstag, 19. September 2026, ab ca. 12:00 Uhr bis ca. 21:30 Uhr." überschreiten 180, also bleibt nur die Ankündigung; die Maßnahmen je Linie kommen nie an die Reihe. Heute unsichtbar (Platz 12), mit G.4-A auf dem Display. Im Cache tragen **20 von 37** Langmeldungen einen Block je Linie (`Linie X: …` oder `Maßnahmen:`), **3** eröffnen mit „folgenden …". Alle 37 sind länger als 180 Zeichen. |
| **Konkreter Lösungsvorschlag** | Wenn der erste Satz eine Liste ankündigt („folgenden Verkehrsmaßnahmen/Änderungen/Maßnahmen") oder der Text einen `Maßnahmen:`-Block trägt, beginnt der Auszug beim ersten `Linie X:`-Satz; die Ankündigung sagt der Titel schon, den Zeitraum die Datumszeile des Items. Ergebnis: „Linie D: Derzeit kein Betrieb zwischen Börse und Quartier Belvedere. Linie 1: Umleitung in beiden Richtungen zwischen Kliebergasse und Hintere Zollamtsstraße …" — 180 Zeichen, die etwas sagen. Vorher an den 20 Fällen gegenprüfen, dass kein `Linie X:`-Satz aus dem Zusammenhang gerissen wird. |

### G.7 — Drei ÖBB-Items mit identischem Titel — **NIEDRIG** (Rang 2)

`Wien Hauptbahnhof ↔ Gramatneusiedl` steht dreimal im gehobenen Feed (Plätze
48, 67, 82) — drei Bauphasen (3.–5.10., 31.10.–30.11., 5.–7.12.), drei
verschiedene Beschreibungen, ein Titel. Auf dem Display stünde derselbe
Titel dreimal, und keiner steht für sich. Heute weit unter dem Deckel, kein
Handlungsbedarf; wenn ein Routen-Titel mehrfach auftaucht, gehört der
Zeitraum in den Titel. Festgehalten, damit es nicht als Duplikat
fehlgedeutet wird — es ist keines.

### Fremde Befunde aus `audit-2026-09-19.md` — reproduziert, unverändert offen

Beide heute im Cache und durch den echten Parser geführt:
`_extract_prefix_lines` erkennt `Linien 25 und 26` nicht als Linienblock,
`_strip_redundant_line_token` kennt nur die `/`-Form und Einzelcodes, nicht
`4A. 80A, N29:`. Ergebnis unverändert `25: Linien 25 und 26 Betrieb ab
Josef-Baumann-Gasse & Ersatzbus ab Josef-Baumann-Gasse` (Platz 20) und
`4A/80A/N29: 4A. 80A, N29: Wittelsbachstraße` (Platz 19). Beide Rang 2, beide
heute durch G.4 unsichtbar. Der erste Fall wird mit G.4-A zugleich gefaltet
(in `25/26/27: Gleisbauarbeiten`); der Parser-Fix bleibt trotzdem nötig für
Tage ohne Langmeldung.

### C.5 heute

8 von 10 englischen Titeln haben die Form `<Ursache> <Fragment>`
(`31: Demonstration service from Wallensteinstraße`). Der Vorschlag vom
17.09. — ein Trennzeichen an der Fuge, in beiden Sprachen — steht; zu messen
bleibt, wie viele Titel dadurch `TITLE_CHAR_LIMIT` reißen.

### Punkt 6 (09-18) — erste Hälfte **geschlossen**, zweite ohne Handlungsbedarf

`docs/assets/site.js` baut das Kachel-Label aus `DELAY_THRESHOLD_MIN` und
filtert strikt `>`, wie Backend und Markdown-Dashboard („Verspätungen > 9
min"); `tests/test_dashboard_delay_threshold.py` pinnt beide aneinander. Der
Kommentar an Ort und Stelle beschreibt genau den Befund als behoben. Die
zweite Hälfte — „Minuten" in Überschriften, „min" in Zellen — ist eine
Einheiten-Konvention, kein Widerspruch. Zu.

### B.1 — Stand

`Praterstern` zuletzt gesehen am 14.08. 13:57, seit **36 Tagen** stumm, 0
Zeilen im 30-Tage-Fenster bei 1.461 Zeilen der Gegenrichtung.
`DIRECTIONS` in `scripts/update_stammstrecke_status.py` führt beide
Richtungen, jeder Tick fragt beide (`for direction in DIRECTIONS`). Die
Anforderung hält; der Befund bleibt betrieblich.

### Lehre aus der Suite

Der G.2-Stand fiel in der vollen Suite genau einmal durch:
`test_fuzzy_merge_silvester` bekam `Details about Lauf., Pfad.` — das Komma
hinter dem Punkt. Die gezielten Tests hatten das nicht gefunden, weil alle
gemessenen Fälle satzzeichenfrei sind. Wer eine Titelregel auf ein anderes
Feld überträgt, muss die Formen **dieses** Feldes testen: Satz, mehrere
Sätze, Abkürzung. Die Schranke (`_collapse_description_prefix`: Satzform
bleibt gestapelt) ist jetzt Teil des PR.

### Register-Fortschreibung

| ID | Säule | Schwere | Rang | Stand |
| --- | --- | --- | --- | --- |
| **G.4** | Feed (DE) | HOCH | 1 | **neu** — 11 von 11 Top-Plätzen ein Ereignis, Langmeldung auf 12; Lösung A gemessen (9/47), Betreiber-Entscheid nötig |
| **G.5** | Feed (DE) | MITTEL | 2 | **neu** — 1/10 heute |
| **G.6** | Feed (DE) | MITTEL | 2 | **neu** — 20/37 Langmeldungen betroffen, sichtbar erst mit G.4-A |
| **G.7** | Feed (DE) | NIEDRIG | 2 | **neu** — 3 ÖBB-Items, ein Titel; kein Handlungsbedarf |
| **G.2** | Feed (DE) | MITTEL | 2 | 4 von 6 in diesem PR, plus Satzform-Schranke |
| fremd (2) | Feed (DE) | offen | 2 | reproduziert, Plätze 19 und 20 |
| **C.5** | Feed (EN) | NIEDRIG | 3 | 8/10 heute |
| **Punkt 6** (09-18) | Statistik | — | 3 | **geschlossen** |
| **B.1** | Provider | KRITISCH (betrieblich) | — | 36 Tage, beide Richtungen werden abgefragt |

**Empfohlene Reihenfolge:** G.4 (nach Entscheid des Betreibers) zusammen mit
G.6 → G.5 → die beiden fremden Befunde → G.2-Rest → C.5 → G.3 → G.7.

---

## Audit Update 17:41 (Europe/Vienna) — zwei fremde Befunde aus #1845 nachgeprüft

Während dieser Prüfung wurde #1845 gemergt und hängte zwei Befunde
(Zeitstempel 14:40) an `audit-2026-09-19.md`. Beide durch den Code geführt.

### „`_AB_DATE_RE` strandet zweistellige Jahre" — **widerlegt**

Behauptet: `Bauarbeiten ab 14.09.26` werde zu `Bauarbeiten26`, weil das
Muster nur `(?:\d{4})?` kenne. Tatsächlich lautet es in
`src/providers/wl_text.py` bereits `(?:\d{4}|\d{2})?(?!\d)`, und
`_tidy_title_wl("Bauarbeiten ab 14.09.26")` liefert `Bauarbeiten` — genau
wie mit vierstelligem Jahr und ohne Jahr. Im heutigen Cache trägt kein Titel
ein zweistelliges Jahr nach `ab`; in den letzten 200 Revisionen von
`docs/feed.xml` gibt es weder einen solchen Titel noch eine Verklebung der
Form `Wort26`. Der Befund beschreibt einen Code, den es nicht gibt. Kein
Handlungsbedarf, nichts zu ändern.

### „Minuten" in Überschriften, „min" in Zellen — **Geschmacksfrage, nicht Fehler**

Derselbe Punkt wie die zweite Hälfte von Punkt 6 (09-18), oben um 17:40
geschlossen: Die Überschrift nennt die Einheit ausgeschrieben, die Zelle
abgekürzt — das ist die übliche Tabellenkonvention. Der fremde Vorschlag
(Überschriften auf `min` umstellen) macht die Tabelle nicht richtiger,
nur einheitlicher im Wortlaut. Rang 3, Entscheidung beim Betreiber; hier
keine Empfehlung dafür.

### Register

| ID | Stand |
| --- | --- |
| fremd (#1845, `_AB_DATE_RE`) | **widerlegt**, geschlossen |
| fremd (#1845, „Minuten"/„min") | = Punkt 6 (2. Hälfte), Geschmacksfrage, kein Handlungsbedarf |

---

## Audit Update 17:50 (Europe/Vienna) — Vorschlag B zu G.4 gemessen: die Skepsis ist berechtigt

Anlass: Der Betreiber findet B (Kurzmeldungen mit gleichem Ursachenwort am
gleichen Tag zu einem Ereignis-Item bündeln) interessant, zweifelt aber, ob
es gut funktioniert. Gemessen am heutigen Cache und über die 269 Revisionen
von `cache/wl_9d709a/events.json` vom 12. bis 19.09. Ursachenwörter: die
kuratierte Menge `_CATEGORY_PREFIX_WORDS` (15 Einträge, `demonstration`
enthalten), am Titelanfang oder als `wegen <Ursache>` am Ende.

### Was B heute ausgäbe

Zehn Kurzmeldungen zur Demonstration, inklusive `71: Fahrtbehinderung wegen
Demonstration`. Titel: `1/2/D/2A/31/3A/4A/71/66A/74A: Demonstration`
(43 Zeichen). Beschreibung aus den Fragmenten: 314 Zeichen. Nach dem
180-Zeichen-Schnitt überleben **5 von 10** Linien:

```
1: Betrieb ab Hintere Zollamtsstraße · 2: Züge halten Steig A · D: Betrieb ab
Quartier Belvedere S · 2A: Kein Betrieb · 31: Betrieb ab Wallensteinstraße ·
3A: Busse halten in …
```

4A, 71, 66A und 74A stünden im Titel, ohne dass die Beschreibung ein Wort
zu ihnen sagt. Die Langmeldung derselben Demonstration nennt für jede Linie
die Maßnahme.

### Wie oft B falsch bündeln würde

| Messung | Wert |
| --- | --- |
| Revisionen | 269 |
| Revisionen mit einer Gruppe ≥ 5 Kurzmeldungen (Ursache, Tag) | 19 |
| verschiedene Gruppen ≥ 2 Kurzmeldungen in der Woche | 8 |
| davon nachweislich **mehrere** Ereignisse in einer Gruppe | 3 |

Die drei: **Falschparker 19.09.** — Linie 37 um 11:40, 60 um 10:20, 66A um
12:40, 40/41 um 14:28 mit `42/9: Umleitung bis Gersthof`, 42 Antonigasse
um 14:00 — mindestens fünf Vorfälle, B machte daraus
`37/40/41/42/60/66A/9: Falschparker`. **Falschparker 16.09.** — 12A um
05:43, 5 um 13:35, 89A um 14:28, 9 um 14:55, 42 um 19:08 — fünf, über den
Tag verteilt. **Veranstaltung 18.09.** — `12/2` am Schwarzenbergplatz gegen
`40/41/9/42` bei Gersthof — zwei. Sechs Linien, ein Titel, kein gemeinsamer
Vorfall: Das wäre auf dem Display eine falsche Aussage, und zwar an der
Stelle, an der der Titel für sich allein stehen muss.

Richtig bündelt B nur bei den stadtweiten Ereignissen (Demonstration 19.09.,
Veranstaltung 17.09. mit neun Linien). Für die Demonstration gibt es die
Langmeldung, dort leistet A dasselbe mit der vollständigeren Beschreibung.
Für den 17.09. fand die strenge Regel keinen Anker; ob WL dort eine
Langmeldung hatte, ist nicht geprüft.

Nebenbefund, ohne Handlungsbedarf: WL veröffentlicht zu einem Vorfall oft
zwei Kurzmeldungen (`40: Fahrtbehinderung Falschparker` und
`40: Falschparker`, gleicher Beginn, gleicher Text). `deduplicate_fuzzy`
führt sie zusammen — geprüft, ein Item.

### Bewertung

B in der Form „Ursachenwort + Tag" ist **verworfen**: Ein generisches
Ursachenwort (`Falschparker`, `Bauarbeiten`, `Veranstaltung`) benennt eine
Art von Vorfall, nicht einen Vorfall. Ein Bündel braucht einen
Ereignis-Anker, und den liefert nur die Langmeldung — das ist A. Für
Ereignisse ohne Langmeldung bleibt C das ehrliche Netz: Es bündelt nichts
Falsches, es begrenzt nur die Plätze.

**Empfehlung angepasst:** A + G.6, danach C. B gestrichen.

### Register

| ID | Stand |
| --- | --- |
| **G.4** | Option B gemessen und **verworfen** (3 von 8 Gruppen wären falsche Bündel; 5 von 10 Linien überlebten den Schnitt). Empfehlung: A + G.6, dann C. |

---

## Audit Update 18:09 (Europe/Vienna) — Entscheid zu G.4, G.5 umgesetzt

### Entscheid des Betreibers zu G.4

Der Betreiber hat die angepasste Empfehlung angenommen: **A** (Faltung
Kurz→Lang auf Linien-Obermenge, Kategorie-Gatter für `Störung`→`Hinweis`,
WL-Textbaustein als leer gewertet) **zusammen mit G.6** (Auszug beginnt
beim ersten `Linie X:`-Satz, wenn der Text einen Maßnahmen-Block trägt),
**danach C** (Platzbudget je Ursache und Tag in den Top-N). B ist
gestrichen. Umsetzung als nächster PR nach G.5.

### G.5 — geschlossen, Befund präzisiert

Beim Nachmessen über 300 Revisionen von `docs/feed.xml` (242 verschiedene
Titel/Beschreibungs-Paare, 10 mit `&`-Titel) zeigte sich: 8 der 10
`&`-Items wiederholten einen Titelteil in der Beschreibung — aber 6 davon
sind der Fall „ganze Beschreibung = Titel", den #1836 schon schließt (alle
Vorkommen liegen vor dem 18.09. 21:25 UTC; heute trägt dasselbe
25/26-Item nur noch die Datumszeile). Übrig bleibt der Ein-Absatz-Fall:
ein Absatz mit Inhalt, einer wiederholt den Titelteil. Das ist Item 4 von
heute in zwei Varianten (`Betrieb ab Schwedenplatz Züge halten bei Linie
46` und `Nach einer Fahrtbehinderung … Züge halten bei Linie 46`).

Der Fix sitzt an der Merge-Stelle (`_restates_title` in
`src/feed/merge.py`): Eine Beschreibung, die wortgleich im
zusammengeführten Titel steht, entfällt vor dem Stapeln, auf beiden Seiten.
Nur ganze Wörter zählen. Sechs Testfälle durch `deduplicate_fuzzy`, einer
davon gegen die Reihenfolge der Meldungen, einer gegen das Fehlen des
Aufrufs. Ein Alt-Test (`test_gersthof_full_pipeline`, Mai 2026) pinnte das
Stapeln des Titel-Wiederholers als „Inhalt überlebt“; die Erwartung ist auf
den neuen Vertrag angepasst — der Inhalt überlebt im Titel.

### Register

| ID | Stand |
| --- | --- |
| **G.4** | **freigegeben**: A + G.6, dann C. Nächster PR. |
| **G.5** | **geschlossen** (PR folgt), Befund auf den Ein-Absatz-Fall präzisiert |

---

## Audit Update 18:40 (Europe/Vienna) — G.4-A und G.6 umgesetzt

Umsetzung des freigegebenen Wegs, lokal auf dem Branch; PR folgt, sobald
#1847 (G.5) gemergt ist.

### A — Faltung Kurz→Lang, drei Gatter gelockert

In `src/providers/wl_fetch.py`: (1) Linien der Kurzmeldung ⊆ Linien der
Langmeldung statt Gleichheit; (2) `_categories_compatible` — gleiche
Kategorie immer, dazu `Störung`→`Hinweis`, nicht umgekehrt; (3) WLs
Textbaustein und die eigenen Liniennummern zählen in `_is_headline_only`
als leer. Dazu ein Robustheitsfix, ohne den (1) auf echten Daten ins Leere
liefe: `_content_tokens` löst HTML-Tags und -Entities auf, denn der
Langtext liegt als HTML im Bucket (`Zollamtsstra&szlig;e` fand
`zollamtsstraße` nicht).

**Ein bestehender Vertrag wurde umgekehrt.** `tests/test_wl_display_ticker_fold.py`
hielt seit dem 17.09. fest, dass ein `Hinweis` eine `Störung`-Schlagzeile
nie aufnimmt — Begründung damals: falsche Rubrik, falsche Sortierung. Die
Kategorie erreicht das Display nicht (kein `<category>` im RSS) und bricht
nur Gleichstände der `first_seen`-Sortierung; der Preis der Trennung waren
zwei Plätze für eine Störung. Der Test pinnt jetzt den neuen Vertrag,
die Gegenrichtung (`Hinweis`-Schlagzeile → `Störung`-Langmeldung) bleibt
zu und ist getestet.

Belegt durch Tests mit den gekürzten Rohdaten des Tages
(`tests/test_wl_notice_absorbs_line_subset.py`): 1 und 2A falten in die
Demonstrations-Langmeldung, 31 (nicht in deren Linien), `2: … Steig A`
und `1: Züge halten bei Linie O` (Wörter fehlen im Langtext) bleiben. Was
der Offline-Bau **nicht** zeigen kann: Die Faltung läuft beim
Cache-Refresh (`fetch_events`), nicht beim Feed-Bau; der Cache trägt noch
die Ausgabe des alten Codes. Live greift sie ab dem ersten
`chore: update cycle` nach dem Merge. Erwartung nach der Messung von
17:40: Plätze 1–11 → 7 Kurzmeldungen, Langmeldung auf Platz 8, zwei
Plätze frei.

Nebenbefund ohne Handlungsbedarf: Hinweis-Meldungen müssen
`KW_RESTRICTION` passieren (ein Einschränkungswort im Text); eine reine
Schlagzeile ohne ein solches Wort kommt als `Hinweis` gar nicht erst an.

### G.6 — Maßnahmen zuerst

`_prefer_measures` in `src/build_feed.py`, aufgerufen vor dem Zwei-Satz-
Auszug. Der Offline-Bau mit gehobenem Deckel zeigt für Platz 12 jetzt:

```
Linie D: Derzeit kein Betrieb zwischen Börse und Quartier Belvedere; Linie 1:
Umleitung in beiden Richtungen zwischen Kliebergasse und Hintere
Zollamtsstraße über Landstraße …
```

statt „… kommt es zu folgenden Verkehrsmaßnahmen." Die Satzgrenze ist als
`_SENTENCE_SPLIT_RE` geteilt; die Maßnahmen-Muster referenzieren
`_WL_DESC_LINE_TOKEN` (AST-Test). Bewusst verloren geht die Uhrzeit aus dem
`Zeitraum:`-Satz („ab ca. 12:00 Uhr bis ca. 21:30 Uhr"); die Datumszeile
zeigt nur Tage. Falls das fehlt, ist es ein eigener Punkt, kein Grund,
die Maßnahmen wieder hinter die Ankündigung zu stellen.

### Register

| ID | Stand |
| --- | --- |
| **G.4** | A umgesetzt (lokal), C folgt als eigener PR |
| **G.6** | umgesetzt (lokal), Uhrzeit-Verlust dokumentiert |
