# Audit: Stationsverzeichnis — Funktion, Feed-Anbindung, Datenqualität

**Datum:** 2026-09-13
**Schwerpunkt:** `data/stations.json` — arbeitet das Verzeichnis korrekt, und wird es im Feed-Pfad tatsächlich wirksam?
**Datenbasis:** Repo-Stand `aeeb92c`, Pipeline-Lauf `2026-09-13T05:42:52Z` (`data/stations_last_run.json`),
Live-Caches in `cache/` (WL 49 Items, ÖBB 11, Baustellen 22), realer Feed-Build gegen diese Caches
**Status:** **Alle vier Befunde abgearbeitet.** Befund 4 nach Betreiber-Entscheidung vom 2026-09-13.
Beim Nachgehen der Google-Koordinaten-Frage kam **Befund 5** dazu (korrupte WL-OGD-Koordinaten) — siehe §9.

---

## 1. Executive Summary

**Das Verzeichnis funktioniert, und es wird verwendet.** Beides war die Ausgangsfrage, beides ist
belegt: Der Validator meldet bei 2.243 Stationen null Koordinaten-Anomalien, null GTFS-Abweichungen,
null Sicherheits-, Provider-, Namens- und ID-Kollisionen. Die wöchentliche Pipeline lief mit allen
vier Teilschritten auf Exit-Code 0, die Quarantäne ist leer. 804 Stations-Tests, 392 Provider-/
Skript-Tests und 647 Alias-/Feed-Tests sind grün.

Dass das Verzeichnis den Feed real steuert und kein totes Datenartefakt ist, wurde durch einen
instrumentierten Feed-Build nachgewiesen (Status `success`, 80 Items):

| Aufruf während eines Builds | Calls | Treffer |
| --- | --- | --- |
| `oebb.station_info` | 83 | 74 |
| `oebb.canonical_name` | 267 | 1 |
| `oebb.display_name` | 24 | 24 |
| `baustellen.nearest_rail_station` | 22 | 2 |
| `stations._station_entries` (Entity-Masker) | 2 | 2 |

Auflösungsquote im Produktivbetrieb: ÖBB 99,2 %, VOR 100 %, Baustellen 90,3 %.

**Die Statistik-Extraktion hatte zwei Fehler, die sichtbar auf das Dashboard durchschlugen.**
Beide sind behoben. Sie betrafen nicht das Verzeichnis selbst, sondern dessen Auswertung in
`src/utils/stats.py` — also genau die Stelle, an der das Verzeichnis in eine für Leser:innen
sichtbare Ausgabe übersetzt wird.

| # | Befund | Feed-sichtbar | Status |
| --- | --- | --- | --- |
| 1 | `\| Haltestelle:`-Marker fiel aus dem Suchfenster | **ja** | behoben |
| 2 | Generisches „Lokalbahn" kollabierte auf Guntramsdorf | **ja** | behoben |
| 3 | Sidecar persistierte nur 8 von 11 Validierungskategorien | nein | behoben |
| 4 | `is_pendler()` / `vor_station_ids()` ohne Produktiv-Aufrufer | nein | entschieden |

---

## 2. Befund 1 — Der `| Haltestelle:`-Marker fiel aus dem Suchfenster

**Feed-sichtbar. Höchste Priorität.**

### Symptom

`extract_location_name()` liefert die Ortsangabe für `data/stats/stoerungen_YYYY.csv` und damit
für die Tabelle „Häufigste Störungsorte" in README und Dashboard. Identische Meldung, identisches
Signal — nur die Länge der Beschreibung entschied über das Ergebnis:

```
kurze Beschreibung + " | Haltestelle: Dornbach"  ->  Wien Dornbach (WL)
lange  Beschreibung + " | Haltestelle: Dornbach"  ->  unbekannt
```

### Ursache

Zwei Dinge trafen aufeinander:

1. `src/providers/wl_fetch.py:953` hängt den Marker **ans Ende** der gebauten Beschreibung:
   `desc += f" | Haltestelle: {stops_formatted}"`.
2. `src/utils/stats.py` kappte den gesamten Suchraum auf 1024 Zeichen.

Der Kommentar über der Kappung begründete sie mit „providers cap descriptions at a few hundred
chars". Das trifft für den WL-Provider nicht zu: **69 % der Beschreibungen im Live-Cache liegen
über 1024 Zeichen**, das Maximum bei 4.972. Damit lag der Marker regelmäßig außerhalb des Fensters,
und das autoritativste Signal, das die WL-API liefert — die kuratierte `relatedStops`-Liste —
wurde stillschweigend verworfen. Die Meldung landete als `unbekannt` im Dashboard.

Im aktuellen Cache-Snapshot ist der Defekt nicht sichtbar, weil dort kein Item `relatedStops`
trägt. Er ist deterministisch reproduzierbar und schlägt zu, sobald der Upstream die Felder wieder
liefert.

### Fix

Das Suchfenster ist jetzt zweigeteilt, statt für alle Schritte gleich eng zu sein:

* **Verankerte Label-Regexe** (`| Haltestelle:`, `| Station:`, `| Location:`) sehen bis zu
  `MAX_LABELLED_SCAN_CHARS` (8.192) Zeichen. Diese Muster hängen an einem Literal und kappen ihr
  Capture bei 200 Zeichen — sie bleiben also linear und brauchen keine enge Schranke.
* **Unverankerte Heuristiken** und der Sliding-Window-Scan behalten die 1024-Zeichen-Grenze.

Die Trennung war notwendig, nicht kosmetisch: Ein pauschal größeres Fenster hätte die Präzision
verschlechtert. Der Scan liefert den *ersten* Verzeichnistreffer in Lesereihenfolge, und tief in
einer langen Beschreibung ist das typischerweise eine beiläufig erwähnte Straße statt des
betroffenen Halts — beim 43A-Item hätte das „Wien Hernals" statt „Dornbach" ergeben.

Verifiziert: Kernfall behoben, Kappung jenseits 8.192 greift weiterhin, ReDoS-Schranke hält
(< 0,1 ms auf adversarialem Input), keine Regression auf den echten Cache-Daten (unverändert
8 von 49 `unbekannt` — diese tragen tatsächlich keinen Marker).

---

## 3. Befund 2 — Generisches „Lokalbahn" kollabierte auf Guntramsdorf

**Feed-sichtbar.**

### Symptom

```
"Badner Bahn: Störung Lokalbahn"  ->  Wien Guntramsdorf Lokalbahn (WL)
```

Vier Badner-Bahn-Stationen bis zu 5,6 km auseinander beanspruchen den Alias `lokalbahn`:
Guntramsdorf Lokalbahn, Neu Guntramsdorf, Möllersdorf, Traiskirchen Lokalbahn. Der Tie-Break ist
deterministisch, aber semantisch willkürlich — jede generische Badner-Bahn-Meldung wurde
spezifisch Guntramsdorf zugeschrieben.

Der Validator meldet das als Alias-Schlüssel-Kollision. Die Kollisionsauflösung selbst
(`IDENTITY` vor `TEXT`, dann Quellen-Tie-Break) arbeitet korrekt; das Problem ist, dass ein
generischer Gattungsbegriff überhaupt als Stationsname akzeptiert wurde.

### Fix

`lokalbahn` ist in `_GENERIC_DIRECTORY_TOKENS` aufgenommen. Dieses Set existiert exakt für dieses
Muster — es verhindert bereits, dass „Hbf" alle Vorfälle auf `Wien Hauptbahnhof` bündelt. Der
Befund war dieselbe Klasse, nur unentdeckt.

Generische Erwähnungen liefern jetzt ehrlich `unbekannt`; vollständig benannte Stationen lösen
unverändert auf (`Störung Wien Traiskirchen Lokalbahn` → `Wien Traiskirchen Lokalbahn (WL)`).

---

## 4. Befund 3 — Sidecar verschwieg drei Validierungskategorien

**Nicht feed-sichtbar, aber irreführend für den Betrieb.**

`scripts/update_all_stations.py` schrieb in `data/stations_last_run.json` nur 8 der 11 Kategorien,
die `ValidationReport` führt. Es fehlten `identity_field_conflicts`, `cross_name_alias_issues` und
`alias_collision_issues` — ausgerechnet letztere war im auditierten Lauf die **einzige** Kategorie
mit einem Befund.

Wer nach einem Cron-Lauf den Sidecar liest, sah „alles sauber außer einem Duplikat", während der
vollständige Report zwei Funde hatte. Funktional harmlos (beide Kategorien sind bewusst nicht
blockierend — blockierend sind nur `provider`, `cross-station`, `naming`, `security`), aber es
untergräbt genau die Datei, die als Betriebs-Heartbeat gedacht ist.

**Fix:** Der Block spiegelt jetzt alle 11 Kategorien. Ein Test (`test_update_all_stations_diff_heartbeat.py`)
vergleicht die persistierten Felder gegen die Dataclass-Felder von `ValidationReport` und schlägt
fehl, sobald eine neue Kategorie nicht mitgeschrieben wird.

---

## 5. Befund 4 — Zwei produktiv verwaiste Funktionen (entschieden)

Der Befund lautete: `src/utils/stations.py` exportiert zwei Funktionen ohne Produktiv-Aufrufer.
`is_pendler()` wurde nur in einer Assertion in `tests/test_oebb_whitelist.py` aufgerufen,
`vor_station_ids()` überhaupt nicht. Da beide in `__all__` stehen, war das Entfernen eine
Produkt- und keine Codeentscheidung — sie wurde am 2026-09-13 vom Betreiber getroffen und fällt
für die beiden Funktionen unterschiedlich aus.

### `pendler` bleibt — als bewusst manuelle Markierung

Das Projekt braucht weiterhin eine Kennzeichnung für Pendlerbahnhöfe, und diese wird **manuell**
gesetzt. Damit ist die fehlende Aufruferzahl kein Argument: `is_pendler()` ist der benannte
Zugriff auf ein Datenfeld, das gepflegt werden soll.

Die Arbeitsteilung der beiden Scope-Flags ist deshalb kein Zufall, sondern Absicht:

| Flag | Herkunft | Mechanik |
| --- | --- | --- |
| `in_vienna` | **automatisch** | Koordinate gegen das offizielle `LANDESGRENZEOGD`-Polygon (5.637 Vertices) |
| `pendler` | **manuell** | bst_id in `data/pendler_bst_ids.json` (41 Einträge) oder Name in `data/pendler_candidates.json` |

Beide sind in `_annotate_station_flags` wechselseitig ausschließend; steht ein Wiener Bahnhof
versehentlich auf der Pendler-Liste, gewinnt `in_vienna` und es wird gewarnt. Diese Invariante
ist bereits abgedeckt (`tests/test_update_station_directory_flags.py`,
`tests/test_station_validation.py`), hier war also nichts zu ergänzen.

Umgesetzt wurde nur die Dokumentation: Der Docstring von `is_pendler()` sagt jetzt ausdrücklich,
dass es sich um eine kuratierte Markierung handelt, wo sie gepflegt wird und wie sie sich zu
`is_in_vienna()` verhält. Ohne diesen Hinweis lädt die leere Aufruferliste beim nächsten Aufräumen
erneut zum Entfernen ein.

### `vor_station_ids()` ist entfernt

Altlast, bestätigt. Die VOR/VAO-API wird nur noch für die Überwachung der S-Bahn-Stammstrecke
verwendet; einziger Konsument von `src/providers/vor.py` ist
`scripts/update_stammstrecke_status.py`, und der benennt seine Haltestellen direkt.

Nachgemessen vor dem Entfernen: Kein Aufrufer im gesamten Repo — auch kein Test. Der Docstring der
Funktion behauptete, sie diene als Repository-Default, wenn keine Umgebungsvariable
`VOR_STATION_IDS` gesetzt sei. Diese Variable wird von `src/providers/vor.py` gar nicht gelesen
(tatsächlich gelesen werden `VOR_HTTP_TIMEOUT`, `VOR_MAX_REQUESTS_PER_DAY`,
`VOR_QUOTA_FLUSH_BATCH_SIZE`, `VOR_USER_AGENT`), und die im Docstring genannte Fallback-Datei
`data/vor_station_ids_wien.txt` existiert nicht mehr. Die Funktion war also nicht nur ungenutzt,
sondern beschrieb einen Mechanismus, den es nicht gibt.

Mitgezogen wurden die beiden veralteten Verweise, die dieselbe Fiktion weitertrugen:

* Das Modul-Docstring von `src/providers/vor.py` nannte `VOR_STATION_IDS` als zentrale
  Konfiguration; es benennt jetzt die real gelesenen Variablen und hält den Stammstrecke-Scope fest.
* Der Hilfetext von `--vor-stops` in `scripts/update_station_directory.py` sagte „used for
  VOR_STATION_IDS". Die Option ist aktiv, tut aber etwas anderes: Sie reichert Stationen mit ihrer
  `vor_id` an.

## 6. Geprüft und für unbedenklich befunden

Diese Auffälligkeiten sahen nach Fehlern aus und sind keine:

**Die „Wien …Lokalbahn (WL)"-Namen.** Guntramsdorf, Möllersdorf und Traiskirchen liegen in
Niederösterreich, tragen aber ein `Wien `-Präfix. Das ist eine WL-Namenskonvention; die
Klassifikation ist korrekt (`in_vienna: False`, `pendler: True`).

**Das geografische Duplikat.** `Wien Jura-Soyfer-Gasse (WL)` und `Wien Walter-Lindenbaum-Gasse (WL)`
teilen sich exakt `(48.14919, 16.39268)`. Beides sind reale, verschiedene Halte in der
Per-Albin-Hansson-Siedlung; die identische Koordinate stammt aus den WL-OGD-Quelldaten. Beide
Namen lösen eindeutig auf. Ein Override würde Upstream-Daten überschreiben, ohne dass etwas
gewonnen wäre.

**Die 62,5 % `unbekannt` bei WL in der Jahresstatistik.** Historisch. Im aktuellen Cache sind es
16 %, und die verbleibenden Fälle sind Straßenadressen für Ersatzhalte („Busse halten Kärntner
Ring 5", „Frauenstiftgasse 7"), keine Haltestellennamen. `unbekannt` ist dort die richtige
Antwort — die katalog-first-Strategie verweigert bewusst freie Regex-Treffer, seit diese im Mai
2026 Störungsarten wie „Demonstration Linie" als Orte in die Statistik gespült hatten.

**Die 9,9 % nicht auflösbaren WL-Haltepunktnamen.** Von 2.302 eindeutigen Namen in
`data/wienerlinien-ogd-haltepunkte.csv` lösen 229 nicht auf. Stichprobe: fast durchweg
Betriebspunkte („Abstellplatz", „AEW Wendeanlage") und Werbe-/POI-Texte („ALBERTINA MODERN,
KÜNSTLERHAUS VEREINIGUNG"), keine echten Halte. Einzige echte Lücke: `Leberstraße` existiert
upstream nur als Kompositum („St. Marx S, Leberstraße"), der Kurzname löst daher nicht auf.

---

## 7. Methodik

* **Validator:** `python -m src.cli stations validate` gegen `data/stations.json` und
  `data/gtfs/stops.txt` (167 GTFS-Stops).
* **Feed-Anbindung:** Realer Build gegen die Live-Caches mit umgeleitetem `OUT_PATH`, dazu ein
  Probe-Skript, das die Verzeichnis-API vor dem Build monkeypatcht und alle Aufrufe samt
  Trefferquote zählt.
* **Datenqualität:** Direkte Auswertung von `data/stats/stoerungen_2026.csv` (2.969 Zeilen),
  `cache/wl_9d709a/events.json` und der WL-OGD-Quelldateien.
* **Tests:** `pytest -k "station"` (804), `tests/providers tests/scripts` (392),
  `pytest -k "alias or vienna or build_feed or baustellen"` (647).

Nebenwirkung der Testbuilds: Die Builds hängten 20 Zeilen an `data/stats/stoerungen_2026.csv` an.
Diese wurden zurückgerollt, der Arbeitsbaum war vor den Fixes wieder sauber.

---

## 8. Geänderte Dateien

| Datei | Änderung |
| --- | --- |
| `src/utils/stats.py` | Zweigeteiltes Suchfenster + `MAX_LABELLED_SCAN_CHARS`; `lokalbahn` in `_GENERIC_DIRECTORY_TOKENS` |
| `scripts/update_all_stations.py` | Sidecar spiegelt alle 11 Validierungskategorien |
| `tests/test_utils_stats.py` | 8 Regressionstests zu Befund 1 und 2 |
| `tests/test_update_all_stations_diff_heartbeat.py` | Drift-Guard für Befund 3 |

### Nachtrag 2026-09-13 (Befund 4)

| Datei | Änderung |
| --- | --- |
| `src/utils/stations.py` | `vor_station_ids()` entfernt (samt `__all__`); `is_pendler()` als manuelle Markierung dokumentiert |
| `src/providers/vor.py` | Modul-Docstring: reale ENV-Variablen statt `VOR_STATION_IDS`, Stammstrecke-Scope festgehalten |
| `scripts/update_station_directory.py` | Hilfetext von `--vor-stops` benennt den tatsächlichen Zweck |

---

## 9. Befund 5 — Korrupte Koordinaten in den WL-OGD-Quelldaten

**Feed-sichtbar. Beim Prüfen der Google-Koordinaten-Frage entdeckt.**

### Die Ausgangsfrage war hinfällig

Anlass war die Google-Maps-ToS-Mail und die Annahme, zehn WL-Stationen trügen Google-Koordinaten
im öffentlichen Repo. Diese Annahme stammte aus dem `source`-Feld, nicht aus den Werten — und sie
war falsch. Ein Wertevergleich zeigt: **Acht der zehn Koordinaten sind auf sechs Nachkommastellen
exakt das arithmetische Mittel ihrer eigenen WL-Haltepunkte** (3 bis 17 Stopps je Station). Ein
Google-Places-Ergebnis trifft einen solchen Schwerpunkt nicht zufällig.

Die zwei scheinbaren Ausreißer sind ebenfalls WL-Werte: `Wien Pilgramgasse (WL)` ist exakt das
Mittel über 8 ihrer 9 Stopps (ohne `7493`), `Wien Volkstheater (WL)` über 9 von 10 (ohne `8226`) —
also WL-Aggregate, die vor dem Hinzukommen eines Stopps berechnet wurden. `google_places` steht im
`source`-Feld, weil Google die `_google_place_id` beigesteuert hat; Place IDs dürfen laut ToS
unbegrenzt gespeichert werden. **Es gab nichts umzustellen.**

### Der eigentliche Fehler

`data/wienerlinien-ogd-haltepunkte.csv` enthält sechs Zeilen über drei aufeinanderfolgende DIVAs,
die gültige, aber falsche Koordinaten tragen — alle mit `Municipality=Wien`, alle im Waldviertel:

| DIVA | Station | Koordinate laut OGD | Entfernung vom Stephansplatz |
| --- | --- | --- | --- |
| 60201955 | Halblehenweg (Stopps 3973/3974) | 48,7236 / 15,0678 | **112 km** |
| 60201956 | Wassermanngasse (Stopps 3953/3954) | 48,6946 / 15,5787 | **80 km** |
| 60201954 | Leopoldine-Padaurek-Straße (5082/5083) | 48,4037 / 15,3864 | **76 km** |

Beide OGD-Dateien (`-haltepunkte.csv` und `-haltestellen.csv`) tragen dieselben Werte, der Defekt
sitzt also **upstream bei den Wiener Linien**, nicht im Repo. Von 4.578 abgeglichenen Haltepunkten
weichen genau diese sechs ab; alle übrigen stimmen auf 0,0 m.

Die bestehenden Gates greifen nicht: `_coerce_float` prüft Endlichkeit, die VOR-Branche prüft den
WGS-84-Bereich. (48,72 / 15,07) besteht beides — es ist eine tadellose Koordinate, nur die falsche.

### Nicht mehr latent

Für Halblehenweg und Leopoldine-Padaurek-Straße hält das Verzeichnis noch die korrekten Werte,
weil `_merge_wl_payload` Koordinaten nur in *leere* Slots schreibt. Bei **Wassermanngasse war der
Slot leer** — der Falschwert ist gelandet:

```
Wien Wassermanngasse (WL)  (48.69458, 15.578745)  in_vienna=False
text_has_vienna_connection("Störung in der Wassermanngasse") -> False
```

Eine echte Wiener Störung, die nur diese Haltestelle nennt, fällt damit durch das
Wien-Relevanz-Gate und erscheint nicht im Feed.

### Fix

Ein Plausibilitätsgate in `_aggregate_coordinates` — der einen Stelle, an der Stopp-Koordinaten zur
autoritativen Stationskoordinate werden und `in_vienna` treiben. Stopps jenseits von
`MAX_WL_STOP_DISTANCE_M` (50 km um den Netzmittelpunkt) fließen nicht in den Mittelwert und werden
namentlich geloggt.

Die Schwelle ist gemessen, nicht geschätzt: Der entfernteste echte Halt ist der Badner-Bahn-Endpunkt
Leesdorf/Baden bei **24,9 km**; danach ist die Verteilung bis **76,2 km** leer. 50 km liegt in dieser
Lücke mit rund doppeltem Spielraum über dem realen Netz und 1,5-fachem Abstand unter dem nächsten
Falschwert.

Bewusst wird nur der *Mittelwert* bereinigt, nicht `wl_stops`: Das Array bleibt ein getreues Abbild
des Upstream-Exports, damit der Defekt sichtbar bleibt. Ein Verwerfen der Zeilen beim Einlesen hätte
`wl_stops` geleert und die noch korrekten Altwerte von Halblehenweg und Leopoldine-Padaurek-Straße
zerstört — `_merge_wl_payload` ersetzt dieses Feld vollständig.

Verifiziert an den echten Daten: Die drei korrupten DIVAs liefern jetzt `(None, None)` mit Warnung,
**1.808 andere DIVAs sind unverändert** (null Abweichungen), inklusive der Badner Bahn und der
Grenzstationen wie `Lohnergasse`, deren Bahnsteige das Stadtgrenzen-Polygon überspannen.

### Nachtrag: die bereits gelandete Koordinate ist korrigiert

Gelöst über einen `patch_coords`-Override (DIVA 60201956) mit einer Koordinate aus OpenStreetMap
(Node `12038323326`, `highway=bus_stop`, Großfeldsiedlung, 1210), bezogen über Nominatim, nachdem
Overpass über drei Anläufe nicht verfügbar war.

Die Quelle wurde dreifach gegengeprüft, bevor sie eingetragen wurde:

1. **Kalibrierung.** Dieselbe Nominatim-Abfrage reproduziert die bereits im Mai verifizierte
   Haltestelle DIVA 60201954 auf **22 m** genau — und ihre drei nächsten Verzeichnis-Nachbarn
   (Edelsteingasse 197 m, Heinrich-von-Buol-Gasse 236 m) sind exakt jene, die der Bestands-Override
   als Verifikationsanker nennt.
2. **Nachbarschaft.** Die nächsten Verzeichnis-Stationen zur neuen Koordinate sind Bubergasse
   (184 m), Doderergasse (285 m) und Uhlirzgasse (308 m) — alles Halte der Großfeldsiedlung.
3. **Polygon.** `is_in_vienna` besteht.

Wirkung, gemessen nach dem Anwenden: `text_has_vienna_connection("Störung in der Wassermanngasse")`
liefert jetzt `True` statt `False`; der Eintrag trägt `in_vienna=True`. Der Diff an `stations.json`
umfasst acht Zeilen.

### Nachtrag: der Halblehenweg-Override war falsch und ist korrigiert

Die Kalibrierung warf einen zweiten Befund ab. OSM führt den Halt `Halblehenweg` (Node
`12690676528`, `highway=bus_stop`, an der `Rautenweg`) bei (48.256935, 16.486469); der Override vom
2026-05-16 setzte (48.249175, 16.482824) — **904 m** auseinander, beide in Donaustadt/Hirschstetten.

Drei unabhängige Prüfungen entschieden zugunsten von OSM:

| Prüfung | Alter Wert (Mai) | Neuer Wert (OSM) |
| --- | --- | --- |
| Abstand zum Verlauf der Straße Halblehenweg (Nominatim-Band 48.251444–48.256587) | **252 m südlich davon** | 39 m nördlich des Endes |
| Abstand zum nächsten WL-Nachbarn | 66 m (Kapellergasse) — im untersten **2 %** aller 1.805 WL-Stationen, Median 222 m | 319 m (Breitenlee Rautenweg) — unauffällig |
| Kartenprobe (Google Maps, Linie 28A) | — | Haltestelle `Halblehenweg` liegt auf `Rautenweg` an der Abzweigung |

Der ursprüngliche Eintrag war gegen die Nachbarn Bibernellweg, Kapellergasse und Kalmusweg
verifiziert worden — ein Cluster, das sich als das falsche herausstellte. Der Eintrag trägt jetzt
`corrected: "2026-09-13"` und nennt im `reason` beide Positionen samt Begründung, damit die
Korrektur nachvollziehbar bleibt.

**Ohne Feed-Wirkung.** Beide Positionen liegen in Wien, `in_vienna` war und bleibt `true`, und die
Wien-Erkennung läuft namensbasiert. Für eine Bushaltestelle wird die Koordinate sonst nirgends
ausgewertet — `nearest_rail_station` kommt nur für Bahnstationen infrage. Die Korrektur ist reine
Datengenauigkeit.

### Wie es dazu kam (Chronologie)

Der Guard verhinderte künftigen Schaden, korrigierte Wassermanngasse aber nicht: Der Merge
überschreibt einen vorhandenen Wert nicht, und der Namens-Fallback ist zirkulär (er liest denselben
Eintrag). Offline war keine Koordinate zu beschaffen — beide OGD-Dateien sind falsch,
`data/vor-haltestellen.csv` führt die Station nicht, `data/gtfs/stops.txt` (167 Zeilen) und
`data/oebb_geonetz_stops.json` (1.056 Stops) sind Bahn-Datensätze ohne Stadtbushalte, und Overpass
war aus der Audit-Umgebung gesperrt.

Zwei Versuche, den Wert von einem Sprachmodell zu bekommen, scheiterten an der Kalibrierung: zwei
Antworten für dieselbe Haltestelle lagen 571 m auseinander, und beim einzigen überprüfbaren Fall
(DIVA 60201954) betrug der Fehler 372 m. Drei Overpass-Anläufe scheiterten an der Infrastruktur
(Timeout, Timeout, ein Spiegel mit leerer Datenbank — erkennbar an `timestamp_osm_base: "34"`).
Erfolgreich war erst Nominatim, das dieselben OSM-Daten über ein einfaches GET liefert.

Die Lehre für künftige Fälle: Eine Koordinate aus dem Gedächtnis eines Sprachmodells ist für einen
Override nicht belastbar; ein Abruf mit zitierbarer Node-ID ist es. Der Unterschied war hier
messbar — 5 m gegenüber 574 m zwischen den beiden Modellantworten und der OSM-Node.

---

## 10. Geänderte Dateien (Befund 5)

| Datei | Änderung |
| --- | --- |
| `scripts/update_wl_stations.py` | `_is_plausible_wl_stop` + `MAX_WL_STOP_DISTANCE_M`; `_aggregate_coordinates` überspringt implausible Stopps und loggt sie |
| `tests/test_update_wl_stations_merge.py` | 5 Regressionstests inkl. Badner-Bahn-Grenzfall |
| `data/stations_overrides.json` | `patch_coords` für DIVA 60201956 (Wassermanngasse), Quelle OSM-Node `12038323326` |
| `data/stations.json` | Wassermanngasse auf die korrigierte Koordinate gezogen (`in_vienna` kippt auf `true`) |

---

## Audit Update 19:20

**Typ:** Ganzheitliches System-Audit (6 Säulen) — **rein diagnostisch, READ-ONLY**
**Datum/Uhrzeit:** 2026-09-13, 19:20 CEST
**Datenbasis:** Repo-Stand `ac3ef84` (Branch `claude/practical-clarke-jwp1na`), Live-Caches vom
2026-09-13 16:55 UTC, Pipeline-Lauf `2026-09-13T05:42:52Z`, Ledger-Stand `2026-09-13T18:17` (Verspätungen)
bzw. `2026-09-13T09:55` (Ausfälle)
**Abgrenzung:** Dieses Update ergänzt die Stationsverzeichnis-Tiefenanalyse in §1–§10 oben um einen
Querschnitt über Quota, Provider, Feed, Frontend und Dokumentation. Es wurde **kein** Projekt-Code,
kein Workflow und keine Konfiguration verändert — die einzige geschriebene Datei ist dieser
Audit-Bericht selbst; der zuvor bestehende Inhalt wurde dabei byteweise unverändert übernommen.

---

### A. Executive Summary

Die Pipeline läuft. Alle drei freien Provider-Caches sind vom heutigen Tag (WL 48, ÖBB 11,
Baustellen 22 Events), beide Feeds sind valides XML, die volle Test-Suite ist grün
(**9.186 passed, 2 skipped in 534 s**), der C901-Komplexitäts-Gate und der i18n-Gate bestehen,
und die im README publizierten Kennzahlen stimmen **exakt** mit den CSV-Roh-Ledgern überein
(nachgerechnet in §D.1).

Unter dieser grünen Oberfläche liegen zwei Befunde, die sofortiges Handeln verdienen:

1. **Der Stammstrecken-Monitor misst seit 2026-08-14 nur noch eine von zwei Richtungen.**
   Die Richtung `Praterstern` (nordwärts) liefert seit 30 Tagen **null** Beobachtungen — bei
   Verspätungen *und* Ausfällen. Die Tagesauflösung legt den Hergang offen: Am 13./14.08. verzeichnet
   die Nordrichtung mit 67 bzw. **102** Ausfällen das Sieben- bis Zehnfache eines normalen Tages,
   unmittelbar danach versiegen dort beide Datenströme. Die Ursache liegt damit sehr wahrscheinlich
   **stromaufwärts** (Sperre/Umleitung der Nordrelation), nicht in einem Code-Regress. Der Defekt ist
   ein anderer: **Kein Alarm, kein Exit-Code und kein Health-Check hat den 30-tägigen Halbausfall
   bemerkt**, und README wie Dashboard weisen die dadurch etwa halbierten Kennzahlen weiterhin als
   Korridor-Gesamtwerte aus — der Messausfall liest sich dort wie eine Betriebsverbesserung.
   → **B.1**, **B.2**

2. **Der Workflow `test-vor-api.yml` verbrennt ungezählte VAO-Quota.** Er feuert bei jedem Push auf
   `scripts/**` einen echten `location.name`-Live-Call per `curl` — vorbei an
   `save_request_count`, ohne Preflight-Gate und ohne die `external-api-fetch`-Concurrency-Gruppe.
   Das dokumentierte Budget-Modell in `docs/architecture.md` §7 kennt diesen Konsumenten nicht und
   führt `location.name` explizit mit **0** Calls/Tag. → **A.1 / F.1**

Zusätzlich wurde die im Code als „unmöglich" bezeichnete Quota-Race-Condition **empirisch
reproduziert** (6 Prozesse → 104 reale Reservierungen, Ledger zeigt 100; → **A.2**), und eine
quadratische Laufzeit-Explosion in der Text-Normalisierung vermessen (→ **C.2**; produktiv
derzeit **nicht** erreichbar — die einzige Aufrufstelle nutzt den linearen Pfad).

#### Befund-Übersicht

| ID | Säule | Schwere | Kurzfassung |
| --- | --- | --- | --- |
| **B.1** | Provider | **KRITISCH** | Stammstrecke: Richtung `Praterstern` seit 2026-08-14 stumm (30 d, Ursache vermutlich upstream) |
| **B.2** | Provider | **HOCH** | Keine Alarmierung, wenn ein Richtungs-Bucket dauerhaft leer ist |
| **A.1** | Quota | **HOCH** | `test-vor-api.yml` macht ungezählte Live-VAO-Calls |
| **A.2** | Quota | **HOCH** | Cross-Prozess-Race überschreitet reales Tagesbudget (belegt) |
| **F.1** | Doku | **HOCH** | `architecture.md` §7 Budget-Tabelle kennt A.1 nicht |
| **D.1** | Stationen | **HOCH** | 8 Namens-Kollisionen, `station_info()` verwirft still eine Hälfte |
| **B.3** | Provider | **MITTEL** | DNS-Rebinding-Schutz schaltet sich bei gesetztem `*_PROXY` still ab |
| **A.3** | Quota | **MITTEL** | Quota-Poison-Sentinel wirkt im Lock-Fehlerpfad nicht |
| **B.4** | Provider | **MITTEL** | `prune_cache` läuft nur nach erfolgreichem Write → Stale-Cache ohne Obergrenze |
| **C.2** | Feed | **MITTEL** | Quadratische Regex-Laufzeit in `_MULTI_BULLET_RE` (latent) |
| **D.2** | Stationen | **MITTEL** | Validator prüft keine Namens-Kollisionen (blinder Fleck) |
| **D.3** | Stationen | **MITTEL** | 86 % der 244.412 Aliase sind nach Normalisierung redundant |
| **E.1** | UI | **MITTEL** | i18n-Gate deckt JS-Inline-Ternaries nicht ab |
| **E.2** | UI | **MITTEL** | Deutsche Fehlertexte erreichen die EN-Oberfläche |
| **E.3** | UI | **MITTEL** | Dashboard lädt ~700 KB CSV von `raw.githubusercontent.com` |
| **F.2–F.4** | Doku | **MITTEL** | Docstrings verweisen auf entfernte Skripte / falschen Konsumenten |
| **C.3** | Feed | **NIEDRIG** | Groß-/Kleinschreibung in EN-Feed-Titeln |
| **F.5–F.6** | Doku | **NIEDRIG** | 54 % Docstring-Abdeckung; C901-Baseline veraltet |

---

### 1. API-Quotas & Rate Limiting

#### Ist-Zustand

`data/vor_request_count.json` steht bei `{"date": "2026-09-13", "requests": 39}` — plausibel für
den 30-Minuten-Takt (1 `/departureBoard`-Call pro Tick). `data/places_quota.json` trägt noch
`"month": "2026-05"` mit `total: 3`; das ist **gesund** und kein Fehler: Google Places ist Tier 3
(„letzter Ausweg"), OSM und HAFAS decken seit Mai alles ab. Der Monatswechsel wird sowohl in
`MonthlyQuota.maybe_reset_month` als auch in `preflight_quota_check._check_places` korrekt über
`Europe/Vienna` abgeglichen.

**Positiv geprüft und in Ordnung:**

* Alle Env-Overrides sind **tighten-only** geklammert: `MAX_REQUESTS_PER_DAY = min(env, 100)`,
  `HTTP_TIMEOUT = min(env, 15)`, `QUOTA_FLUSH_BATCH_SIZE = max(1, min(env, MAX_REQUESTS_PER_DAY))`.
  Ein `VOR_MAX_REQUESTS_PER_DAY=99999` kann das Vertragslimit nicht anheben.
* `VOR_RETRY_OPTIONS = {"total": 0}` — urllib3-Retries sind für VOR bewusst deaktiviert, damit ein
  gezählter Call nie mehrere reale Quota-Slots verbraucht. Korrekt und wichtig.
* **Keine Endlosschleifen.** Die beiden Kandidaten sind sauber begrenzt: `client.py:338`
  `while True` bricht bei `MAX_PAGES = 50` bzw. `skipped_due_to_quota`;
  `client.py:494` `while attempt <= self._config.max_retries` ist über `MAX_REQUEST_RETRIES`
  geklammert und negative Werte werden in `__post_init__` auf 0 gebodent.
* Negativ-Clamps gegen manipulierte Zähler (`int_count = max(0, int_count)`) an beiden Lesestellen.
* TOCTOU-sichere, größenbegrenzte Leser (`read_capped_json`, open-then-fstat) an allen
  Quota-Pfaden; `MemoryError` kann die Quota-Kette nicht mehr sprengen.
* Der `CircuitBreaker` ist solide: Zustandsübergänge unter `RLock`, Single-Flight-Probe im
  HALF_OPEN, und — oft vergessen — ein eigener `except BaseException`-Zweig, der den Probe-Slot
  auch bei `KeyboardInterrupt`/`SystemExit` freigibt, damit der Breaker nicht dauerhaft verklemmt.

#### A.1 — Ungezählte VAO-Calls aus `test-vor-api.yml` — **HOCH**

| | |
| --- | --- |
| **Betroffene Komponente** | `.github/workflows/test-vor-api.yml` (Step „Smoke test VOR endpoint", Z. 73–86) |
| **Problembeschreibung** | Der Workflow triggert auf `push` mit den Pfad-Filtern `src/providers/vor.py` und **`scripts/**`** (≈ 40 Dateien) und setzt dann einen echten Live-Request gegen `${VOR_BASE_URL}location.name` ab — als rohes `curl`, also **vollständig an `save_request_count` vorbei**. Der Call taucht in `data/vor_request_count.json` nie auf. Dazu kommen drei fehlende Schutzschichten: (a) der Workflow hat **keine** `concurrency:`-Gruppe, kann also parallel zu `external-api-fetch` laufen; (b) er ruft `scripts/preflight_quota_check.py` nicht auf; (c) der anschließende `pytest -q`-Step läuft mit gesetzten VOR-Secrets. Damit existiert ein automatisierter VAO-Konsument, den weder der Zähler noch das Budget-Modell kennt. In den letzten 30 Tagen haben 4 Commits die Trigger-Pfade berührt — der Sprengsatz ist also klein, aber ein Refactoring-Tag mit 20 Pushes auf `scripts/**` addiert 20 unsichtbare Calls auf die 48 gezählten. |
| **Konkreter Lösungsvorschlag** | Drei Maßnahmen, aufsteigend nach Aufwand: **(1)** Den Pfad-Filter von `scripts/**` auf die tatsächlich VOR-relevanten Skripte verengen (`scripts/update_stammstrecke_hbf.py`, `scripts/verify_vor_access_id.py`, `scripts/check_vor_auth.py`). **(2)** `concurrency: {group: external-api-fetch, cancel-in-progress: false}` ergänzen, damit der Smoke-Test nie parallel zum Cycle läuft. **(3)** Den `curl`-Step durch einen Aufruf ersetzen, der über `src.providers.vor` geht und damit gezählt wird — alternativ dem Step ein `python scripts/preflight_quota_check.py --check vor --margin 1` voranstellen und den Call auf `workflow_dispatch` beschränken. Anschließend `docs/architecture.md` §7 nachziehen (→ **F.1**). |

#### A.2 — Cross-Prozess-Race überschreitet das reale Tagesbudget — **HOCH**

| | |
| --- | --- |
| **Betroffene Komponente** | `src/providers/vor.py:744` `_persist_quota_to_disk`, `:865` `save_request_count`; `scripts/update_stammstrecke_status.py:913` `_charge_one_request` |
| **Problembeschreibung** | Der Fail-Fast in `_charge_one_request` liest über `load_request_count()` **nur den prozesslokalen Cache** und wird von `_QUOTA_LOCK` geschützt — einem `threading.RLock`, der zwischen Prozessen nichts bewirkt. Der `file_lock` greift erst beim Flush. Der Docstring behauptet, der Clamp `new_total = min(disk_count + unsaved_delta, MAX_REQUESTS_PER_DAY)` mache eine Budget-Überschreitung „unmöglich". Das gilt aber **nur für die persistierte Zahl** — die realen HTTP-Calls sind zum Flush-Zeitpunkt längst abgesetzt. Der Clamp verhindert die Überschreitung also nicht, er **verdeckt sie**. Empirisch reproduziert (6 parallele Prozesse gegen eine temporäre Zählerdatei, `WIEN_OEPNV_TEST_QUOTA_BATCH=1`): <br><br>`processes: 6` · `real charges made: 104` · `persisted on disk: {"date": "2026-09-13", "requests": 100}` · **`UNRECORDED OVERAGE: 4`** <br><br>Produktiv ist das Risiko durch die `external-api-fetch`-Concurrency-Gruppe (geteilt von `update-cycle.yml`, `build-feed.yml`, `manual-full-refresh.yml`) weitgehend entschärft — **aber `test-vor-api.yml` steht ausdrücklich außerhalb dieser Gruppe** (→ **A.1**), und `update-stations.yml` nutzt mit `update-stations-${{ github.ref }}` ebenfalls eine eigene Gruppe. |
| **Konkreter Lösungsvorschlag** | Die Reservierung muss **unter dem Dateilock** stattfinden, nicht erst der Flush: `_charge_one_request` so umbauen, dass Lesen → Prüfen → Inkrementieren → Schreiben in *einer* `file_lock(..., exclusive=True)`-Sektion liegt (bei 1 Call pro Tick ist der I/O-Aufwand vernachlässigbar; der Batch-Modus kann für Mehrfach-Call-Szenarien erhalten bleiben). Zusätzlich den Docstring bei `_persist_quota_to_disk` korrigieren — „der Clamp verhindert, dass der *Ledger* über MAX steigt; er verhindert **nicht**, dass real mehr Calls abgesetzt werden" — und beim Klammern ein `log.error` feuern, wenn `disk_count + unsaved_delta > MAX_REQUESTS_PER_DAY`, damit eine tatsächliche Überschreitung überhaupt sichtbar wird statt still weggeschnitten zu werden. |

#### A.3 — Quota-Poison-Sentinel bleibt im Lock-Fehlerpfad wirkungslos — **MITTEL**

| | |
| --- | --- |
| **Betroffene Komponente** | `src/providers/vor.py:836–862` (`_persist_quota_to_disk`, äußerer `except (OSError, TimeoutError)`) |
| **Problembeschreibung** | Zwei Fehlerpfade behandeln denselben Ausfall unterschiedlich. Der innere `except OSError` um `_write_request_count_file` setzt konsequent `_QUOTA_CACHE["count"] = MAX_REQUESTS_PER_DAY + 1` und leert `unsaved_delta` — der Prozess ist danach zuverlässig gesperrt. Der äußere `except (OSError, TimeoutError)` (Lockfile nicht zu öffnen, Lock-Timeout) **gibt nur `MAX_REQUESTS_PER_DAY + 1` zurück, ohne den Cache zu vergiften**. Da der einzige Aufrufer `_charge_one_request` den Rückgabewert von `save_request_count` gar nicht auswertet, hat der Sentinel dort keine Wirkung: Der nächste Call liest denselben unveränderten Cache, kommt am Fail-Fast vorbei und geht raus — bei anhaltendem Lock-Problem wiederholt, während gleichzeitig nichts mehr persistiert wird. |
| **Konkreter Lösungsvorschlag** | Den äußeren Handler an den inneren angleichen (`_QUOTA_CACHE["count"] = MAX_REQUESTS_PER_DAY + 1`, `_QUOTA_CACHE["unsaved_delta"] = 0`) und `_charge_one_request` den Rückgabewert prüfen lassen: `if vor_provider.save_request_count(now) > vor_provider.MAX_REQUESTS_PER_DAY: raise _QuotaExceeded(...)`. Damit wird der Sentinel auf beiden Pfaden wirksam und der Tick bricht sauber mit `quota_exceeded` ab, statt ungezählt weiterzufeuern. |

---

### 2. Provider-Resilienz (ÖBB, VOR, Wiener Linien)

#### Ist-Zustand

Alle drei freien Provider haben **heute** geliefert; die Caches tragen den Zeitstempel
2026-09-13 16:55 und sind gefüllt (WL 48, ÖBB 11, Baustellen 22 Events). Die Fehlerbehandlung der
Cache-Updater ist durchweg sauber: breites `except Exception` um `fetch_events()`, Rückgabe von
Exit-Code 1 **ohne** den bestehenden Cache anzufassen, dazu der `DataDegradationError`-Guard in
`write_cache`, der ein leeres Payload grundsätzlich und ein um >80 % geschrumpftes Payload
ebenfalls ablehnt. Der Workflow begrenzt jeden Fetch zusätzlich mit
`timeout --kill-after=10 90`. Defekte Payloads, Timeouts und unerwartete Strukturen blockieren die
Pipeline also **nicht** — der Feed-Builder liest ohnehin aus den Caches, nicht live.

#### B.1 — Stammstrecke: Richtung `Praterstern` seit 2026-08-14 stumm — **KRITISCH**

| | |
| --- | --- |
| **Betroffene Komponente** | `scripts/update_stammstrecke_hbf.py` (Plattform-Gate Z. 867–882, Richtungsauflösung Z. 899–926); Ledger `data/stats/stammstrecke_2026.csv`, `data/stats/ausfaelle_2026.csv` |
| **Problembeschreibung** | Der Monitor klassifiziert jede Abfahrt in `Meidling` (südwärts) oder `Praterstern` (nordwärts). Über drei Monate war die Verteilung nahezu perfekt symmetrisch — dann bricht die Nordrichtung binnen zweier Tage ab und kehrt nicht zurück. Die **tagesgenaue** Auswertung beider Ledger zeigt den Hergang: <br><br>**`stammstrecke_2026.csv`** (Verspätungen, Zeilen/Tag Meidling ∥ Praterstern) — … · 08-11: 41 ∥ 41 · 08-12: 41 ∥ 41 · **08-13: 38 ∥ 26** · **08-14: 41 ∥ 7** · **08-15: 41 ∥ 0** · 08-16 bis 09-13: **durchgehend 34–42 ∥ 0**. Letzte Nordzeile: **`2026-08-14T13:57:00+02:00`**. <br>**`ausfaelle_2026.csv`** (Ausfälle/Tag) — typisch 1–15 je Richtung · **08-13: 40 ∥ 67** · **08-14: 8 ∥ 102** · 08-15: 7 ∥ 8 · ab 08-16: **nur noch Meidling**. Letzte Nordzeile: `2026-08-15T05:03:00+02:00`. <br><br>**Der Ausfall-Peak geht dem Messverlust unmittelbar voraus.** Am 13./14.08. verzeichnet die Nordrichtung 67 bzw. **102** Ausfälle — das Sieben- bis Zehnfache eines normalen Tages — und genau danach versiegen dort *beide* Datenströme. Das Muster spricht dafür, dass die Ursache **stromaufwärts** liegt (großflächige Sperre/Umleitung der Nordrelation ab Mitte August, wie sie für Sommer-Baustellenfenster typisch ist) und **nicht** in einem Code-Regress: Der Monitor hat die Massenausfälle noch korrekt erfasst und ist erst verstummt, als am Hauptbahnhof keine Nordabfahrten mehr auf den Stammstrecken-Bahnsteigen erschienen. <br><br>Technisch greift dabei das Plattform-Gate: `STAMMSTRECKE_HBF_TRACK_TRUNKS = {"1", "2"}` verwirft jede Abfahrt, deren `rtTrack`/`track` nicht auf Bahnsteig 1 oder 2 führt (`dropped_non_stammstrecke_track`). Werden Nordabfahrten seither über andere Bahnsteige geführt, über Schienenersatzverkehr abgewickelt oder ohne Track-Information ausgeliefert, verwirft das Gate sie formal korrekt. Die Südrichtung bleibt unverändert stabil (08: ≈ 40/Tag, 09: ≈ 39/Tag) — es handelt sich also **nicht** um eine Fehl-Etikettierung nach Süden, sondern um einen vollständigen Verlust der Nord-Beobachtungen. Eine Bisektion auf das auslösende Ereignis war nicht möglich: Die Repo-Historie in dieser Session reicht nur bis zum 12.09. zurück. <br><br>**Der eigentliche Defekt ist nicht der Ausfall, sondern seine Unsichtbarkeit:** 30 Tage lang misst der Monitor den halben Korridor, ohne dass irgendetwas anschlägt (→ **B.2**) — und README wie Dashboard weisen „Beobachtungen (gesamt) 1.214" und „Ausfälle (gesamt) 212" weiterhin als Korridor-Kennzahlen aus. Nachgerechnet stammen **100 % davon aus der Südrichtung** (`direction`-Verteilung der letzten 30 Tage = `{'Meidling': 1214}`). Ein Leser der Zeitreihe sieht ab Mitte August einen scheinbaren Rückgang von Verspätungen und Ausfällen, der in Wirklichkeit ein Messausfall ist. |
| **Konkreter Lösungsvorschlag** | **Sofort (Diagnose):** Klären, ob es sich um eine reale Betriebslage handelt — ein manueller `/departureBoard`-Abruf am Wien Hbf zeigt, ob aktuell überhaupt Nordabfahrten gelistet sind und mit welchem `rtTrack`/`track`. Ergänzend die Zähler `dropped_no_track` / `dropped_non_stammstrecke_track` aus `_log_track_drops` in den Workflow-Logs um den 13./14.08. auswerten; steigt dort `dropped_non_stammstrecke_track` sprunghaft, ist die Bahnsteig-Hypothese bestätigt. **Dann, je nach Befund:** (a) **Reale Sperre/Umleitung** → kein Code-Fix nötig, aber der Zustand muss sichtbar werden (siehe **B.2**) und die betroffene Zeitspanne im Dashboard/README als richtungs-unvollständig gekennzeichnet werden, damit der Messausfall nicht als Betriebsverbesserung fehlgelesen wird; (b) **Bahnsteig geändert** → `STAMMSTRECKE_HBF_TRACK_TRUNKS` erweitern bzw. `_TRACK_TRUNK_RE` an das neue Format anpassen; (c) **VAO liefert keine Track-Info mehr für Nordabfahrten** → den Nicht-Cancelled-Pfad so ergänzen, dass die bereits vorhandene terminus-basierte Richtungsauflösung auch ohne Track greifen darf (heute liegt sie hinter dem Gate und kommt nie zum Zug). **In jedem Fall** ist **B.2** umzusetzen — ohne Alarmierung wiederholt sich der Vorfall unbemerkt.

#### B.2 — Kein Alarm, wenn ein Richtungs-Bucket dauerhaft leer bleibt — **HOCH**

| | |
| --- | --- |
| **Betroffene Komponente** | `scripts/update_stammstrecke_hbf.py:1050–1073` (`_process_tick`), `scripts/health_check.py:101–105` (`UPDATERS`) |
| **Problembeschreibung** | `_process_tick` schreibt `written_per_dir` für beide Richtungen und gibt **immer** `"ok"` zurück — auch wenn eine Richtung 0 Beobachtungen hat. Die Zahl landet ausschließlich in einer `LOGGER.info`-Zeile, die niemand auswertet. `scripts/health_check.py` prüft nur die drei freien Updater (`update_wl_cache.py`, `update_oebb_cache.py`, `update_baustellen_cache.py`) sowie Feed- und Stations-Frische — **die Stammstrecken-Ledger kommen darin überhaupt nicht vor**. Der 4-Stunden-Frischecheck `check_feed_freshness` schlägt ebenfalls nicht an, weil der Feed ja weiterhin alle 30 Minuten sauber gebaut wird. Ergebnis: Ein 30-tägiger Halbausfall der Kernmessung konnte vollständig unbemerkt bleiben — das ist die eigentliche Systemschwäche hinter **B.1**. |
| **Konkreter Lösungsvorschlag** | Zwei Ebenen: **(1) Im Skript** — nach dem Tick prüfen, ob eine Richtung über ein Zeitfenster (z. B. die letzten 24 h im Ledger) null Zeilen hat, und in dem Fall `LOGGER.error` + Rückgabe eines `degraded`-Status feuern, den `main()` in einen von 0 verschiedenen Exit-Code übersetzt. **(2) Im Health-Check** — `scripts/health_check.py` um einen `check_stammstrecke_directions(now)` erweitern, der beide Ledger liest und pro `DIRECTION_LABELS`-Eintrag die jüngste Zeile prüft; älter als z. B. 6 h → `Check(ok=False)`. Da der Workflow `health-check.yml` alle 6 Stunden läuft und bei Fehlern `::error`-Annotationen setzt, wäre **B.1** damit spätestens am 14.08. abends aufgefallen. |

#### B.3 — DNS-Rebinding-Schutz deaktiviert sich still bei gesetztem `*_PROXY` — **MITTEL**

| | |
| --- | --- |
| **Betroffene Komponente** | `src/utils/http.py:1641–1643` (`verify_response_ip`, „Proxy Compatibility (Task C)") |
| **Problembeschreibung** | `verify_response_ip` kehrt sofort zurück, sobald **irgendeine** der Variablen `HTTP_PROXY`, `HTTPS_PROXY`, `ALL_PROXY`, `http_proxy`, `https_proxy`, `all_proxy` in `os.environ` **existiert** — der Wert wird nicht einmal gelesen. Damit fällt die gesamte Peer-IP-Prüfung (`is_ip_safe(peer_ip)`, inkl. der drei Fallbacks über `_pinned_ip` und die Request-URL) weg: ein Fail-Open. Das ist nicht theoretisch — es ließ sich in dieser Audit-Umgebung direkt beobachten: `tests/places/test_client_security.py::test_client_ssrf_protection` schlägt hier fehl („DID NOT RAISE"), obwohl `is_ip_safe("127.0.0.1")` korrekt `False` liefert; mit `env -u HTTPS_PROXY …` laufen dieselben 7 Tests grün durch. **Der eigentliche Schaden ist die Test-Blindheit:** In einer Umgebung mit Proxy-Variablen wird der SSRF-Test nicht rot — er wird *wirkungslos grün*, sobald man die Assertion umdreht, und deckt genau dort nichts mehr ab, wo der Schutz fehlt (Corporate-Runner, Self-Hosted-Runner hinter Proxy, injizierte Env-Variable). |
| **Konkreter Lösungsvorschlag** | Statt „Variable existiert" prüfen, ob die konkrete Anfrage **tatsächlich** über einen Proxy lief — `response.connection`/`response.raw._pool` trägt bei requests/urllib3 die Proxy-Information; alternativ beim Anlegen der Session ein explizites Flag setzen und nur dann überspringen. Mindestens aber: (a) den leeren String ausschließen (`os.environ.get(k, "").strip()` statt `k in os.environ`), (b) die Umgehung per `log.warning` protokollieren, damit sie in Runner-Logs sichtbar wird, und (c) den Sicherheitstest so schreiben, dass er die Proxy-Variablen selbst via `monkeypatch.delenv` entfernt — dann testet er in jeder Umgebung dasselbe. |

#### B.4 — `prune_cache` läuft nur nach erfolgreichem Write — **MITTEL**

| | |
| --- | --- |
| **Betroffene Komponente** | `src/utils/cache.py:316` (`prune_cache`), aufgerufen ausschließlich aus `:549` innerhalb von `write_cache` |
| **Problembeschreibung** | Die einzige produktive Aufrufstelle von `prune_cache` liegt **am Ende von `write_cache`**. Fällt ein Provider aus, wird `write_cache` nie erreicht (der Updater bricht vorher mit Exit 1 ab) — und damit läuft auch die Eviction nie. Die 48-Stunden-Grenze `max_age_hours=48` ist also genau in dem Szenario unerreichbar, für das sie gedacht ist. Zusammen mit dem `DataDegradationError`-Guard, der ein leeres Payload grundsätzlich zurückweist, ist das System stark in Richtung „alte Daten behalten" verzerrt — ohne jede Obergrenze. **Abgemildert** wird das durch zwei vorhandene Mechanismen, weshalb der Befund nicht höher eingestuft ist: `build_feed._detect_stale_caches` warnt ab `CACHE_MAX_AGE_HOURS = 24` im Run-Report, und die Item-Filter in `build_feed.py:3389 ff.` werfen Einträge mit vergangenem `ends_at` bzw. jenseits von `MAX_ITEM_AGE_DAYS = 365` ohnehin raus. Die Stale-Warnung ist allerdings reine Report-Kosmetik: sie setzt keinen Exit-Code und schließt den betroffenen Provider nicht aus dem Feed aus. |
| **Konkreter Lösungsvorschlag** | Den Stale-Zustand vom reinen Warnen in eine wirksame Konsequenz überführen: In `_detect_stale_caches` bei Überschreiten eines zweiten, höheren Schwellwerts (z. B. `2 × CACHE_MAX_AGE_HOURS`) die Items des betroffenen Providers **nicht mehr in den Feed aufnehmen** und den Provider im Run-Report als `stale-excluded` führen. Ergänzend `prune_cache(provider=…)` auch im Fehlerpfad der Updater aufrufen (also in `update_*_cache.py` vor `return 1`), damit die Eviction nicht an einem erfolgreichen Write hängt. |

#### B.5 — Inkonsistente Fehler-Semantik in `oebb._fetch_xml` — **NIEDRIG**

| | |
| --- | --- |
| **Betroffene Komponente** | `src/providers/oebb.py:1430–1481` (`_fetch_xml`) |
| **Problembeschreibung** | Die Funktion ist als `-> ET.Element | None` deklariert und gibt bei Content-/Format-Fehlern sauber `None` zurück. Beim zweiten fehlgeschlagenen Netzwerkversuch (`attempt == 1`) macht sie stattdessen ein nacktes `raise` — die `requests.RequestException` verlässt `_fetch_xml` und `fetch_events()`, das sie nicht abfängt. Produktiv ist das folgenlos, weil `scripts/update_oebb_cache.py:41` sowohl `RequestException` als auch `Exception` abfängt und den Cache unangetastet lässt. Die Asymmetrie „Formatfehler → `None`, Netzfehler → Exception" ist aber eine Stolperfalle für jeden künftigen Aufrufer, der sich auf die `| None`-Signatur verlässt. |
| **Konkreter Lösungsvorschlag** | Den finalen `raise` durch `log.warning(...)` + `return None` ersetzen, damit die Signatur hält, und einen Regressionstest ergänzen, der `fetch_events()` bei dauerhaftem Netzwerkfehler auf `[]` festnagelt. |

---

### 3. RSS-Feeds & Datenkonsistenz

#### Ist-Zustand — alle harten Prüfungen bestanden

Beide Feeds wurden maschinell validiert:

| Prüfung | `docs/feed.xml` | `docs/feed.en.xml` | `docs/sitemap.xml` |
| --- | --- | --- | --- |
| XML wohlgeformt (`ET.parse`) | ✅ | ✅ | ✅ |
| XML-Deklaration + Encoding | ✅ `utf-8` | ✅ `utf-8` | ✅ `utf-8` |
| `<item>`-Anzahl | 10 | 10 | — (13 `<url>`) |
| In XML 1.0 **verbotene** Codepoints | **0** | **0** | **0** |
| `U+FFFD` (Encoding-Unfälle) | **0** | **0** | **0** |

Die Escaping-Prüfung ist damit bestanden: Es gibt keine unsauberen Zeichenkodierungen, keine
Steuerzeichen und keine Ersetzungszeichen. Die Kanal-Metadaten sind pro Sprache korrekt gesetzt
(`language: de` / `language: en`, eigener Titel und eigene Description), die GUIDs sind über beide
Sprachfassungen stabil — das ist richtig so, weil es dasselbe Ereignis ist.

#### C.1 — Datenkonsistenz der publizierten Kennzahlen: **verifiziert korrekt**

Die README-Tabellen wurden gegen die Roh-Ledger nachgerechnet (Referenzzeitpunkt
2026-09-13 18:30 CEST, wie im README ausgewiesen):

| Kennzahl | README | Nachgerechnet | |
| --- | --- | --- | --- |
| Beobachtungen 30 d | 1.214 | **1214** | ✅ |
| Ø Verspätung 30 d | 0,4 min | **0,3868 → 0,4** | ✅ |
| Kritische Verspätungen (> 9 min) | 14 | **14** | ✅ |
| Beobachtungen 60 min | 2 | **2** | ✅ |
| Ø Verspätung 60 min | 0,0 min | **0,0** | ✅ |
| Ausfälle 30 d | 212 | **212** | ✅ |
| Häufigste Linien | S2 (81), CJX9 (71), S4 (49) | **S2 81, CJX9 71, S4 49** | ✅ |

Die Aggregationskette vom CSV bis ins README arbeitet also exakt. **Wichtige Einschränkung:**
Diese Zahlen sind zwar korrekt *berechnet*, aber wegen **B.1** inhaltlich unvollständig — sie
beschreiben nur die Südrichtung.

#### C.2 — Quadratische Laufzeit in `_MULTI_BULLET_RE` (ReDoS-Klasse, latent) — **MITTEL**

| | |
| --- | --- |
| **Betroffene Komponente** | `src/utils/text.py:50` `_MULTI_BULLET_RE = re.compile(r"(?:\s*•\s*){2,}")`, angewandt in `html_to_text` (`:325`) |
| **Problembeschreibung** | Das Muster enthält zwei benachbarte `\s*` innerhalb einer wiederholten Gruppe. Trifft es auf einen langen Whitespace-Lauf **ohne** `•`, probiert die Engine an jeder Startposition den gesamten Lauf durch — Laufzeit O(n²). Gemessen (isoliert, `re.sub` über reine Whitespace-/Newline-Läufe): n=1.000 → 0,001 s · 2.000 → 0,004 s · 4.000 → 0,016 s · 8.000 → 0,062 s · 16.000 → 0,248 s — sauber vervierfacht bei doppelter Länge. Ende-zu-Ende über `html_to_text("<br>" * n, collapse_newlines=False)` — also mit provider-kontrolliertem HTML, das *null* Content-Zeichen enthält und deshalb ungekürzt durch `truncate_html(limit=5000)` läuft: 128 KB → 1,09 s · 256 KB → 4,09 s · **512 KB → 15,8 s**. Am `MAX_SAFE_LENGTH`-Deckel von 1.000.000 Zeichen wären das hochgerechnet ~60 s für **ein einziges** Beschreibungsfeld; der HTTP-Deckel `MAX_PAYLOAD_SIZE` liegt bei 10 MiB. <br><br>**Entwarnung für den Ist-Zustand — und der Grund für „MITTEL" statt „HOCH":** Die **einzige** produktive Aufrufstelle ist `src/build_feed.py:4332` und übergibt `collapse_newlines=True`. Auf diesem Pfad ersetzt `_NEWLINE_CLEANUP_RE` jeden Newline vorab durch `" • "`, das Muster *matcht* dann und läuft linear. Nachgemessen: 128 KB → 0,198 s · 256 KB → 0,309 s · 512 KB → 0,577 s · **1 MB → 1,07 s**. Produktiv ist der Angriff derzeit also **nicht** erreichbar. Das Risiko ist die Signatur: `collapse_newlines` ist mit `False` vorbelegt, d. h. **jeder künftige Aufrufer, der das Flag nicht explizit setzt, landet im quadratischen Pfad**. |
| **Konkreter Lösungsvorschlag** | Die Mehrdeutigkeit aus dem Muster nehmen — `re.compile(r"\s*(?:•\s*){2,}")` bindet den führenden Whitespace einmalig und entfernt das verschachtelte Quantifizierer-Paar; das Matching-Verhalten für die beabsichtigten Eingaben bleibt identisch (mit einem Round-Trip-Test über die bestehenden Feed-Fixtures absichern). Ergänzend `collapse_newlines` zu einem Pflicht-Keyword-Argument machen (`*, collapse_newlines: bool`), damit niemand versehentlich den langsamen Pfad erbt, und in `truncate_html` zusätzlich die **Ausgabelänge** deckeln, nicht nur die Eingabelänge — aktuell kann markup-lastiges HTML den 5.000-Zeichen-Content-Deckel passieren und trotzdem ~1 MB an die Regex-Kette weiterreichen. |

#### C.3 — Groß-/Kleinschreibung in EN-Feed-Titeln — **NIEDRIG**

| | |
| --- | --- |
| **Betroffene Komponente** | Übersetzungspfad für Feed-Titel (`docs/feed.en.xml`) |
| **Problembeschreibung** | Beim Übersetzen wird die Wortmitte-Schreibweise nicht an die Titelposition angepasst. Im aktuellen Feed: `"44: event Trains stop Rosensteingasse on line 9 direction Westbahnhof"` und `"86A/86AR/87A: event"` (aus „Veranstaltung"), ebenso `"3A: buses stop Kärntner Ring 5"`. Deutsche Substantive sind großgeschrieben, ihre englischen Entsprechungen nicht — nach dem `": "` wirkt das wie ein Tippfehler. Rein kosmetisch, aber es ist die erste Zeile, die ein EN-Abonnent sieht. |
| **Konkreter Lösungsvorschlag** | Im Titel-Renderer den Teil nach `": "` einmalig kapitalisieren (erstes Zeichen `upper()`, Rest unangetastet, damit Linienkürzel wie `86A` und Eigennamen unverändert bleiben). |

#### C.4 — Feed publiziert 10 von 81 aktiven Meldungen — **INFO**

| | |
| --- | --- |
| **Betroffene Komponente** | `src/config/defaults.py:42` `DEFAULT_MAX_ITEMS = 10` |
| **Problembeschreibung** | Die Caches halten aktuell 48 + 11 + 22 = 81 Events; der Feed enthält 10. `MAX_ITEMS` wird in `update-cycle.yml` nicht überschrieben, es greift also der Default. Das ist eine bewusste Designentscheidung (Aktualität vor Vollständigkeit) und **kein Fehler** — aber der Abstand ist groß genug, dass er bewusst getroffen sein sollte, zumal `docs/site.html` dieselbe Quelle für die Störungsliste nutzt und damit ebenfalls nur 10 Meldungen zeigt. |
| **Konkreter Lösungsvorschlag** | Keine Codeänderung nötig. Empfehlung: Den Wert in `docs/architecture.md` als bewusste Kappung dokumentieren, und für das Dashboard prüfen, ob dort ein höherer `MAX_ITEMS`-Wert (oder ein zweiter, ungekappter Feed) sinnvoll wäre — die Website hat nicht die Größenbeschränkungen eines RSS-Readers. |

---

### 4. Stationsverzeichnis (Station Directory)

#### Ist-Zustand

Die Kernqualität ist gut und deckt sich mit dem Befund der Tiefenanalyse in §1 oben: 2.243
Stationen, **0 fehlende Koordinaten**, 0 Koordinaten-Anomalien, 0 GTFS-Abweichungen, 0 Provider-
und Sicherheitsprobleme, leere Quarantäne, Diff-Bericht ohne Änderung (`+0`), alle vier
Teilskripte mit Exit-Code 0. Die Lookup-Performance im Betrieb ist ausgezeichnet:
**0,1 µs pro `station_info()`-Aufruf** im warmen Zustand.

#### D.1 — 8 Namens-Kollisionen; `station_info()` verwirft still eine Hälfte — **HOCH**

| | |
| --- | --- |
| **Betroffene Komponente** | `data/stations.json`; `scripts/update_wl_stations.py:1086` `_merge_colocated_duplicates`; `src/utils/stations.py:918` `station_info` |
| **Problembeschreibung** | Acht Namen existieren doppelt — alle WL-Stops, alle **ohne** unterscheidenden Identifier (`vor_id`, `bst_id`, `eva_nr`, `ifopt_id` sind bei allen 16 Einträgen `None`): <br><br>`Wien Arbeitergasse, Gürtel (WL)` 157 m · `Wien Schottenring (WL)` 165 m · `Wien Lafitegasse (WL)` 177 m · `Wien Heinrich-von-Buol-Gasse (WL)` 281 m · `Wien Kirchengasse (WL)` 304 m · `Wien Skraupstraße (WL)` 310 m · `Wien Heizwerkstraße (WL)` 463 m · `Wien Märzstraße (WL)` 479 m <br><br>Alle acht liegen im Band **150–500 m** — also exakt in der Lücke, die `_merge_colocated_duplicates` mit `_COLOCATED_MERGE_DISTANCE_M = 150.0` bewusst offen lässt. Der Docstring dieser Funktion verweist für genau diesen Fall auf eine nachgelagerte Absicherung: *„they remain separate entries with the existing DIVA-suffix disambiguation"*. **Diese Absicherung existiert nicht mehr** — 30 Zeilen darüber (`:1072`) steht, dass `_disambiguate_duplicate_names` am 2026-05-12 entfernt wurde. Die Kollisionen laufen seither ungebremst durch. <br><br>**Messbare Auswirkung:** `station_info()` löst jeden dieser Namen deterministisch auf den Eintrag mit der **niedrigeren `wl_diva`** auf und verwirft den zweiten stillschweigend — verifiziert: `station_info("Wien Kirchengasse (WL)")` → `(48.199674, 16.352869)`, der zweite Eintrag `(48.202301, 16.351749)` ist über den Namen nicht erreichbar. Das trifft jeden namensbasierten Konsumenten, u. a. `baustellen.relevant_station`, das mit `DEFAULT_STATION_RADIUS_M = 150.0` arbeitet: Bei `Wien Märzstraße` (479 m Abstand) liegt die Baustelle am *anderen* Halt garantiert außerhalb des Radius und fällt aus dem Feed. Bemerkenswert ist zudem `Wien Schottenring (WL)`: Ein Eintrag trägt `source: "google_places,wl"`, der andere nur `"wl"` — hier wurde kostenpflichtige Places-Quota auf einen Eintrag verwendet, der anschließend nicht mit seinem Zwilling zusammengeführt wurde. |
| **Konkreter Lösungsvorschlag** | Die im Docstring versprochene Disambiguierung wiederherstellen — aber ohne den kosmetischen Nachteil, der zur Entfernung geführt hat: Statt den DIVA-Suffix in den **Anzeigenamen** zu schreiben (`Wien Bahnhof (WL 60205022)`, was den RSS-Feed verunstaltete), einen separaten, nicht angezeigten Schlüssel führen — z. B. `disambiguated_key: "wien märzstraße (wl)#60200909"` — und `_station_lookup` diesen als Identity-Class-Alias registrieren lassen. Der Feed rendert weiter den sauberen Namen, aber beide Einträge bleiben adressierbar. Parallel den irreführenden Docstring in `_merge_colocated_duplicates` korrigieren und in `station_info` bei einer echten Namens-Kollision ein `log.warning` mit beiden `wl_diva`-Werten feuern, damit der stille Verlust sichtbar wird. Für `Wien Schottenring (WL)` zusätzlich prüfen, ob ein `patch_merge`-Override in `data/stations_overrides.json` die beiden Einträge zusammenführen sollte. |

#### D.2 — Validator kennt keine Namens-Kollisionen (blinder Fleck) — **MITTEL**

| | |
| --- | --- |
| **Betroffene Komponente** | `src/utils/stations_validation.py:614` `_find_duplicate_coordinate_groups`; Ausgaben `docs/stations_validation_report.md`, `data/stations_last_run.json` |
| **Problembeschreibung** | Die Duplikatprüfung arbeitet ausschließlich **koordinatenbasiert** (gerundete Lat/Lon-Gruppen). Entsprechend meldet der Bericht *„Geografische Duplikate: 1"* und *„Namens-Probleme: 0"* — während acht Namens-Kollisionen im Verzeichnis stehen (**D.1**). Das ist kein Rechenfehler, sondern eine Kategorie, die schlicht nicht geprüft wird. Die Konsequenz ist gravierender als der Einzelbefund: Die Executive Summary des Audits in §1 dieser Datei stützt sich auf genau diese Null-Meldung („null … Namens- und ID-Kollisionen"). Ein Gate, dessen Grünmeldung eine ungeprüfte Eigenschaft suggeriert, ist gefährlicher als gar kein Gate. |
| **Konkreter Lösungsvorschlag** | Eine Prüfung `_find_duplicate_name_groups` ergänzen, die auf dem **normalisierten** Namen (`_normalize_token`) gruppiert und jede Gruppe mit ≥ 2 Einträgen meldet — mit Distanz und den beteiligten `wl_diva`/`bst_id`-Werten, analog zur bestehenden `Alias-Schlüssel-Kollisionen`-Sektion, die dieses Format bereits vorbildlich umsetzt. Die Kennzahl in `stations_last_run.json` als eigenes Feld (`name_duplicates`) führen, damit sie nicht mit `duplicates` (geografisch) verwechselt wird. |

#### D.3 — 86 % der 244.412 Aliase sind nach Normalisierung redundant — **MITTEL (Performance/Größe)**

| | |
| --- | --- |
| **Betroffene Komponente** | `scripts/enrich_station_aliases.py`; `data/stations.json`; `src/utils/stations.py:637` `_station_lookup` |
| **Problembeschreibung** | Das Verzeichnis trägt **244.412 Aliase** auf 2.243 Stationen — im Mittel **109 pro Station**, im Maximum **692** (`Wien Laxenburger Straße, Gudrunstraße (WL)`). Sie werden kombinatorisch erzeugt: Präfix (`Bahnhof …`/`Wien …`/ohne) × Trenner (`, ` / ` / `) × Schreibvariante (`Str.` / `Strasse` / `Straße`, für **jedes** Namensglied) × Suffix (`(WL)` / `Station` / ohne). Der Lookup normalisiert diese Varianten anschließend über `_normalize_token` (Casefolding, Interpunktion, `ß`→`ss`) **wieder zusammen** — und zwar fast vollständig: <br><br>• Einzelstation: **692 Aliase → 32 distinkte Schlüssel (95 % redundant)**; ein einzelner Schlüssel (`laxenburger str gudrunstrasse`) wird von **56** Schreibweisen erreicht. <br>• Projektweit: **244.412 Aliase → 33.011 distinkte Schlüssel = 86 % redundant.** <br><br>Kosten: Die Aliase machen **7,29 MB von 8,22 MB (88,7 %)** des Verzeichnisses aus; die Datei liegt mit 10,7 MB im Git und wird wöchentlich neu geschrieben (Repo-Wachstum). Der kalte Aufbau von `_station_lookup()` dauert **1,98 s** pro Prozess — bei 48 Ticks/Tag plus Feed-Builds ist das messbare, vollständig vermeidbare Rechenzeit. Der warme Lookup ist mit 0,1 µs dagegen völlig unkritisch; **es ist kein Laufzeitproblem der Abfrage, sondern eines der Erzeugung und Speicherung**. |
| **Konkreter Lösungsvorschlag** | Die Alias-Erzeugung gegen den Normalisierer laufen lassen, statt an ihm vorbei: In `enrich_station_aliases.py` jeden Kandidaten vor dem Anhängen durch `_normalize_token` schicken und nur speichern, wenn der Schlüssel noch nicht belegt ist. Das reduziert das Feld auf ~33.000 Einträge, schrumpft `data/stations.json` um geschätzt 6–7 MB und verkürzt den kalten Lookup-Aufbau entsprechend. Da der Lookup ohnehin nur normalisierte Schlüssel verwendet, ist die Änderung **verhaltensneutral** — absicherbar mit einem Test, der für eine Stichprobe historischer Alias-Schreibweisen prüft, dass `station_info()` vor und nach der Umstellung dieselbe Station liefert. Ein Migrationslauf sollte über `docs/stations_diff.md` verifiziert werden (erwartet: 0 Added / 0 Removed / 0 Renamed). |

---

### 5. Mehrsprachige Webseite (UI/UX)

#### Ist-Zustand

Die Sprachumschaltung ist handwerklich gut gebaut: `applyTranslationsToDom` setzt korrekt
`document.documentElement.lang`, übersetzt `textContent`, `aria-label`, `title` und `content`
(Meta/OG), tauscht die Feed-`href` zwischen `feed.xml` und `feed.en.xml`, stellt nach
`innerHTML`-Rewrites die dynamischen Jahres-Labels wieder her und rendert Live-Status,
Fehlerzeilen und Zeitstempel-Suffix neu. Der `localStorage`-Zugriff ist in `try/catch` gekapselt
(Private Mode). Der i18n-Gate läuft grün: **73 HTML-Schlüssel, 78 JS-Schlüssel, alle zugeordnet.**
Die Assets sind konsistent — `scripts/optimize_site_assets.py --check` meldet keine Drift, und die
Content-Hashes in `docs/site.html` stimmen mit den Dateien überein (`site.min.css` → `2ba857116103`,
`site.min.js` → `0327248538c0`, beide verifiziert).

#### E.1 — i18n-Gate deckt die JS-Inline-Ternaries nicht ab — **MITTEL**

| | |
| --- | --- |
| **Betroffene Komponente** | `scripts/check_i18n_coverage.py`; `docs/assets/site.js` |
| **Problembeschreibung** | Der Gate vergleicht ausschließlich `data-i18n*`-Attribute aus `docs/site.html` gegen das `I18N_EN`-Wörterbuch. Ein erheblicher Teil der Oberflächentexte lebt aber gar nicht dort, sondern in **12 verstreuten Inline-Ternaries** der Form `currentLang === "en" ? "Construction" : "Baustellen"` (u. a. `:602`, `:604`, `:638`, `:671`, `:688`, `:734`, `:987`, `:421`) plus den parallelen Wörterbuch-Paaren `CHART_TEXT_DE/EN`, `WEATHER_TEXT_DE/EN`, `WEEKDAY_LONG_DE/EN`. Für diesen Bereich existiert **keine** Abdeckungsprüfung: Wer einen neuen String direkt als deutsches Literal einbaut, bekommt einen grünen Gate und eine EN-Oberfläche mit deutschem Text. Der Gate suggeriert eine Vollständigkeit, die er nicht prüft. |
| **Konkreter Lösungsvorschlag** | Den Gate um eine Heuristik für `site.js` erweitern: alle String-Literale einsammeln, die deutschsprachige Marker enthalten (Umlaute, `ß`, oder eine kleine Stoppwortliste wie `keine`, `nicht`, `Uhr`, `werden`), und jedes davon als Verstoß melden, das **nicht** innerhalb eines `currentLang`-Ternaries, eines `*_DE`-Wörterbuchs oder von `I18N_EN` steht. Mittelfristig sauberer: die Ternaries in `I18N_EN` überführen und über einen einzigen `t(key)`-Helfer auflösen — dann prüft der bestehende Gate-Mechanismus automatisch mit. |

#### E.2 — Deutsche Fehlertexte erreichen die englische Oberfläche — **MITTEL**

| | |
| --- | --- |
| **Betroffene Komponente** | `docs/assets/site.js:532`, `:534` (`parseFeed`), `:425` (`showError`), `:1332` |
| **Problembeschreibung** | `parseFeed` wirft zwei fest deutsche Meldungen: `throw new Error("Feed konnte nicht geparst werden")` und `throw new Error("Feed ohne <channel>-Element")`. Diese landen über `showError("feed-error", "feed-error-prefix", err.message)` unverändert in der Oberfläche. Der **Präfix** wird korrekt übersetzt, das **Detail** nicht — ein EN-Nutzer liest also `"Feed could not be loaded: Feed konnte nicht geparst werden"`. Verschärfend: `showError` persistiert das Detail in `node.dataset.errorDetail`, und `applyTranslationsToDom` (`:1573`) baut die Zeile beim Sprachwechsel aus genau diesem Dataset neu zusammen — der deutsche Text überlebt also jeden Umschaltvorgang. Dass es sich um ein Versehen handelt, zeigt Zeile `:1221`: dort ist die Meldung englisch (`"weather payload without usable data"`). |
| **Konkreter Lösungsvorschlag** | Die geworfenen Fehler auf i18n-Schlüssel umstellen statt auf freie Texte: `throw new Error("feed-parse-failed")`, im `catch` den Schlüssel über `statusText()` auflösen und erst dann an `showError` geben; die beiden neuen Schlüssel in `I18N_EN` und als `data-i18n`-Default hinterlegen, damit der Gate sie abdeckt. Für nicht übersetzbare Details (z. B. HTTP-Statuszeilen) einen generischen, sprachneutralen Fallback verwenden. |

#### E.3 — Dashboard zieht ~700 KB CSV von `raw.githubusercontent.com` — **MITTEL**

| | |
| --- | --- |
| **Betroffene Komponente** | `docs/assets/site.js:15` `RAW_BASE`, `:466` (`fetchText` für die Jahres-Ledger) |
| **Problembeschreibung** | Die Statistik-Panels laden ihre Rohdaten nicht von GitHub Pages, sondern direkt von `https://raw.githubusercontent.com/<repo>/main/data/stats/` — pro Seitenaufruf `stammstrecke_2026.csv` (**430 KB**), `stoerungen_2026.csv` (187 KB) und `ausfaelle_2026.csv` (87 KB), zusammen **rund 700 KB**, nur um daraus ein 30-Tage-Fenster zu berechnen. Zwei Probleme: **(a) Wachstum** — die Ledger sind Jahresdateien; 8.904 Zeilen entsprechen vier Monaten, hochgerechnet aufs volle Jahr also ~1,3 MB allein für die Stammstrecke, und jeder Besucher lädt im Dezember die Januar-Daten mit. **(b) Fremde Origin** — `raw.githubusercontent.com` ist kein Auslieferungs-CDN, unterliegt eigenen Rate-Limits und liegt außerhalb der Pages-Zusage. Fällt es aus oder drosselt es, bleibt die Seite selbst erreichbar, aber die Diagramme scheitern — und zwar in die Fehlerzeile aus **E.2** hinein. |
| **Konkreter Lösungsvorschlag** | Im `update-cycle.yml` neben dem Markdown-Dashboard eine **vorverdichtete JSON-Datei** unter `docs/` erzeugen (z. B. `docs/stats-summary.json` mit den bereits aggregierten 30-Tage- und 60-Minuten-Werten sowie den Zeitreihen für die Charts) und `site.js` von dort lesen lassen — gleiche Origin wie die Seite, wenige KB statt 700, kein Wachstum über das Jahr. Die Roh-CSVs bleiben unter `data/stats/` für Analysen verlinkt. Falls die Roh-Ledger im Frontend erhalten bleiben sollen, mindestens auf monatliche Dateien umstellen und nur die benötigten Monate nachladen. |

---

### 6. Dokumentations-Status

#### Ist-Zustand

Die Dokumentationsbasis ist für ein Projekt dieser Größe überdurchschnittlich: `README.md` (138 Z.),
`docs/architecture.md` (41 KB), `docs/development.md` (67 KB), dazu `how-to/`, `reference/`,
`examples/`, ein `llms.txt` und ein umfangreiches `docs/archive/audits/`. Eine automatisierte
Prüfung aller Markdown-Dateien (ohne Archiv) auf tote Datei-Verweise ergab 14 Treffer — davon
sind nach manueller Prüfung **die meisten korrekt**: `docs/development.md` erklärt die verkürzten
Cache-Pfade in einer expliziten Fußnote in Z. 74; `docs/how-to/test-api-secrets.md` beschreibt eine
*anzulegende* Datei, keine vorhandene; `data/secrets.env` ist bewusst nicht eingecheckt. Die
verbleibenden echten Treffer stehen im `CHANGELOG.md` und sind dort als Historie legitim.
`docs/architecture.md` selbst ist inhaltlich aktuell — sie beschreibt korrekt den
`/departureBoard`-Pfad, die 48 Calls/Tag und `update_stammstrecke_hbf.py` als aktives Skript.

**Die tatsächliche Drift sitzt nicht im Markdown, sondern in den Docstrings und Kommentaren des
Quellcodes.**

#### F.1 — VOR-Budget-Tabelle bildet den realen Verbrauch nicht ab — **HOCH**

| | |
| --- | --- |
| **Betroffene Komponente** | `docs/architecture.md` §7, Z. 700–745 |
| **Problembeschreibung** | Die Tabelle führt als Konsumenten nur „Stammstrecke `/departureBoard`" (48) und „Auth-Diagnose" (0–2) und schließt mit **„Tagesbudget gesamt: 48 / 100"**. Zwei Aussagen sind durch **A.1** widerlegt: Der Fließtext behauptet, der Stammstrecken-Monitor sei „der **einzige** automatisierte VOR-Konsument im Projekt", und die Zeile „**Station-Enrichment `location.name`** | **0** (Pfad und Skript 2026-05-11 entfernt)" schreibt genau den Endpunkt auf null, den `test-vor-api.yml` bei jedem Push auf `scripts/**` automatisiert aufruft. Auch der Absatz „Keine automatisierten Nicht-Stammstrecke-Pfade" zählt die Workflows einzeln auf (`update-cycle.yml`, `update-stations.yml`, `manual-full-refresh.yml`) — `test-vor-api.yml` fehlt in der Aufzählung. Ein Betreiber, der sein Budget anhand dieser Tabelle plant, unterschätzt den Verbrauch. |
| **Konkreter Lösungsvorschlag** | Nach der Behebung von **A.1** die Tabelle um eine Zeile „**CI-Smoke-Test `location.name`** (`test-vor-api.yml`, bei Push auf VOR-Pfade)" mit realistischer Obergrenze ergänzen, die Aussage „einziger automatisierter Konsument" entsprechend relativieren und `test-vor-api.yml` in die Workflow-Aufzählung des Schutzmechanismus 1 aufnehmen. Ergänzend als dauerhafte Absicherung: einen Test, der alle Workflow-Dateien nach Aufrufen gegen `routenplaner.verkehrsauskunft.at` durchsucht und fehlschlägt, wenn ein Treffer weder die `external-api-fetch`-Concurrency-Gruppe noch den Preflight-Gate verwendet. |

#### F.2 — `vor.py`-Docstring benennt den falschen Konsumenten — **MITTEL**

| | |
| --- | --- |
| **Betroffene Komponente** | `src/providers/vor.py:14–17` (Modul-Docstring), `:71` |
| **Problembeschreibung** | Der Docstring erklärt: *„`scripts/update_stammstrecke_status.py` is this module's sole consumer"*. Das stimmt seit dem 2026-05-15-Umbau nicht mehr: Aktiver Produzent ist `scripts/update_stammstrecke_hbf.py` (so auch in `update-cycle.yml:453` verdrahtet), `update_stammstrecke_status.py` ist der Legacy-`/trip`-Pfad, der nur noch `_charge_one_request` und einige Helfer per Import beisteuert. Beide Skripte importieren `vor`, es gibt also auch keinen „sole consumer" mehr. Wer die Quota-Rechnung aus diesem Docstring ableitet, sucht die 48 Calls/Tag im falschen Skript. Der zweite Verweis in Z. 71 schreibt dem Monitor weiterhin den `/trip`-Endpunkt zu. |
| **Konkreter Lösungsvorschlag** | Den Docstring auf den Ist-Zustand ziehen: `update_stammstrecke_hbf.py` als aktiven Konsumenten benennen, `update_stammstrecke_status.py` als Legacy-Modul mit weiterverwendeten Helfern kennzeichnen und den Endpunkt von `/trip` auf `/departureBoard` korrigieren — analog zur bereits korrekten Darstellung in `docs/architecture.md` §7. |

#### F.3 — Kommentare verweisen auf gelöschte Skripte (inkl. Zeilennummer) — **MITTEL**

| | |
| --- | --- |
| **Betroffene Komponente** | `src/providers/vor.py:217`, `:238`, `:721` |
| **Problembeschreibung** | Drei Sicherheits-Kommentare stützen ihre Argumentation auf Dateien, die am 2026-05-11 entfernt wurden: Z. 238 nennt *„plus `_limit_reached` in `scripts/update_vor_cache.py`"* als zusätzliche Lesestelle der Quota-Konstante, Z. 721 zitiert sogar eine konkrete Fundstelle *„(`scripts/update_vor_cache.py:87`)"*, und Z. 217 führt *„the cache-update script at `scripts/update_vor_stations.py`"* als Nutzer des Timeout-Deckels. Keine der drei Dateien existiert. Das ist mehr als Kosmetik: Die Kommentare begründen, **warum** bestimmte Clamps nötig sind, und ein Wartender, der die zitierten Stellen nicht findet, kann eine Schutzmaßnahme für obsolet halten und entfernen. |
| **Konkreter Lösungsvorschlag** | Die drei Verweise durch die real existierenden Lesestellen ersetzen (`scripts/update_stammstrecke_hbf.py` bzw. `scripts/update_stammstrecke_status.py:_charge_one_request` und `scripts/preflight_quota_check.py`) und die Begründung beibehalten — die Clamps selbst sind weiterhin richtig. Als Dauerlösung einen Test ergänzen, der alle in `src/` und `scripts/` per Backtick zitierten `scripts/*.py`-Pfade gegen das Dateisystem prüft; das fängt die nächste Löschung automatisch ab. |

#### F.4 — `_merge_colocated_duplicates` verweist auf entfernte Absicherung — **MITTEL**

| | |
| --- | --- |
| **Betroffene Komponente** | `scripts/update_wl_stations.py:1101–1103` und `:1128–1131` |
| **Problembeschreibung** | Der Docstring erklärt zweimal, dass Stops im Band 150–500 m absichtlich **nicht** zusammengeführt werden, weil sie *„disambiguated by DIVA suffix downstream — see `_disambiguate_duplicate_names`"* bzw. *„remain separate entries with the existing DIVA-suffix disambiguation"* seien. Diese Funktion wurde ausweislich des Kommentars 30 Zeilen darüber (`:1072`) am 2026-05-12 entfernt. Der Docstring beschreibt damit eine Sicherheitsnetz-Konstruktion, die es nicht mehr gibt — und genau daraus resultieren die acht Kollisionen aus **D.1**. |
| **Konkreter Lösungsvorschlag** | Docstring und Realität zusammenführen: Entweder die Disambiguierung wie in **D.1** vorgeschlagen wiederherstellen (dann stimmt der Text wieder), oder den Docstring auf den Ist-Zustand korrigieren und die verbleibende Mehrdeutigkeit dort explizit als bekannte Einschränkung benennen. Die derzeitige Mischform — Verweis auf eine gelöschte Funktion — ist die schlechteste Variante, weil sie das Problem als gelöst darstellt. |

#### F.5 — Docstring-Abdeckung 54 %; drei Module ohne Modul-Docstring — **NIEDRIG**

| | |
| --- | --- |
| **Betroffene Komponente** | `src/`, `scripts/` (AST-Auswertung über alle öffentlichen Symbole) |
| **Problembeschreibung** | Von 436 öffentlichen Funktionen/Klassen tragen 236 einen Docstring — **54,1 %**; 200 Symbole sind undokumentiert. Der Kontrast zum Rest des Projekts ist auffällig: Sicherheitsrelevante Stellen sind teils absatzweise kommentiert, während zentrale API-Oberflächen leer bleiben (`GooglePlacesConfig`, `iter_nearby`, `quota_skipped_kinds`, `StationEntry`, `MergeConfig`, `MergeOutcome`, `init_providers`, `build_parser`). Drei Module haben **gar keinen** Modul-Docstring — darunter mit `src/build_feed.py` (**5.106 Zeilen**) das größte und zentralste Modul des Projekts, außerdem `src/feed/merge.py` (813 Z.) und `src/feed/logging_safe.py` (350 Z.). |
| **Konkreter Lösungsvorschlag** | Nicht flächendeckend nachdokumentieren, sondern gezielt: zuerst die drei fehlenden Modul-Docstrings (jeweils 5–10 Zeilen zu Zweck, Ein-/Ausgaben und Position in der Pipeline), danach die öffentlichen Datenklassen und Client-Methoden in `src/places/`. Danach die Quote per `ruff`-Regel `D` (pydocstyle) auf dem erreichten Niveau einfrieren, damit sie nicht zurückfällt — analog zur bereits etablierten C901-Baseline. |

#### F.6 — C901-Baseline veraltet — **NIEDRIG**

| | |
| --- | --- |
| **Betroffene Komponente** | `.c901-baseline.txt` |
| **Problembeschreibung** | `scripts/check_complexity.py` läuft grün (0 neue Verstöße), meldet aber: *„2 pre-existing offender(s) no longer exceed 15"* — `_format_item_content` und `prune_log_file` wurden vereinfacht, stehen aber weiterhin in der Baseline. Solange sie dort stehen, dürfen sie unbemerkt wieder komplexer werden. |
| **Konkreter Lösungsvorschlag** | `scripts/regen_c901_baseline.sh` laufen lassen und die verkleinerte Baseline committen, damit die erreichte Verbesserung festgeschrieben ist. |

---

### 7. Methodik dieses Updates

Alle Aussagen beruhen auf Messungen im Repository, nicht auf Codelektüre allein:

| Prüfung | Werkzeug / Vorgehen |
| --- | --- |
| Test-Suite | `pytest -q -p no:randomly --timeout=120` → **9.186 passed, 2 skipped, 534 s** |
| Feed-Validierung | `xml.etree.ElementTree.parse` + Regex-Scan auf XML-1.0-verbotene Codepoints und `U+FFFD` |
| Kennzahlen-Abgleich | Nachrechnen der README-Tabellen aus `data/stats/*.csv` (Referenz 2026-09-13 18:30 CEST) |
| Richtungs-Ausfall (**B.1**) | Monats- **und tagesweise** `direction`-Verteilung beider Ledger (`cut`/`uniq -c` über die CSV-Spalten) + Bestimmung der letzten Nord-Zeile; Gegenprobe auf dem Ledger-Stand nach dem nächsten Cycle-Tick |
| Quota-Race (**A.2**) | 6-Prozess-`multiprocessing.Pool` gegen eine temporäre Zählerdatei, `WIEN_OEPNV_TEST_QUOTA_BATCH=1`; temporäre Datei nach dem Lauf entfernt |
| ReDoS (**C.2**) | `re.sub`-Zeitmessung über wachsende Eingaben, isoliert und Ende-zu-Ende über `html_to_text`, beide `collapse_newlines`-Pfade |
| Stationen (**D.1/D.3**) | Gruppierung nach Name bzw. `_normalize_token`, Haversine-Distanzen, Byte-Anteil der Alias-Felder, Kalt-/Warm-Zeitmessung von `_station_lookup` / `station_info` |
| Frontend | `check_i18n_coverage.py`, `optimize_site_assets.py --check`, SHA-256-Abgleich der `?v=`-Hashes gegen die Asset-Dateien |
| Doku | AST-Auswertung für Docstring-Abdeckung; Regex-Scan aller `.md` auf tote Datei-Verweise, danach **manuelle** Einzelprüfung jedes Treffers |

**Zwei Einschränkungen, die für die Bewertung der Befunde wichtig sind:**

1. **Die Repo-Historie ist in dieser Session flach** (67 Commits, alle vom 12./13.09.2026). Eine
   Bisektion auf das auslösende Ereignis von **B.1** um den 14.08. war deshalb nicht möglich; die
   Ursachenanalyse dort stützt sich auf die Ledger-Daten und den Code, nicht auf die Historie.

2. **Ein Testfehlschlag in dieser Umgebung ist ein Umgebungsartefakt, kein Projektdefekt.**
   `tests/places/test_client_security.py::test_client_ssrf_protection` schlägt fehl, weil die
   Audit-Umgebung `HTTPS_PROXY` gesetzt hat und `verify_response_ip` daraufhin vorzeitig
   zurückkehrt. Ohne die Proxy-Variablen laufen dieselben Tests grün
   (`env -u HTTPS_PROXY … pytest tests/places/test_client_security.py` → 7 passed), und der oben
   genannte Gesamtlauf mit 9.186 grünen Tests wurde ebenfalls ohne Proxy-Variablen durchgeführt.
   Der Fehlschlag ist gleichwohl **aufschlussreich** — er belegt die Fail-Open-Konstruktion aus
   **B.3** praktisch.

**Read-Only-Nachweis:** `git status --short` war während der gesamten Analysephase leer; die einzige
am Ende geschriebene Datei ist dieser Audit-Bericht. Es wurde kein Projekt-Code, kein Workflow und
keine Konfiguration verändert. Die Race-Condition-Reproduktion (**A.2**) arbeitete auf einer
temporären Zählerdatei, die anschließend entfernt wurde — `data/vor_request_count.json` blieb
unangetastet. Für die Messungen wurden lediglich `requirements.txt` und `requirements-dev.txt` in
die Laufzeitumgebung installiert, also außerhalb des Repositorys.

---

### 8. Empfohlene Reihenfolge

1. **B.1** — Ursache des Nord-Ausfalls bestimmen und beheben; die 30-Tage-Kennzahlen bis dahin
   als richtungs-unvollständig kennzeichnen.
2. **B.2** — Alarmierung für leere Richtungs-Buckets nachrüsten, damit sich **B.1** nicht
   wiederholen kann. *(Ohne diesen Schritt ist Schritt 1 nur eine Einzelfallreparatur.)*
3. **A.1 + F.1** — `test-vor-api.yml` eindämmen und die Budget-Tabelle korrigieren.
4. **A.2 / A.3** — Quota-Reservierung unter das Dateilock ziehen, Sentinel-Pfade angleichen.
5. **D.1 / D.2** — Namens-Disambiguierung wiederherstellen und den Validator um die fehlende
   Prüfkategorie erweitern.
6. **E.2 / E.1** — Deutsche Fehlertexte aus der EN-Oberfläche entfernen, Gate erweitern.
7. **C.2, D.3, E.3, B.3, B.4** — Härtung und Entschlackung.
8. **F.2–F.6** — Docstring- und Baseline-Pflege.
