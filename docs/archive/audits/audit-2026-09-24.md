# Audit 2026-09-24

## Audit Update 10:40 (Europe/Vienna) — P.1 live bestätigt; neuer Fund O.1: Burgenland-Route als Wien-Route im deutschen Feed

**Anlass:** Check-in nach dem Merge von PR #1855 (P.1). Nach dem
Fast-Forward auf `origin/main` (`1bac37e`) wurden beide Feeds des Baus
10:01 geprüft. Dabei fiel im deutschen Feed ein inhaltlich falscher Titel
auf, der vor dem geplanten nächsten Schritt F.2 behandelt wurde
(„Sichtbares im Feed hat Vorrang").

### Live-Kontrolle P.1 (in Ordnung)

`docs/feed.en.xml`, Bau 10:01: Suche nach Platzhalter-Resten ohne führendes
„X" (`(?:ENT|GLO)[0-9a-f]{8,32}X\d+X`) über den ganzen Feed → kein Treffer.
Das N6-Item („Neilreichgasse 20-22, Quellenstraße 189") steht nicht mehr
unter den zehn Plätzen; direkt am Item lässt sich die Reparatur daher nicht
mehr zeigen, der Feed ist aber frei von dieser Form.

### O.1 — `REX 6: Wien Baumgarten (WL) ↔ Ebenfurth`: eine Burgenland-Route belegt Platz 4 (behoben)

- **Betroffene Komponente:** `src/providers/oebb.py` (`_ZWISCHEN_PLAIN_RE`,
  `_VON_NACH_PLAIN_RE`, `_extract_zwischen_routes`), mittelbar
  `data/stations.json` (Alias `Baumgarten` → `Wien Baumgarten (WL)`).
- **Problembeschreibung:** Seit dem Cache-Update 2026-09-22 07:31 steht im
  deutschen Feed `REX 6: Wien Baumgarten (WL) ↔ Ebenfurth` (heute Platz 4
  von 10). Die ÖBB-Beschreibung meint Baumgarten im Burgenland: „zwischen
  Ebenfurth Bahnhof und Baumgarten im Bgld-Schattendorf Bahnhof keine REX
  6-Züge". `im` und `am` stehen in den Grenzwort-Listen der Routenregeln
  als Zeitpräpositionen („Felixdorf Bahnhof am 10.02.2026"); der zweite
  Endpunkt wurde deshalb nach „Baumgarten" abgeschnitten, und dieser Stumpf
  löst über den Alias `Baumgarten` auf die WL-Haltestelle „Wien Baumgarten
  (WL)" auf. Damit galt die Route als Wien ↔ Pendler (Ebenfurth ist
  Pendler), die Meldung wurde als Wien-relevant durchgelassen und ihr Titel
  auf die Wiener Haltestelle umgeschrieben. Nach der strengen Routenregel
  (beide Enden bekannt, mindestens eines in Wien) gehört sie nicht in den
  Feed — sie verdrängt eine echte Meldung (Rang 1) und nennt einen falschen
  Ort (Rang 2).
- **Messung:** Über alle 32 verschiedenen ÖBB-Meldungen der Cache-Historie
  endeten sechs Endpunkte unmittelbar vor `im`/`am`: fünf davon sind echte
  Zeitangaben oder Datums-Fragmente, einer ist dieser Fall. An erfundenen
  Beschreibungen trifft derselbe Schnitt jeden zweiten Endpunkt mit „im/am"
  im Namen: „Brunn am Gebirge" → „Brunn", „Neusiedl am See" → „Neusiedl"
  (beide dann unbekannt), „Wolkersdorf im Weinviertel" → „Wolkersdorf".
- **Konkreter Lösungsvorschlag / Umsetzung:** `_with_place_qualifier`
  hängt beim zweiten Endpunkt ein einzelnes großgeschriebenes Zusatzwort
  nach `im`/`am` wieder an, wenn unmittelbar danach das Stationssuffix
  (`Bahnhof`/`Bahnhst`/`Hbf`/`Bhf`/`Bf`) folgt — die Form, in der ÖBB
  einen vollständigen Stationsnamen schreibt. Zeitangaben haben diese
  Form nie (bei „Felixdorf Bahnhof am 10.02." steht das Suffix *vor* dem
  `am`). Der REX-6-Endpunkt wird „Baumgarten im Bgld-Schattendorf", ist
  unbekannt, die Route fällt. Nebenwirkung: „Brunn am Gebirge" und
  „Neusiedl am See" lösen als zweiter Endpunkt jetzt vollständig auf.
- **Verworfen:** Eine zweite Absicherung — reine WL-Einträge (1751 von
  2243, `source == "wl"`) nie als ÖBB-Endpunkt zulassen. In der
  Historie wäre sie folgenlos gewesen (einziger WL-only-Endpunkt war
  Baumgarten), aber einige WL-only-Einträge tragen Namen, die auch
  S-Bahn-Halte sein könnten (z. B. „Wien Unter St. Veit (WL)"); ein Fehlgriff
  dort verwürfe echte ÖBB-Meldungen, also Rang 1 in die andere Richtung.
- **Restrisiko:** Trägt der ÖBB-Rohtitel selbst nur „Baumgarten ↔
  Ebenfurth", bleibt die Titel-Route relevant. Der Rohtitel ist nicht
  archiviert; die ÖBB-Titel der Historie nennen die Anzeigenamen der
  Stationen, das spricht für den vollen Namen. Nachkontrolle nach dem
  nächsten ÖBB-Cache-Update nach dem Merge.
- **Tests:** `tests/test_oebb_place_qualifier_endpoint.py` (12 Fälle): der
  aufgezeichnete Endpunkt, `_is_relevant` am aufgezeichneten Text,
  `fetch_events` mit drei Rohtitel-Varianten (Item fällt), zwei
  Pendler-Bahnhöfe mit Zusatz, die `von … nach`-Form, vier Zeitangaben,
  die nicht angehängt werden. Zwei Mutationen geprüft, beide gefangen
  (Zusatz nicht angehängt; Suffix-Bedingung entfernt).

### Weitere Beobachtungen am Bau 10:01

- **`4A/80A/N29: Wittelsbachstraße` (Platz 10) ist sauber.** Der am
  2026-09-19/20 notierte doppelte Linienpräfix („4A/80A/N29: 4A. 80A, N29:
  Wittelsbachstraße") steht im aktuellen Feed nicht mehr — die Quelle
  liefert den Titel jetzt ohne die Wiederholung. Der Parser-Befund bleibt
  für künftige Meldungen dieser Form offen, ist aber nicht mehr sichtbar.
- **`U4: Vordere Zollamtsstraße von Marxergasse … bis Unbenannte
  Verkehrsfläche und Rad…` (Platz 8)** ist die bekannte Kappung der Stadt
  Wien bei 100 Zeichen; das „…" setzt `_mark_upstream_truncation`
  (`scripts/update_baustellen_cache.py`) bewusst. Kein neuer Befund.
- **EN, Rang 3:** „N31: Stammersdorf replacementless Discontinued" —
  „ersatzlos aufgelassen" übersetzt Marian wörtlich und mit Großschreibung.
  Vorschlag für später: Glossar-Eintrag „ersatzlos aufgelassen" →
  „closed without replacement". Dokumentiert, nachrangig.
- **F.2 weiterhin latent:** Die drei „Wien Hauptbahnhof ↔ Gramatneusiedl"-
  Items stehen noch im ÖBB-Cache, ihre `first_seen` (08.07., 24.08., 10.09.)
  liegen aber unter den zehn Plätzen, die heute bis zum 16.09.
  zurückreichen. Kein sichtbarer Schaden; bleibt nach O.1 nächster Punkt.

**Priorität nach AGENTS.md:** O.1 betrifft den deutschen Feed direkt
(Rang 1: belegt einen Platz, der einer echten Meldung zusteht; Rang 2:
falscher Ort im Titel) und ging deshalb vor F.2.

## Audit Update 11:24 (Europe/Vienna) — O.1 gemergt; F.2 umgesetzt (ohne Zusammenführen)

**Anlass:** PR #1860 (O.1) wurde um 11:20 gemergt, alle elf Checks grün.
Der ÖBB-Cache wird nur bei inhaltlicher Änderung committet (zuletzt
23.09. 22:01 UTC); die Wirkung auf das REX-6-Item zeigt sich erst mit dem
ersten Zyklus nach dem Merge und wird dann hier nachgetragen. Bis dahin F.2.

### F.2 — Eine ÖBB-Strecke, mehrere Bauphasen: ein Platz (umgesetzt)

- **Betroffene Komponente:** `src/build_feed.py`
  (`_defer_repeated_route_titles`, aufgerufen in `main()` nach der
  Sortierung und vor `_apply_topic_budget`).
- **Problembeschreibung:** unverändert gegenüber
  `audit-2026-09-19_2.md` (F.2/G.7). Drei Items „Wien Hauptbahnhof ↔
  Gramatneusiedl" (03.–05.10., 31.10.–30.11., 05.–07.12.) überstehen beide
  Dedupe-Stufen und das Platzbudget; heute liegen sie unter den zehn
  Plätzen, an einem ruhigen Tag stünde dieselbe Zeile dreimal auf den
  Displays (Rang 1: zwei andere Meldungen fielen weg).
- **Abweichung vom ursprünglichen Vorschlag:** Vorgeschlagen war, die drei
  zu *einem* Item zu falten und die Fenster in der Beschreibung
  aufzuzählen. Beim Lesen der drei Texte verworfen: Die Phasen sind
  verschiedene Maßnahmen — im Oktober fallen einzelne benannte Züge aus,
  im November und Dezember fahren nachts keine Nahverkehrszüge. Ein
  gemeinsamer Text (der der ersten Phase) verfälschte zwei davon
  (Rang 2). Auch die im Audit genannte Alternative — Platzbudget mit dem
  Titel als Schlüssel — reicht nicht: Das Budget behält das *erste* Item
  der Sortierung, und nach `first_seen` sortiert führt die
  Dezember-Phase (erstmals gesehen 10.09.), nicht die Oktober-Phase.
- **Umsetzung:** Von mehreren ÖBB-Items mit wortgleichem Titel
  (Leerraum und Groß-/Kleinschreibung ignoriert) behält das mit dem
  frühesten `starts_at` seinen eigenen Platz; die übrigen rücken hinter
  das Feld, in ihrer Reihenfolge. Nichts wird verworfen, kein Item rückt
  nach vorn. Endet die erste Phase, fällt sie über `_drop_old_items` weg
  und die nächste hält den Platz. Andere Quellen bleiben unberührt — ein
  WL-Titel nennt Linie und Ort, zwei gleiche sind Sache der
  Dedupe-Stufen.
- **Tests:** `tests/test_repeated_route_titles.py` (11 Fälle): die
  aufgezeichnete Reihenfolge (Oktober bleibt, Dezember und November
  rücken nach hinten), nichts fällt weg, Einzelroute unberührt,
  Leerraum/Groß-Klein, fehlender Start zählt als spätester, Gleichstand
  behält die Sortierung, `datetime`-Werte, drei andere Quellen unberührt,
  Ende-zu-Ende durch `main()` (neun WL-Items, drei Phasen, zehn Plätze →
  eine Phase, alle neun WL-Items). Drei Mutationen geprüft, alle
  gefangen (Führung nach Sortierung statt frühestem Fenster;
  Quellen-Prüfung entfernt; Pass nicht verdrahtet).

## Audit Update 11:36 (Europe/Vienna) — O.1 live bestätigt; neuer sichtbarer Fund W.1

### O.1 live (in Ordnung)

Der erste Zyklus nach dem Merge (Commit `7151d69`, 11:31) hat den
ÖBB-Cache neu geschrieben: 11 → 10 Einträge, entfallen ist genau
`REX 6: Wien Baumgarten (WL) ↔ Ebenfurth`, kein anderer Eintrag geändert
oder hinzugekommen. Im deutschen Feed (Bau 11:30) steht es nicht mehr.
Damit ist auch das Restrisiko aus Update 10:40 ausgeräumt — der
ÖBB-Rohtitel trug offenbar den vollen Namen, nicht nur „Baumgarten".

### W.1 — `N66/N68R: N66, Rufbus N68: Quellenplatz` (Platz 2): Linien doppelt im Titel — offen

- **Betroffene Komponente:** `src/providers/wl_lines.py`
  (`_extract_prefix_lines`, `_strip_redundant_line_token`).
- **Problembeschreibung:** Der WL-Titel lautet „N66, Rufbus N68:
  Quellenplatz"; der Provider setzt den erkannten Präfix `N66/N68R:`
  davor, entfernt die Linienaufzählung im Titelrumpf aber nicht. Auf dem
  Display steht dieselbe Linienangabe zweimal, die zweite in anderer
  Schreibweise („Rufbus N68" statt „N68R"). Dieselbe Klasse wie der
  frühere Fund „4A/80A/N29: 4A. 80A, N29: Wittelsbachstraße"
  (Rückstand seit 2026-09-19): Der Parser kennt nur die `/`-Form und
  Einzelcodes, keine Aufzählung mit Komma oder Zusatzwort. Rang 2
  (verstümmelt den Titel, verdrängt nichts).
- **Konkreter Lösungsvorschlag:** Wenn der Titelrumpf mit einer
  Linienaufzählung bis zum ersten Doppelpunkt beginnt, deren Codes
  (nach Entfernen von Zusatzwörtern wie „Rufbus", „Linie(n)" und
  Trennern `,`/`.`/`und`) genau die Linien des Präfixes sind, wird
  sie gestrichen. Vor der Umsetzung an der Cache-Historie messen, wie
  viele Titel diese Form tragen und ob ein Präfix je mehr oder weniger
  Linien nennt als die Aufzählung.

**Nächste Schritte:** F.2-PR (Update 11:24) steht, danach W.1.

## Audit Update 12:07 (Europe/Vienna) — F.2 gemergt; W.1 umgesetzt; Querverweis auf audit-2026-09-23.md

### F.2 (in Ordnung)

PR #1861 gemergt (`8eda342`), alle Checks grün. Die drei
Gramatneusiedl-Phasen stehen heute unter den zehn Plätzen; eine sichtbare
Wirkung im Feed ist erst an einem ruhigeren Tag zu erwarten.

### W.1 — Linienliste nach dem Präfix (umgesetzt)

- **Betroffene Komponente:** `src/providers/wl_lines.py`
  (`LINES_COMPLEX_PREFIX_RE`, `_extract_prefix_lines`,
  `_ensure_line_prefix`, neu `_is_rufbus_twin`).
- **Messung:** 453 Revisionen des WL-Caches, 608 verschiedene Titel.
  Genau zwei Titel beginnen nach dem Präfix mit einer durch Doppelpunkt
  abgeschlossenen Liste der eigenen Linien: `N66/N68R: N66, Rufbus N68:
  Quellenplatz` und `4A/80A/N29: 4A. 80A, N29: Wittelsbachstraße`. Die
  weiteren Titel, deren Rumpf mit einer Linienangabe beginnt, sind
  entweder inhaltlich richtig („27: Linie 25 in Tokiostraße benützen")
  oder alte Cache-Stände, die der heutige Code bereits bereinigt
  („3A: 3A Netzänderung …"). Offen bleibt „25: Linien 25 und 26 Betrieb
  ab …" (Liste ohne Doppelpunkt, Präfix nennt nur eine der beiden
  Linien) — eigener Befund, nicht Teil von W.1.
- **Umsetzung:** `. ` (Punkt mit Leerraum) gilt als Listentrenner, und
  ein einzelner Code vor `Rufbus X` genügt. Der Punkt zählt nur mit
  folgendem Leerraum, damit `13.10:`/`17.30:` keine Liste werden; Wörter
  wie in `10. Bezirk:` lehnt das bestehende strenge Token-Gate ab.
  `relatedLines` nennt den Rufbus `N68R`, der Titel `Rufbus N68`:
  `_is_rufbus_twin` behandelt beide als eine Linie, sonst hieße der
  Präfix `N66/N68R/N68`. Da `_post_filter_wl` gecachte Titel beim
  Feed-Bau neu parst, wirkt die Korrektur beim ersten Bau nach dem
  Merge, ohne auf ein WL-Cache-Update zu warten.
- **Tests:** `tests/test_wl_line_list_repeated_in_title.py` (13 Fälle),
  fünf Mutationen geprüft, alle gefangen.
- **Methodischer Hinweis:** Eine Mutation, die die Quelldatei auf
  *gleiche Länge* umschreibt und innerhalb derselben Sekunde
  zurückgesetzt wird, hinterlässt eine gültig wirkende `.pyc` mit dem
  mutierten Code (Python prüft Zeitstempel und Größe). Der erste volle
  Testlauf nach den Mutationen schlug deshalb an `13.10: Sperre` fehl,
  obwohl die Quelle korrekt war. Nach dem Löschen der `__pycache__`-
  Verzeichnisse: 9831 passed. Künftig nach jeder Mutation den
  Bytecode-Cache löschen oder mit `PYTHONDONTWRITEBYTECODE=1` testen.

### Querverweis auf `audit-2026-09-23.md` (Jules)

- **Punkt 1 (EN, „replacementless Discontinued", „Ab 30. September"):**
  deckt sich mit der Beobachtung in Update 10:40 (Rang 3).
- **Punkt 2 („Minuten" in `docs/statistik.md`):** unverändert offen,
  Rang 3; bereits in `audit-2026-09-20.md` Update 14:17 notiert.
- **Punkt 3 (U4-Titel „… und Rad…"):** Die Kappung stammt aus der Quelle
  (die Stadt Wien kürzt `BEZEICHNUNG` bei 100 Zeichen), und das „…"
  setzt `_mark_upstream_truncation` in
  `scripts/update_baustellen_cache.py` bewusst: Den Rest des Worts
  abzuschneiden würde Information verwerfen, die sich nicht zurückholen
  lässt. Das abgeschnittene Wort ist laut Beschreibung „Radetzkybrücke",
  nicht „Radweg". Der Vorschlag, an Wortgrenzen zu kürzen, widerspricht
  dieser dokumentierten Entscheidung und wird nicht übernommen. Die
  englische Folge („wheel…") ist ein eigener, nachrangiger EN-Befund:
  Ein Fragment mit „…" am Titelende sollte unübersetzt bleiben.

## Audit Update 12:11 (Europe/Vienna) — Korrekturen: fremde Befunde an Code und Daten nachgeprüft

**Anlass:** Hinweis des Betreibers, dass die Befunde von Jules nicht
ungeprüft zu übernehmen sind. Update 12:07 hatte zwei Punkte aus
`audit-2026-09-23.md` ohne eigene Prüfung als „deckt sich" bzw. „offen"
eingetragen. Nachgeprüft wurden alle fremden Punkte vom 20.09. (14:17)
und 23.09. (14:24) sowie eine eigene Aussage aus Update 10:40.

### K.1 — Korrektur zu Update 12:07, Punkt 2 („Minuten"): nicht offen, seit 19.09. geschlossen

In Update 12:07 steht „unverändert offen". Das ist falsch.
`audit-2026-09-19_2.md` hat den Punkt zweimal behandelt und geschlossen:
„Punkt 6 (09-18) — … zweite [Hälfte] ohne Handlungsbedarf" („Die
Überschrift nennt die Einheit ausgeschrieben, die Zelle abgekürzt — das
ist die übliche Tabellenkonvention") und im Register „Geschmacksfrage,
kein Handlungsbedarf". Die von Jules angeführte „projektweite
UI-Konsistenz-Richtlinie (in früheren Audits und Memory festgelegt)"
existiert nicht; die früheren Audits sagen das Gegenteil. Der Punkt
bleibt geschlossen; Entscheidung beim Betreiber.

### K.2 — Jules 23.09. Punkt 1 / 20.09. Finding 2: Befund richtig, Diagnose falsch

- **Befund bestätigt:** `docs/feed.en.xml` zeigt „To: etwa 20 Meter in
  Richtung …" (64A), „Duration: Ab 22. September …" (72A),
  „Ersatzlos Discontinued" und „Duration: Ab 30. September …" (N31).
- **Diagnose widerlegt:** Jules schreibt, das NMT-Modell scheitere an
  diesen Fragmenten. Das Modell sieht sie gar nicht. Eine abschließende
  Folge von mindestens zwei `Label: Wert`-Einträgen („Haltestelle:",
  „Von:", „Nach:", „Dauer:") trennt `_split_label_record` ab, und
  `_render_label_record` rendert sie **bewusst ohne Modell**: Labels
  über das Glossar, Werte wörtlich. Grund (Docstring, Fund vom
  18.09.): Marian vertauschte und verdoppelte die Adressen. Die
  Annahme dahinter — „jeder Wert ist ein Haltestellen- oder
  Straßenname oder eine Hausnummer" — gilt für `Von:`/`Haltestelle:`,
  aber nicht für `Dauer:` („Ab 22. September …") und nicht für den
  Abstandsfall bei `Nach:` („etwa 20 Meter in Richtung …").
- **Vorschlag verworfen:** Glossar-Einträge „Ab" → „From" usw. wirken
  im Glossar-Pass vor *jeder* Übersetzung, also auch in Prosa, wo
  „ab" Teil von „Betrieb ab …", „ab sofort" oder trennbaren Verben ist.
- **Eigener Vorschlag (Rang 3):** In `_render_label_record` die
  bekannten Wertformen deterministisch übersetzen — `Dauer:` („Ab
  <Datum>", „bis voraussichtlich <Monat Jahr>", „etwa HH:MM Uhr",
  Monatsnamen) und den Abstandsfall `Nach: etwa N Meter in Richtung X`
  —, alles andere bleibt wörtlich. Vorher an allen veröffentlichten
  Beschreibungen mit Label-Record messen, welche Wertformen vorkommen.
- **Zusatz zu Finding 2 vom 20.09.:** Vorgeschlagen war, gestrandete
  Präpositionen („Ab", „Von", „Nach") vor dem „…" in
  `_trim_truncation_tail` zu streichen. Diese Kürzung teilen sich DE
  und EN; der Vorschlag änderte also den deutschen Feed (heute z. B.
  „Nach: etwa 20 Meter in Richtung …"), um einen EN-Befund zu beheben
  — gegen die Priorität in AGENTS.md. Verworfen.

### K.3 — Jules 23.09. Punkt 3: Komponente und Wort falsch

Jules verortet die U4-Kappung in `tidy_title` in
`src/providers/baustellen.py`. Eine Funktion `tidy_title` gibt es im
Projekt nicht; die nächstliegende, `_tidy_title_wl` in
`src/providers/wl_text.py`, betrifft nur WL-Titel. Die Kappung kommt aus der Quelle, das „…" setzt
`_mark_upstream_truncation` in `scripts/update_baustellen_cache.py`.
Das abgeschnittene Wort ist laut Beschreibung „Radetzkybrücke", nicht
„Radweg". Die Bewertung aus Update 12:07 bleibt.

### K.4 — Jules 20.09. Finding 3 (`4A. 80A, N29:`): Befund und Ort richtig

Tatsachen und vorgeschlagener Ort (`LINES_COMPLEX_PREFIX_RE`) stimmen.
Umgesetzt in W.1 (PR #1862) erst nach eigener Messung über die
WL-Cache-Historie; dabei zeigte sich zusätzlich der Rufbus-Fall
(`N68R`/`Rufbus N68`), den der Vorschlag nicht abdeckte.

### K.5 — Selbstkorrektur zu Update 10:40 („replacementless Discontinued")

In Update 10:40 steht, Marian übersetze „ersatzlos aufgelassen"
wörtlich und mit Großschreibung. Nur halb richtig: „replacementless"
stammt vom Modell, das großgeschriebene „Discontinued" aus dem
Glossar-Eintrag `"Aufgelassen": "Discontinued"` in `src/build_feed.py`
(angelegt, weil das Modell „Aufgelassen" einst als „Open" übersetzte).
Das Glossar setzt den Eintrag unabhängig von der Stellung im Satz in
seiner festen Schreibweise ein. Vorschlag bleibt ein Glossar-Eintrag
für die ganze Wendung „ersatzlos aufgelassen" → „closed without
replacement" (längster Treffer gewinnt); die Großschreibung mitten im
Satz ist ein eigener kleiner Punkt am Glossar.

**Register fremder Befunde (Stand 12:11):**

| Quelle | Punkt | Befund | Diagnose/Vorschlag | Stand |
| --- | --- | --- | --- | --- |
| 20.09. 14:17 | 1 „Minuten" | Geschmacksfrage | — | geschlossen (19.09.) |
| 20.09. 14:17 | 2 „Ab …" EN | richtig | falsch / ändert DE | verworfen, eigener Vorschlag K.2 |
| 20.09. 14:17 | 3 `4A. 80A, N29:` | richtig | richtig | umgesetzt (W.1) |
| 23.09. 14:24 | 1 EN-Fragmente | richtig | falsch | eigener Vorschlag K.2 |
| 23.09. 14:24 | 2 „Minuten" | wie oben | — | geschlossen |
| 23.09. 14:24 | 3 U4 „Rad…" | Kappung real | falsche Komponente, falsches Wort | bewusstes Verhalten, kein Handlungsbedarf |

## Audit Update 14:10

### Affected Component
`cache/baustellen_d438c3/events.json` / `src/utils/text.py`

### Display Issue Description
The `events.json` payload still contains awkwardly concatenated compound words lacking spaces, like "DerFußgänger- und Radverkehr", "DieFahrtrichtung", "Derrechte Fahrstreifen", "Die restlichenFahrstreifen" and "Eine Zufahrt". These issues bypass the sanitization in `src/utils/text.py` because `_GLUED_WORD_RE` requires two lowercase letters preceding the break or explicitly allows some opening function words (e.g. `Im`, `Am`) but not common articles like `Der` or `Die`.

### Proposed Solution
Modify `_GLUED_WORD_RE` in `src/utils/text.py` to allow a broader set of function words like `Der`, `Die`, `Das`, `Eine` by expanding the list of opening function words explicitly.

---

### Affected Component
`docs/feed.en.xml` / `docs/feed.xml` / `cache/baustellen_d438c3/events.json`

### Display Issue Description
The Baustellen feed titles are occasionally truncated mid-word as seen in `docs/feed.xml` and `docs/feed.en.xml`, such as: "U4: Vordere Zollamtsstraße von Marxergasse und Kleine Marxerbrücke bis Unbenannte Verkehrsfläche und Rad…". This truncation leads to a clumsy English translation "wheel..." (as a translation of "Rad...").

### Proposed Solution
Fix the sanitization/truncation logic in `tidy_title` for `src/providers/baustellen.py` (which truncates titles) to ensure it truncates safely at word boundaries, instead of indiscriminately appending ellipses, or add rules to `build_feed.py` to handle truncated trailing text gracefully before translation.

---

### Affected Component
`docs/feed.en.xml` / `src/build_feed.py`

### Display Issue Description
In `docs/feed.en.xml`, the English translation of "ersatzlos aufgelassen" results in "replacementless Discontinued". It combines a clumsy direct translation of "ersatzlos" ("replacementless") and "Discontinued" but with mixed casing ("replacementless Discontinued"). A similar situation occurs for "N31: Stammersdorf ersatzlos aufgelassen" in the German payload.

### Proposed Solution
Update `_apply_domain_glossary` in `src/build_feed.py` to include mappings for "ersatzlos aufgelassen" to a more natural phrase like "permanently closed" or "discontinued without replacement".
## Audit Update 15:18 (Europe/Vienna) — W.1 gemergt; K.2 umgesetzt; neuer Befund B.1 (Rebuild-Workflow veröffentlicht den Feed nicht)

### W.1 (PR #1862) — gemergt 15:05, Live-Kontrolle offen

Der Rebuild nach dem Merge (Workflow `build-feed.yml`, 15:05–15:07) lief
mit dem neuen Code: `data/first_seen.json` trägt für das N66-Item jetzt die
EN-Übersetzung „N66/N68R: Quellenplatz" (vorher „N66/N68R: N66, N68:
Quellenplatz"). `docs/feed.xml` wurde in diesem Lauf aber nicht committet
(siehe B.1) und zeigt weiter den Stand 15:01 mit dem alten Titel. Die
Live-Kontrolle erfolgt mit dem Zyklus 15:31; Nachtrag folgt.

### K.2 — Werte eines Label-Records ohne Modell ins Englische (umgesetzt)

- **Betroffene Komponente:** `src/build_feed.py` (`_gloss_record_values`,
  `_RECORD_VALUE_RULES`, `_render_label_record`; `_GLOSSARY_BASE`;
  `_TRANSLATION_CACHE_EPOCH` 15 → 16).
- **Problembeschreibung:** siehe Update 12:11, K.2. Drei der zehn EN-Items
  trugen deutsche Reste im Record („Duration: Ab 30. September 2026, etwa
  13:00, bis expected Mai 2027"; „To: etwa 20 Meter in Richtung …";
  „Ersatzlos Discontinued").
- **Messung:** 460 Revisionen des WL-Caches, 557 verschiedene Items, 60
  mit Label-Record. Labels: `Dauer` 40, `Grund` 40, `Haltestelle` 35,
  `Haltestellen` 28, `Zeitraum` 20, `Von` 20, `Nach` 19. Die `Dauer`-Werte
  folgen einer kleinen Grammatik (Präposition, Tag, Monat, „etwa", Uhrzeit,
  vier Endformeln: „Dauer unbekannt", „auf (derzeit) unbestimmte Zeit",
  „bis auf Widerruf", „Nicht absehbar"); `Von`/`Nach` tragen Straße plus
  Hausnummer, „gegenüber N", „vor X", „nach X", „im Zuge X",
  „Nebenfahrbahn", einmal „etwa N Meter in Richtung X", einmal „ersatzlos
  aufgelassen".
- **Umsetzung:** `_gloss_record_values` setzt genau diese Formen um —
  nur im Record, nach dem Glossar (Labels sind dann Platzhalter, „Nach:"
  kann nicht zu „after:" werden) und vor dem Maskieren. Datumspräpositionen
  sind an die folgende Ziffer (oder das „…" eines gekappten Werts)
  gebunden; „Am Schöpfwerk" bleibt ein Haltestellenname. Großschreibung
  nur am Wertanfang, Monatsnamen fest; „12 bis 14" wird „12-14", eine
  Uhrzeitspanne nicht; „30. September" verliert den Punkt, „1.
  Haidequerstraße" nicht. Zwei globale Phrasen: „ersatzlos aufgelassen"
  → „closed without replacement" (richtet auch den N31-Titel),
  „Betriebsschluss" → „end of service".
- **Bewusst ausgeklammert:** `Ersatzhaltestelle:` innerhalb eines
  `Haltestelle`-Werts (2 Fälle) ist kein Label in `_FIELD_LABEL_EN` und
  bleibt wörtlich; „auf Dauer von etwa drei Monaten" (1 Fall). Die
  Großschreibung eines Glossar-Eintrags mitten im Satz (K.5) bleibt ein
  eigener Punkt am Glossar.
- **Tests:** `tests/test_label_record_values_en.py` (23 Fälle): die drei
  Live-Records Ende-zu-Ende, neun `Dauer`-Formen, drei `Von`/`Nach`-Formen,
  Haltestellenname mit „Am", Uhrzeitspanne, Label bleibt Label, Ordinal
  einer Straße, Determinismus, Prosa-Pfad trägt die Regeln nicht, die zwei
  globalen Phrasen, Epoche ≥ 16. Zwei Pins in
  `tests/test_label_record_split.py` von „Duration: Ab …" auf „Duration:
  From …" angepasst. Fünf Mutationen geprüft, alle gefangen (Pass nicht
  aufgerufen; keine Großschreibung am Wertanfang; „am" ohne Ziffern-Anker;
  Bereichsregel ohne Uhrzeit-Ausschluss; Werte vor den Labels ersetzt).

### B.1 — `build-feed.yml` committet nur `data/first_seen.json`, nie die Feed-XMLs (Rang 4, offen)

- **Betroffene Komponente:** `.github/workflows/build-feed.yml`, Schritt
  „Commit and push changes" (`stefanzweifel/git-auto-commit-action`, Pin
  v7 seit 12.09.).
- **Problembeschreibung:** Im Lauf 36003339176 (15:05–15:07) meldet die
  Action vor dem Commit `M README.md`, `M data/first_seen.json`,
  `M docs/feed.en.xml`, `M docs/feed.xml` — committet wird „1 file
  changed": nur `data/first_seen.json`, obwohl alle vier im mehrzeiligen
  `file_pattern` stehen. Dasselbe Bild bei allen sechs Rebuild-Commits
  seit dem 13.09. (der erste nach Anlage der Datei). Folge: Nach einem
  Code-Merge erreicht der korrigierte Feed die Öffentlichkeit erst mit dem
  nächsten Zyklus (bis 30 Minuten später), obwohl der Workflow genau dafür
  da ist („`docs/feed.xml` is the public output"); der Modelllauf je
  Code-Push publiziert nur den Zustand. Zwischenzeitlich passen Zustand
  (neue EN-Übersetzung) und Feed nicht zusammen — folgenlos, weil der
  Zyklus beides überschreibt.
- **Konkreter Lösungsvorschlag:** Den Schritt durch denselben
  Plain-git-Block ersetzen, den `update-cycle.yml` seit dem 26.05. nutzt
  (Validieren der XMLs → `git add` der Allowlist → Commit → Push mit
  Wiederholungen, nie rot). Damit entfällt auch die Abhängigkeit vom
  Action-Download, die dort der Anlass war. Eigener PR, nach K.2.

## Audit Update 15:51 (Europe/Vienna) — W.1 live bestätigt

Nachtrag zu Update 15:18: Der Zyklus 15:30 (Commit `df68c5e`) zeigt in
`docs/feed.xml` und `docs/feed.en.xml` auf Platz 4 `N66/N68R:
Quellenplatz` statt `N66/N68R: N66, Rufbus N68: Quellenplatz` — ohne
neues WL-Cache-Update, weil `_post_filter_wl` die gecachten Titel beim
Bau neu parst. Damit ist W.1 am Produktions-Feed bestätigt. Zeitverlauf
als Beleg für B.1: Merge 15:05, Rebuild-Workflow 15:07 ohne Feed-Commit,
sichtbar erst mit dem Zyklus 15:30.

## Audit Update 16:41 (Europe/Vienna) — K.2 live bestätigt; Beobachtung E.1

### K.2 (PR #1863, gemergt 16:13) — live in Ordnung

Erster Zyklus nach dem Merge (16:30, Commit `d13423c`), `docs/feed.en.xml`:

- Item 3: „N31: Stammersdorf closed without replacement" (vorher
  „replacementless Discontinued"); Record „From: Brünnerstraße opposite
  262 closed without replacement Duration: From 30 September …".
- Item 5 (64A): „To: Approx. 20 metres towards …" (vorher „To: etwa 20
  Meter in Richtung …").
- Item 6 (72A): „Duration: From 22 September …" (vorher „Duration: Ab 22.
  September …").

Die Kappung bei 180 Zeichen schneidet die Records an derselben Stelle wie
zuvor; der deutsche Feed ist unverändert („N31: Stammersdorf ersatzlos
aufgelassen"). Der Epochensprung 15 → 16 lief im Rebuild 16:15 und im
Zyklus 16:30 ohne Fehler durch. Wieder derselbe Verlauf wie bei W.1:
Rebuild-Workflow 16:15 ohne Feed-Commit (B.1), sichtbar mit dem Zyklus.

### E.1 — „Stop stop on line N31" für „Haltestellenauflassung" (Rang 3, offen)

- **Betroffene Komponente:** `src/build_feed.py` (`_GLOSSARY_BASE`).
- **Problembeschreibung:** Die Prosa vor dem Record, „Haltestellenauflassung
  der Linie N31 in Richtung Schwedenplatz U", kommt aus dem Modell als
  „Stop stop on line N31 towards Schwedenplatz U" (vor Epoche 16: „Station
  departure of line N31"). „Haltestellenverlegung" ist im Glossar („stop
  relocation", siehe 64A/72A), das seltenere Kompositum
  „Haltestellenauflassung" nicht — Marian zerlegt es sinnlos.
- **Konkreter Lösungsvorschlag:** Glossar-Eintrag „Haltestellenauflassung"
  → „stop closure" (Plural „Haltestellenauflassungen" → „stop closures")
  mit Epochensprung, damit das gecachte N31-Item neu übersetzt wird. Klein
  genug, um mit dem nächsten Glossar-PR mitzufahren.

### B.1 — umgesetzt (Nachtrag zu Update 15:18)

`.github/workflows/build-feed.yml`: Die Schritte „Pull concurrent remote
changes" und „Commit and push changes" (`git-auto-commit-action`, v7) sind
durch den Schritt „Validate and publish feed outputs" ersetzt — reines
`git` nach dem Muster von `update-cycle.yml`: XML-Validierung beider
Feeds, Staging der bisherigen Allowlist (`data/first_seen.json`,
`docs/feed.xml`, `docs/feed.en.xml`, `README.md`,
`data/stats/stoerungen_*.csv`; `docs/stats-summary.json` bleibt wie bisher
draußen), Commit, Push mit bis zu vier Versuchen und Rebase-Abgleich, nie
rot. Nebenwirkung: Rebuild-Commits tragen den Bot als Autor (bisher
Autor „Origamihase", Committer Bot), wie die Zyklus-Commits. Geprüft:
YAML parst, `bash -n` auf das Skript, `tests/test_en_feed_workflow_deps.py`
(9 Fälle) grün, und eine lokale Simulation des Validierungs- und
Staging-Teils in einem Wegwerf-Repository (alle fünf Ausgaben werden
committet, `stats-summary.json` nicht; ohne Änderung kein Commit; ein
kaputtes `feed.xml` überspringt den Publish mit Warnung). Die Push-Schleife
ist unverändert aus `update-cycle.yml` übernommen. Live-Nachweis: der
erste `chore: rebuild feed`-Commit nach dem Merge muss die Feed-XMLs
tragen.

## Audit Update 18:24 (Europe/Vienna) — B.1 live bestätigt

PR #1865 gemergt 18:21. Der vom Merge selbst ausgelöste Rebuild
(`build-feed.yml`) committete um 18:22 als `b7ae093` — Autor
`github-actions[bot]` — genau `docs/feed.xml`, `docs/feed.en.xml` und
`README.md`; `lastBuildDate` des Feeds ist 18:22:17, also die
Rebuild-Zeit und nicht mehr die des vorigen Zyklus (18:01).
`data/first_seen.json` fehlt in diesem Commit zu Recht: Seit dem Zyklus
18:01 hatte sich der Zustand nicht geändert, und der Schritt stagt nur,
was sich geändert hat. Damit veröffentlicht ein Code-Merge den Feed
wieder innerhalb von etwa einer Minute statt mit dem nächsten Zyklus.
Zum Vergleich die sechs Rebuild-Commits davor (13.09.–24.09. 16:15):
jeweils nur `data/first_seen.json`.

Nachtrag B.1: Das Lauf-Log (Job 107725225373, Schritt „Validate and publish
feed outputs") endet mit `f894cbe2d..b7ae093cf HEAD -> main` und „Feed
outputs published on attempt 1/4." — der Push gelang im ersten Versuch.

### K.6 — Fremder PR #1864 (Jules, „Audit Update 14:10") an Code und Daten geprüft

Der PR fügt einen englischen Block **zwischen** Update 12:11 und Update
15:18 dieser Datei ein (chronologisch passend, aber nicht angehängt) mit
drei Befunden:

1. **Verklebte Wörter im Baustellen-Cache** („DerFußgänger- und
   Radverkehr", „DieFahrtrichtung", „Derrechte Fahrstreifen", „Die
   restlichenFahrstreifen", „Eine Zufahrt"), Diagnose: `_GLUED_WORD_RE`
   kenne die Artikel `Der`/`Die`/`Das`/`Eine` nicht. **Diagnose falsch,
   Befund zu vier Fünfteln gegenstandslos:** Der Cache ist Rohtext; im
   Feed-Pfad läuft `repair_glued_words` (`build_feed.py`, Zusammenfassung).
   Durch diese Funktion geprüft: „DerFußgänger-" → „Der Fußgänger-",
   „DieFahrtrichtung" → „Die Fahrtrichtung" (kommt im Cache gar nicht
   vor), „restlichenFahrstreifen" → „restlichen Fahrstreifen" — alle
   drei repariert, weil der Regex an zwei Kleinbuchstaben vor einem
   Großbuchstaben ansetzt; ein Artikel in der Funktionswortliste änderte
   daran nichts. „Eine Zufahrt" ist keine Verklebung. Übrig bleibt
   „Derrechte" (Item „Burggasse 67", heute nicht im Feed): Kleinbuchstabe
   nach „Der", also keine Großbuchstaben-Grenze — der vorgeschlagene
   Listeneintrag würde auch das nicht treffen. Eine Regel „Der/Die/Das +
   Kleinbuchstabe" wäre mehrdeutig („Derby", „Diesel", „Dasein"); Rang 2
   nur, falls ein solches Item die zehn Plätze erreicht — vorgemerkt,
   kein Handlungsbedarf.
2. **U4 „Rad…" → EN „wheel…"** mit Vorschlag `tidy_title` in
   `baustellen.py`: Wiederholung von 23.09. Punkt 3, Bewertung wie K.3
   (Kappung kommt von der Stadt Wien, Funktion existiert nicht). Der
   EN-Teil („wheel…") ist ein eigener Rang-3-Punkt (Update 12:07).
3. **„replacementless Discontinued"** mit Vorschlag Glossar-Eintrag:
   Befund richtig, seit 16:13 durch #1863 (K.2, „closed without
   replacement") behoben und um 16:30 live bestätigt — der PR ist an
   dieser Stelle überholt.

Entscheidung über den PR beim Betreiber; textuell kollidiert er nicht
mit den Anhängen dieser Datei.

### E.1 — umgesetzt

`_GLOSSARY_BASE` in `src/build_feed.py`: „Haltestellenauflassung" →
„stop closure", „Haltestellenauflassungen" → „stop closures", neben den
bestehenden `Haltestellenverlegung(en)`-Einträgen. Messung über 563
WL-Items der Cache-Historie: 4 Singular, 1 Plural; daneben 36 „Aufgelassene"
und 9 „aufgelassen", die das Glossar schon kannte. Epoche 16 → 17, weil
das gecachte N31-Item („Stop stop on line N31 …") sonst bestehen bliebe —
der Quelltext und damit sein Digest haben sich nicht geändert. Tests:
`tests/test_glossary_haltestellenauflassung.py` (4 Fälle).

## Audit Update 19:16 (Europe/Vienna)

Nachtrag zu K.6: PR #1864 (Jules) wurde um 18:23 gemerged (Merge-Commit
`6e821d6`). Der Satz „Entscheidung über den PR beim Betreiber" in Update
18:24 war beim Schreiben schon überholt; der eingefügte Block „Audit Update
14:10" steht seither zwischen Update 12:11 und Update 15:18 dieser Datei.

### E.1 — live noch nicht prüfbar

Der Rebuild nach dem Merge von #1866 (Run 36031566937, Job 107741329363,
19:03, Commit `6614d11`) hat wegen Epoche 17 alle zehn Items neu übersetzt.
Ergebnis in `docs/feed.en.xml`: **8 von 10 Items stehen auf Deutsch** — nur
„U4: Vordere Zollamtsstraße …" (Item 8) und „4A/80A/N29: Wittelsbachstraße"
(Item 10) sind englisch. Das N31-Item, an dem E.1 zu prüfen wäre, ist
darunter (Titel und Beschreibung deutsch). Der deutsche Feed ist unberührt.

### N.1 — Nonce-abhängige Massen-Fallbacks bei voller Neuübersetzung (Rang 3, nur EN)

**Betroffene Komponente:** `src/build_feed.py` — `_PLACEHOLDER_NONCE =
secrets.token_hex(8)` (Zeile 1977) mit den daraus abgeleiteten
Platzhalter-Formaten und Regexes; `_translate_text_attempt` mit dem
Residual-Guard `_RESIDUAL_PLACEHOLDER_RE`. Ausgabe Rang 2
(`docs/feed.en.xml`).

**Problembeschreibung:** Das Log des 19:03-Laufs enthält zehnmal
„Translation for identity … left a residual placeholder; discarding and
falling back to source." für acht Identitäten (`1d0ceb…`, `31d6ac…`,
`c6414f…` = N31 Titel und Beschreibung, `aa349c…` = U2 Titel, `8df3b1…`,
`35bf40…`, `b25a18…` = D Titel und Beschreibung, `8632b6…` = 36A/36B
Titel); keine „dropped … verbatim entity"-Zeile, kein Pipeline-Fehler. Der
Rebuild um 16:15 (Job 107672132879, Epoche 16) hatte **dieselben zehn
Identitäten** neu übersetzt — ohne eine einzige Residual-Warnung.
Codeunterschied zwischen den beiden Läufen: zwei Glossar-Einträge
(„Haltestellenauflassung(en)"), die nur im N31-Text vorkommen, und die
Epochennummer. Für die anderen sieben Identitäten war die Modelleingabe
bis auf einen Wert identisch: das Nonce. Es wird pro Prozess neu gewürfelt
(16 Hex-Zeichen) und steckt in jedem Platzhalter (`XENT<nonce>X<n>X`,
`XGLO<nonce>X<n>X`); seit dem 2026-06-01 ist im CHANGELOG dokumentiert,
dass Marian Nonce-Fragmente verstümmelt (gedropptes Hex-Zeichen, `…de…` →
`…en…`, verirrtes `from`). Ein Nonce, dessen SentencePiece-Zerlegung solche
Fragmente enthält, trifft fast jeden Text desselben Laufs — die Häufung ist
der Fingerabdruck eines Prozess-, nicht eines Item-Faktors. Das Nonce wird
nicht geloggt; die Hypothese ist aus den Logs deshalb nicht direkt
beweisbar, und in der Sandbox ist das Modell nicht lauffähig. Der Guard
hat wie vorgesehen gearbeitet: kein Sentinel wurde veröffentlicht, nichts
wurde gecacht, jeder gescheiterte Wert wird im nächsten Zyklus mit neuem
Nonce erneut versucht.

Messung über alle „chore: rebuild feed"-Commits seit 17.09. (Spalte
„deutsch" = EN-Items, deren Beschreibung gleich der deutschen ist; Epoche
= Wert vor/nach dem Merge davor):

| Rebuild | PR | Epoche | deutsch |
| --- | --- | --- | --- |
| 17.09. 22:43 | #1826 | 6 → 7 | 0 |
| 18.09. 15:33 | #1828 | 7 → 8 | 7 |
| 18.09. 16:33 | #1829 | 8 → 9 | 3 |
| 18.09. 18:04 | #1831 | 9 → 10 | 2 |
| 18.09. 18:28 | #1832 | 10 → 11 | 2 |
| 18.09. 18:59 | #1833 | 11 → 12 | 0 |
| 19.09. 15:18 | #1842 | 12 → 13 | 0 |
| 19.09. 16:23 | #1843 | 13 → 14 | 0 |
| 24.09. 16:15 | #1863 | 15 → 16 | 0 |
| 24.09. 19:04 | #1866 | 16 → 17 | 8 |

(Rebuilds ohne Epochenwechsel: neun, davon einer mit 4 — 19.09. 22:00,
#1852 —, zwei mit 2, drei mit 1, drei mit 0.) Zehn Epochen-Bumps: fünfmal
0, dreimal 2–3, zweimal 7–8. Über alle 344 EN-Feed-Commits seit 17.09.
hatten 88 mindestens eine deutsche Beschreibung, 8 mindestens drei.

Davon zu trennen ist eine zweite Klasse: Items, die über viele Nonces
hinweg scheitern — „REX 6: Wien Baumgarten (WL) ↔ Ebenfurth" (22.09., 26
Zyklen in Folge), „11A: Buses stop Vorgartenstraße …" (19.09., rund 22
Zyklen, Titel englisch, Beschreibung deutsch), „72A: Kraftwerk Simmering"
und „12A/14A: Pilgramgasse U" (18.09., über drei Epochen-Bumps hinweg).
Das sind item-spezifische Ursachen (verworfene Entitäten, das später
behobene `14AX`-Debris), kein Lotterie-Effekt; keines davon ist heute im
Feed.

**Konkreter Lösungsvorschlag:**

1. Zuerst beobachten: Die Zyklen 19:31, 20:01 und 20:31 wiederholen die
   acht Übersetzungen mit jeweils neuem Nonce. Erholen sich die Items
   (erwartet: die meisten schon um 19:31), ist der Prozess-Faktor
   bestätigt; ein Item, das drei Zyklen lang deutsch bleibt, gehört in die
   zweite Klasse und braucht sein Log.
2. Nonce einmal pro Build auf INFO loggen („Placeholder nonce: …"). Nach
   Prozessende ist der Wert wertlos, aber nur so lässt sich ein schlechter
   Lauf mit seinem Nonce korrelieren und die Hypothese in CI belegen.
3. Build-interner Retry: Scheitert ein Feld am Residual-Guard oder an
   `_entities_dropped_by_translation`, denselben Text einmal mit einem
   zweiten, unabhängigen Nonce übersetzen. Dafür müssen Nonce, beide
   Formate und die drei nonce-gebundenen Regexes (`_ENTITY_PLACEHOLDER_RE`,
   `_UNMASK_PLACEHOLDER_RE`, `_PLACEHOLDER_DEBRIS_RE`) in ein kleines
   Schema-Objekt wandern, das `_mask_entities`, `_apply_domain_glossary`
   und `_unmask_entities` als Parameter erhalten. Bei prozessweiter
   Korrelation macht ein zweiter Wurf aus einem 8-von-10-Lauf einen mit
   voraussichtlich 0–1.
4. Erst nach Messung, optional: Nonce-Alphabet ohne Buchstaben (nur
   Ziffern), um die Klasse „übersetztes Fragment" zu entfernen. Ob
   Ziffernfolgen für Marian stabiler sind als Hex, lässt sich nur mit dem
   Modell messen (CI, nicht Sandbox).

Einordnung: Rang 3, aber heute der sichtbarste EN-Befund; der deutsche
Feed ist nicht betroffen. Kein PR vor Abschluss von Punkt 1.

### Nebenbefund (Rang 4): `docs/feed-health.json`

Die Datei ist per `.gitignore` (Zeile 20) ausgeschlossen und nicht
getrackt; `docs/index.md` und `docs/development.md` sagen das korrekt. Der
Kommentar in `src/feed/reporting.py` Zeile 779 („committed to `main` by the
`update-cycle.yml` cron tick, served on GitHub Pages") und `update-cycle.yml`
Zeile 548 („feed-health") beschreiben dagegen eine Veröffentlichung, die
nicht stattfindet — veraltete Kommentare. Außerdem schreibt
`tests/test_topic_budget.py` (`test_the_budget_is_wired_into_main`, eigener
Testlauf 18:36) über `main()` einen Health-Report in das echte `docs/`:
der Test biegt `OUT_PATH` ins Temp-Verzeichnis um, die beiden
`FEED_HEALTH*`-Pfade nicht (Inhalt danach: `feed_path` unter
`/tmp/pytest-…`, Provider-Status „Keine aktuellen Daten"). Folgenlos für
Git und Pages; Kommentar-Hygiene und Test-Isolation, nachrangig.

## Audit Update 19:51 (Europe/Vienna)

### N.1 — Punkt 1 abgeschlossen: Self-Heal im Zyklus 19:31

Commit `7f4e772` (19:31): **0 von 10** EN-Beschreibungen deutsch (19:04:
8 von 10). Alle acht Felder wurden im nächsten Prozess mit neuem Nonce
erfolgreich übersetzt — der Prozess-Faktor ist damit belegt, ohne dass ein
Item in die zweite Klasse (item-spezifisch) fiel. PR #1867 (Audit N.1)
ist offen, CI grün. Punkt 2 des Vorschlags (Nonce loggen) ist unten
umgesetzt; Punkt 3 (build-interner Retry) bleibt Vorschlag.

### E.1 — live verifiziert, mit Einschränkung

Item 4 um 19:31: Titel „N31: Stammersdorf closed without replacement",
Beschreibung „stop closure of line N31 towards Schwedenplatz U Stop:
Stammersdorf From: Brünnerstraße opposite 262 closed without replacement
Duration: From 30 September 2026, approx. 13:00, until expected May 2027
Reason: pipeline works". Glossar und Record-Rendering greifen; der kleine
Satzanfang ist E.2 (unten).

### D.1 — Kalenderdatum im Titel zerrissen (Rang 3, nur EN)

**Betroffene Komponente:** `src/build_feed.py`, `_mask_entities` /
`_LINE_ENTITY_RE`; Ausgabe `docs/feed.en.xml`.

**Problembeschreibung:** Item 5 um 19:31: „U2: Folding ramps out of
service on **2709.2026**" (deutsch: „am 27.09.2026"); um 18:30 stand
dasselbe Item mit „on 27.09.2026" im Feed. `data/first_seen.json`
desselben Commits enthält die gleiche Verstümmelung für „27A/28A/29A:
Event on 2709.2026". Ursache aus dem Masker reproduziert (ohne Modell):
`_LINE_ENTITY_RE` maskiert den Tag „27" als linienförmige Zahl allein,
das Modell sieht `XENT…X3X.09.2026` — ein Platzhalter, der ohne
Leerzeichen an einem Punkt klebt (dieselbe Form wie beim `N6`-Fall vom
19.09.) — und lässt den Punkt weg. Messung (jeder achte EN-Feed-Commit seit
15.08., 118 verschiedene übersetzte Titel): 8 Titel mit Datum, **3 davon
verstümmelt** (die beiden von heute und ÖBB „Update 1 (1609.2026 07:22)"
vom 16.09.). In Beschreibungen kamen Daten in der Stichprobe nur zweimal
vor, beide intakt. Beide heutigen Werte sind als Erfolg gecacht; ohne
Eingriff blieben sie bis zum Ablauf der Items (27.09.) stehen.

**Konkreter Lösungsvorschlag — umgesetzt:** `_DATE_ENTITY_RE`
(`\b\d{1,2}\.\d{1,2}\.\d{4}\b`) maskiert das Datum **vor** dem
Linien-Durchlauf als eine wortgleiche Entität; damit greift auch
`_entities_dropped_by_translation`, falls das Modell das Datum verliert.
Für die zwei vergifteten Cache-Werte kein Epochensprung (der würde alle
Items erneut durch die Nonce-Lotterie aus N.1 schicken), sondern ein
gezielter Self-Heal: `_cached_translation_defect` erklärt einen gecachten
EN-Wert zum Miss, wenn ihm ein Datum des deutschen Quelltexts fehlt
(zusätzlich zum bestehenden Rest-Platzhalter-Check). Tests:
`tests/test_translation_date_entity.py` (16 Fälle); die Property-Tests des
Maskers (`unmask(mask(x)) == x`) laufen unverändert durch.

### E.2 — EN-Beschreibungen beginnen klein (Rang 3, nur EN)

**Betroffene Komponente:** `src/build_feed.py`, Render-Pfad in
`_apply_lang_overlay`; Ausgabe `docs/feed.en.xml`.

**Problembeschreibung:** 5 der 10 Beschreibungen um 19:31 beginnen mit
„stop relocation of line …" bzw. „stop closure of line …". Das Glossar
setzt Substantive als englische Gattungswörter klein ein, weil sie auch
mitten im Satz stehen; die Großschreibung aus C.3
(`_capitalise_title_body`) erreichte nur den Titel. Messung (jeder sechste
EN-Feed-Commit seit 10.09., 169 verschiedene EN-Beschreibungen): **45
beginnen klein**, obwohl der deutsche Quelltext groß beginnt — „irregular
intervals" 22-mal, „stop relocation" 14-mal, dazu „service from",
„diversion from", „stop closure", „roadworks in", „construction works",
„replacement bus", „service obstruction". Kein neuer Befund von heute,
aber heute am sichtbarsten.

**Konkreter Lösungsvorschlag — umgesetzt:** `_capitalise_sentence_start`
hebt genau den ersten Buchstaben der gerenderten EN-Beschreibung an (kein
`str.capitalize`, das Linienkürzel und Stationsnamen zerstören würde);
`_capitalise_title_body` delegiert daran. Der Eingriff liegt im
Render-Pfad, wirkt also auch auf die 45 gecachten Werte ohne
Epochensprung. Tests: `tests/test_en_summary_sentence_start.py` (16
Fälle).

### W.2 — „Wegen …" ohne „because of" (Rang 3, nur EN, dokumentiert)

Item 10 um 19:31: „roadworks in the area of Justgasse / Ruthnergasse are
redirected to lines 36A and 36B." für „Wegen Straßenbauarbeiten im Bereich
Justgasse / Ruthnergasse werden die Linien 36A und 36B umgeleitet." — das
Modell lässt „Wegen" fallen und macht die Baustelle zum Subjekt. Derselbe
Wortlaut steht seit 18.09. in `data/first_seen.json` (also über mehrere
Nonces hinweg gleich, kein Lotterie-Effekt). Messung (jeder sechste
EN-Feed-Commit seit 01.09., 136 DE/EN-Paare): 30 Beschreibungen beginnen
mit „Wegen", **2 davon** ohne „because of"/„due to" — beide mit einem
Glossar-Platzhalter direkt hinter „Wegen" („Straßenbauarbeiten",
„Bauarbeiten"); das D-Item („Wegen Gleisbauarbeiten …" → „Because of
track construction works …") zeigt, dass die Form meist funktioniert.
Kein Eingriff: Ein Glossar-Eintrag für „wegen" wäre im Satzinneren falsch
platziert, und eine Phrasenregel für zwei Vorkommen ist nicht belegt genug.
Vorgemerkt; erneut prüfen, wenn ein weiteres „Wegen"-Item kippt.

### Nonce-Logging (N.1 Punkt 2) — umgesetzt

Die Zeile „Übersetzungs-Pipeline … geladen" trägt jetzt das
Platzhalter-Nonce des Builds. Der Wert ist außerhalb des Prozesses wertlos
(jeder Lauf würfelt neu) und alle Quelltexte des Laufs sind zu diesem
Zeitpunkt bereits geholt; er erlaubt künftig, einen Lauf mit
Residual-Fehlschlägen mit seinem Nonce zu korrelieren.

Verifikation der Änderungen: acht Mutationen gegen die neuen Testdateien
(Datums-Durchlauf entfernt / hinter den Linien-Durchlauf verschoben /
Datums-Check im Self-Heal entfernt / symmetrische Mengendifferenz /
Rest-Platzhalter-Check entfernt / Beschreibung nicht großgeschrieben /
`str.capitalize` / Titel-Helfer delegiert nicht) — jede wurde von dem im
Docstring genannten Test gefangen; `__pycache__` nach jeder Mutation
geleert. Statische Prüfungen und die volle Suite: siehe PR.

## Audit Update 20:34 (Europe/Vienna)

PR #1868 (D.1, E.2, Nonce-Logging) wurde um 20:25 gemerged. Live-Verlauf
über den Rebuild (`fbc6c99`, 20:26) und den Zyklus 20:31 (`f7ff578`):

### E.2 — live

20:26: **0 von 10** EN-Beschreibungen beginnen klein (vorher 5). „Stop
relocation of line 7A …", „Stop closure of line N31 …" — aus dem Cache
gerendert, ohne Neuübersetzung.

### D.1 — live, mit einem Umweg über N.1

Rebuild 20:26 (Job 107773196140): „Cache self-heal: missing or mangled
date in EN title for aa349c…; retrying." — der vergiftete U2-Titel wurde
wie vorgesehen verworfen. Die Neuübersetzung scheiterte jedoch am
Residual-Guard („left a residual placeholder"), das Item fiel für einen
Zyklus komplett auf Deutsch zurück (title_ok=False → beide Felder deutsch,
1 von 10). Zyklus 20:31 (Job 107775089424): Self-Heal erneut (es war
nichts gecacht worden), diesmal ohne Warnung — Item 6 lautet **„U2:
Folding ramps out of service on 27.09.2026"**, Datum intakt. 0 von 10
deutsch.

Der zweite vergiftete Wert („27A/28A/29A: event on 2709.2026") steht
weiter in `data/first_seen.json`: Der Self-Heal läuft — wie die
Epochen-Verdrängung — nur für Items, die gerendert werden, und dieses
Item liegt außerhalb der zehn Plätze. Er heilt, sobald das Item wieder in
den Feed kommt; bis dahin sieht ihn niemand.

### N.1 — erste Nonce-Datenpunkte

Seit #1868 steht das Nonce in der Pipeline-Ladezeile. Beide heutigen
Läufe hatten genau einen Modellaufruf (den U2-Titel, maskiert
`XGLO…: XENT… am XENT…`):

| Lauf | Nonce | Ergebnis |
| --- | --- | --- |
| 20:26 Rebuild | `a4a49e4f0d4a1889` | Rest-Platzhalter → Fallback |
| 20:31 Zyklus | `53e52bb6c30f96c9` | sauber |

Gleicher Text, gleicher Code, anderes Nonce — die Lotterie aus N.1 in
einem Fall reproduziert. Zu wenig für eine Aussage über Alphabet oder
Fragmente; die Zeile sammelt ab jetzt bei jedem Lauf. Punkt 3 des
Vorschlags (build-interner Retry mit zweitem Nonce) hätte den
Zyklus-Ausfall um 20:26 voraussichtlich vermieden; bleibt Vorschlag, Rang
3.

## Audit Update 21:04 (Europe/Vienna)

### Manueller Full Refresh 20:56–21:00 — Auswertung der Logs

Vom Betreiber ausgelöst (`manual-full-refresh.yml`, Runner-Job ab 20:56:42,
Logs als ZIP übergeben). Ergebnis-Commit `73255e2` „chore: full project
refresh (manual trigger) [skip ci]" um 20:59:55, Push
`ebd8c15b0..73255e20c main -> main` über `git-auto-commit-action` im
ersten Versuch; der Zyklus 21:01 (`67181be`) setzte danach normal auf.

Was der Lauf getan hat:

| Schritt | Ergebnis |
| --- | --- |
| Baustellen-Cache | 16 von 71 Meldungen behalten (55 ohne ÖPNV-Bezug) |
| Wiener-Linien-Cache | 68 Items; 5 Anzeigetafel-Kurzmeldungen in die Langfassung übernommen |
| ÖBB-Cache | 10 Items (zweimal geladen: eigener Schritt und erneut im Stationsverzeichnis-Schritt) |
| Stammstrecke | 1 VAO-Request (`/departureBoard`, 28 Abfahrten, Meidling=1, Praterstern=0), Zähler in `data/vor_request_count.json` fortgeschrieben |
| Stationsverzeichnis | neu gebaut, Validierung: 2.244 Stationen, 1 geografisches Duplikat, sonst 0 Befunde |
| Feed | 91 → 87 nach Fuzzy-Dedupe, 10 Items, F.2 aktiv („2 weitere Zeitfenster hinter das Feld") |

**Übersetzung: nicht berührt.** Der Feed-Bau dauerte 3,0 s statt der
üblichen ~10 s, das Log enthält keine Zeile „Übersetzungs-Pipeline …
geladen": Alle zehn EN-Felder kamen unter Epoche 17 aus dem Cache, das
Modell wurde gar nicht erst geladen. Der EN-Feed nach dem Lauf: 0 von 10
deutsch, 0 kleine Satzanfänge, U2-Datum intakt — identisch mit 20:31.
Das bestätigt die Einschätzung aus dem Chat vor Update 18:24: Ein Full
Refresh liefert für
N.1/D.1 keine Datenpunkte (kein Nonce, kein Modellaufruf); er erneuert
Caches und Stationsverzeichnis.

### Nebenbefund (Rang 4): kein Quota-Pre-Flight im manuellen Workflow

`manual-full-refresh.yml` ruft `update_stammstrecke_hbf.py` direkt nach
„Validate VOR secrets" auf; der Schritt „VAO quota pre-flight"
(`update-cycle.yml` Z. 426, `scripts/preflight_quota_check.py`) fehlt.
Die zweite Schicht greift (`_charge_one_request` reserviert den Slot vor
dem Call, Z. 206 des Skripts), ein manueller Lauf kann das Tagesbudget
also nicht überziehen — aber er verbraucht einen der 100 Requests, ohne
dass der Workflow vorher prüft, ob der Tag ihn noch hergibt (heute: 42
projiziert um 20:30, Budget 100). **Vorschlag:** den Pre-Flight-Schritt
aus `update-cycle.yml` vor „Refresh Stammstrecke status" übernehmen, mit
demselben `quota_ok`-Gate. Außerdem verwendet der Workflow weiterhin
`git-auto-commit-action` (v7) mit vorgelagertem Reconcile-Schritt — für
einen manuell ausgelösten Lauf ohne Nebenläufigkeit unkritisch; B.1 betraf
`build-feed.yml`.

### Stammstrecke Nordrichtung — Stand unverändert

`data/stats/stammstrecke_2026.csv`: `Praterstern` zuletzt 2026-08-14
13:57, seit **41 Tagen** stumm (Tagesreihe: 13.08. 26, 14.08. 7, danach
0 Zeilen bei ~41 pro Tag Richtung Meidling). Beide heutigen Läufe fragen
beide Richtungen ab und loggen „DEGRADED — Richtung Praterstern ohne
Messwerte"; `docs/statistik.md` trägt den Abdeckungshinweis. Befund B.1
der Audits vom 13.09., 17.09. und 19.09. (betrieblich, kein Code) —
keine neue Erkenntnis, nur der aktualisierte Zähler.

## Audit Update 23:23 (Europe/Vienna)

### T.1 — Gekappte Baustellen-Titel aus der eigenen Beschreibung vervollständigt (Rang 2, DE-Feed)

Anlass: Frage des Betreibers zu Item 8, „U4: Vordere Zollamtsstraße von
Marxergasse und Kleine Marxerbrücke bis Unbenannte Verkehrsfläche und
Rad…", ob es dafür eine schönere Lösung gibt, von der auch vergleichbare
Meldungen profitieren.

**Betroffene Komponente:** `src/build_feed.py`, `_post_filter_baustellen`
(Titelbildung der Stadt-Wien-Baustellen beim Lesen des Caches); Quelle des
Schnitts ist `BEZEICHNUNG` im WFS der Stadt Wien (Kappung bei 100
Zeichen, markiert durch `_mark_upstream_truncation` in
`scripts/update_baustellen_cache.py`, Audit 12.09.).

**Problembeschreibung:** Der Titel endet mitten im Wort („Rad…") und nennt
mit „Unbenannte Verkehrsfläche" einen Platzhalter der Stadt für ein
namenloses Straßenstück. Auf dem Display ist der Endpunkt der Sperre
damit nicht lesbar, obwohl die Beschreibung desselben Items ihn
vollständig nennt: „… im Abschnitt von der Kleine Marxerbrücke bis und in
Richtung zur Radetzkybrücke …". Messung über die Cache-Historie seit Juni
(22 verschiedene Baustellen-Titel, jeder dritte Commit): 3 Titel gekappt —
„Rad…" (Beschreibung bietet „Radetzkybrücke" und „Radweg"),
„Schlachthausgas…" (Beschreibung: „Schlachthausgasse"), „Otto Wagner
Hofpavillon…" (ganzes Wort, 99-Zeichen-Fall mit gestrichenem
Anführungszeichen; nichts zu ergänzen). „Unbenannte Verkehrsfläche" kommt
einmal vor. Rang 2, weil der deutsche Feed betroffen ist; kein Item wird
verdrängt.

**Konkreter Lösungsvorschlag — umgesetzt:** `_repair_baustellen_title`
läuft im Post-Filter vor dem Linien-/Bahnhofspräfix:

1. **Wortvervollständigung aus der Beschreibung.** Endet der Titel mit
   „…", wird das angeschnittene Fragment (mindestens drei Buchstaben,
   Bindestriche erlaubt) in der Beschreibung desselben Items gesucht. Genau
   ein Kandidat → übernommen. Mehrere Kandidaten → nur einer, der in der
   Beschreibung hinter einer Bereichspräposition steht („bis", „zur",
   „zum", „nach", „von", „ab", „zwischen", „Richtung"), also dort, wo der
   Endpunkt eines „von … bis …"-Titels steht; „zur Radetzkybrücke" gewinnt
   so gegen „Der Radweg". Zwei solche Kandidaten oder keiner → Titel bleibt
   wie geliefert. Das Auslassungszeichen entfällt nach der Ergänzung: Der
   Titel endet dann mit einem vollständigen Endpunkt.
2. **Platzhalter streichen.** „Unbenannte Verkehrsfläche" fällt aus einer
   Endpunkt-Liste, die auch einen echten Namen nennt („bis Unbenannte
   Verkehrsfläche und Radetzkybrücke" → „bis Radetzkybrücke"); als
   einziger Endpunkt bleibt es stehen.

Ergebnis am Cache von heute (16 Items): „U4: Vordere Zollamtsstraße von
Marxergasse und Kleine Marxerbrücke bis Radetzkybrücke" und „Landstraßer
Hauptstraße von Emmerich-Teuber-Platz und Juchgasse und Apostelgasse bis
Schlachthausgasse"; die übrigen 14 Titel unverändert. GUID, `first_seen` und Feed-Position bleiben, weil der
Zustand über die GUID aus dem Rohtitel geschlüsselt ist
(`_state_key_for_item`); der englische Titel wird über den
Quelltext-Digest einmal neu übersetzt (bisher „… to unnamed traffic are…").
Der Cache selbst bleibt die unveränderte Quelle; das Skript verweist im
Docstring auf die Reparatur. Tests: `tests/test_baustellen_title_repair.py`
(20 Fälle, darunter die drei Live-Titel wörtlich); fünf Mutationen
(Reparatur nicht aufgerufen / Präpositionsfilter entfernt /
Mindestlänge entfernt / Platzhalter auch als einziger Endpunkt gestrichen
/ Auslassungszeichen behalten) jeweils vom benannten Test gefangen,
`__pycache__` nach jeder Mutation geleert. Statische Prüfungen sauber;
volle Suite: siehe PR.
