requests>=2.33.0,<2.34
python-dateutil>=2.9,<3
defusedxml>=0.7.1,<1
openpyxl>=3.1.5,<4
dnspython>=2.0.0,<3
# XXE Mitigation applied
# Translation engine (Helsinki-NLP/opus-mt-de-en, lazy-loaded in
# src.build_feed._get_translation_pipeline). ``torch`` is intentionally
# omitted here — the GitHub Actions workflow installs the CPU-only
# wheel from the official PyTorch index ahead of this requirements file
# to keep the runner footprint small. Local contributors who exercise
# the EN feed path can install ``torch`` separately via the same
# index (see ``.github/workflows/build-feed.yml``).
transformers>=4.41,<5
sentencepiece>=0.2.1,<1
# pyhafas removed 2026-05-09: the released wheel on PyPI does not export
# ``pyhafas.profile.OEBBProfile`` so the Stammstrecke monitor was migrated
# to the VOR/VAO ReST API. The active producer since 2026-05-15 is
# ``scripts/update_stammstrecke_hbf.py`` (``/departureBoard`` at Wien Hbf);
# it reuses the shared infrastructure (pending-trip ledger, quota charger,
# circuit breaker tuning) from the legacy ``scripts/update_stammstrecke_status.py``
# via import.
