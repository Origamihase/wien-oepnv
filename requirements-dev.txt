-r requirements.txt
mypy>=1.10,<1.11
ruff>=0.4.7,<0.5
bandit>=1.9.4,<2.0
pip-audit>=2.7,<3
hypothesis>=6.152.4
pytest>=9.0.3
responses>=0.26.0,<1
pytest-timeout>=2.4.0,<3
pytest-cov>=5.0,<7
pre-commit>=4.6.0,<5
# Static-site asset pipeline (docs/site.html + docs/assets/*). The
# minifiers are pure-Python and required by both the pre-commit drift
# check and the manual ``scripts/optimize_site_assets.py`` run. Pillow
# handles the resize step for the train sprite and footer backdrop;
# the lossy image binaries (pngquant, optipng, jpegoptim) are only
# needed when refreshing the committed PNG/JPEG assets and the script
# degrades gracefully if any of them is missing.
rcssmin>=1.2.2,<2
rjsmin>=1.2,<2
Pillow>=10.0,<13
